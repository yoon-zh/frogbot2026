# Nav2 Travel 模式参数文件。
# Travel 使用先验 2D/3D 地图重定位链路，并使用室内低负载 MPPI 平滑控制配置。
# AMCL 节点参数块；负责静态地图定位和粒子滤波。
# 改错节点名或层级：AMCL 会读不到参数并回退默认值。
amcl:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # alpha1 表示机器人转动时的转角噪声系数；决定 AMCL 对转角里程计的信任程度。
    # 调大：更不相信转角里程计、粒子更分散；调小：更相信转角里程计、收敛更紧。
    alpha1: 0.2
    # alpha2 表示直行引起的转角噪声系数；决定平移误差如何映射到朝向误差。
    # 调大：更容易把直行误差解释成角度漂移；调小：更相信直行时朝向稳定。
    alpha2: 0.2
    # alpha3 表示机器人平移时的平移噪声系数；决定 AMCL 对位移里程计的信任程度。
    # 调大：更不相信平移里程计、粒子更分散；调小：更相信平移里程计、定位更紧。
    alpha3: 0.2
    # alpha4 表示转动引起的平移噪声系数；决定转弯误差如何扩散到位置估计。
    # 调大：转弯时位置更容易发散；调小：转弯时更相信位置估计。
    alpha4: 0.2
    # alpha5 表示额外运动噪声系数；用于补偿模型未覆盖的随机扰动。
    # 调大：鲁棒性更强但粒子更散；调小：估计更集中但更依赖模型正确。
    alpha5: 0.2
    # 机器人基坐标系；AMCL 会把定位结果对齐到该机体坐标。
    # 改成错误坐标系：定位姿态会与真实车体不一致。
    base_frame_id: "base_link"
    # beam skip 距离阈值；用于判断哪些激光束可以被视作异常并跳过。
    # 调大：更容易忽略偏离模型的量测；调小：更严格地要求每束参与匹配。
    beam_skip_distance: 0.5
    # beam skip 误差比例阈值；决定异常束占多大比例才触发跳束。
    # 调大：更宽松、不易触发跳束；调小：更敏感、更容易启用跳束。
    beam_skip_error_threshold: 0.9
    # beam skip 通过阈值；决定多少束一致时才接受当前扫描。
    # 调大：更保守、更强调一致性；调小：更激进、更容易接受当前扫描。
    beam_skip_threshold: 0.3
    # 是否启用 beam skip；用于处理局部遮挡或动态障碍导致的异常激光束。
    # 改为 true：对异常束更宽容；改为 false：所有束都严格参与匹配。
    do_beamskip: true
    # AMCL 输出的全局坐标系；通常与地图坐标系一致。
    # 改成其它坐标系：下游导航的 TF 链会对不上。
    global_frame_id: "map"
    # 短量测分布参数；决定激光被近距离遮挡时的概率衰减速度。
    # 调大：更容易把短量测视作合理遮挡；调小：更容易把短量测视作异常。
    lambda_short: 0.1
    # 似然场最大匹配距离；决定离地图障碍多远仍能贡献激光匹配分。
    # 调大：远离障碍的量测也能参与评分；调小：只有更贴近障碍的量测才有高分。
    laser_likelihood_max_dist: 2.0
    # 定位时接受的最大激光距离；超过该值的量测不会参与 AMCL。
    # 调大：可利用更远量测但噪声也更多；调小：更保守但远距信息更少。
    laser_max_range: 20.0
    # 定位时接受的最小激光距离；用于过滤本体附近或近场噪声。
    # 调大：更能滤掉近场噪声；调小：会保留更多近距离量测。
    laser_min_range: -1.0
    # 激光观测模型类型；决定 AMCL 如何计算扫描与地图的匹配分。
    # 改成其它模型：定位收敛特性和容错性都会变化。
    laser_model_type: "likelihood_field"
    # 单次参与 AMCL 匹配的最大激光束数量；用于平衡定位信息量和算力开销。
    # 调大：匹配更细致但 CPU 更高；调小：计算更省但匹配信息更少。
    max_beams: 50
    # 最大粒子数；决定 AMCL 最多可以保留多少候选位姿。
    # 调大：定位更稳但算力和内存更高；调小：更省资源但复杂场景更易丢定位。
    max_particles: 1000
    # 最小粒子数；决定 AMCL 至少保留多少候选位姿。
    # 调大：低不确定性时也保持更稳但更耗资源；调小：更省资源但抗突发误差更弱。
    min_particles: 300
    # 里程计坐标系；AMCL 会基于该坐标系接收运动信息。
    # 改成错误坐标系：定位更新会与里程计链路错位。
    odom_frame_id: "odom"
    # 自适应粒子滤波误差上界；用于估算需要多少粒子才能满足当前精度。
    # 调大：更容易减少粒子数、算力更省；调小：更严格地要求粒子数量。
    pf_err: 0.05
    # 自适应粒子滤波置信度；决定希望以多高置信度覆盖真实位姿。
    # 调大：更追求高置信度、粒子数倾向增多；调小：更容易接受较少粒子。
    pf_z: 0.99
    # 快速恢复系数；控制异常时向粒子群快速注入随机粒子的速度。
    # 调大：恢复更积极但更容易抖动；调小：恢复更慢但状态更稳。
    recovery_alpha_fast: 0.0
    # 慢速恢复系数；控制长期失配时随机粒子的注入速度。
    # 调大：长期偏移时更快扩散搜索；调小：更保守但恢复更慢。
    recovery_alpha_slow: 0.0
    # 重采样间隔；决定 AMCL 每做多少次更新后执行一次粒子重采样。
    # 调大：重采样更少、效率更高；调小：重采样更频繁、收敛更快但更耗算力。
    resample_interval: 1
    # 运动模型类型；决定 AMCL 如何把里程计运动映射到粒子。
    # 改成不匹配的模型：定位响应和误差传播会失真。
    robot_model_type: "nav2_amcl::DifferentialMotionModel"
    # 定位结果保存频率；决定 AMCL 多久把当前位姿写回参数。
    # 调大：保存更频繁、恢复更及时；调小：写入更少但重启后恢复更慢。
    save_pose_rate: 0.5
    # 命中模型标准差；决定激光命中障碍时的误差容忍范围。
    # 调大：对偏差更宽容；调小：更强调精确命中。
    sigma_hit: 0.2
    # 是否广播 map 到 odom 的 TF；决定 AMCL 是否直接输出定位变换。
    # 改为 true：由 AMCL 提供 TF；改为 false：需要外部链路自行维护该 TF。
    # Travel 中 localizer 与 AMCL 都不直接发 TF；prior_map_tf_authority 是唯一 map -> odom owner。
    tf_broadcast: false
    # TF 容忍时间；决定该模块接受多大的 TF 时间偏差。
    # 调大：对 TF 延迟更宽松；调小：更严格但更容易因延迟失败。
    transform_tolerance: 1.0
    # 最小更新或进展阈值；低于它时系统通常认为变化不够明显。
    # 调大：更不敏感；调小：更敏感。
    update_min_a: 0.05
    # 最小更新或进展阈值；低于它时系统通常认为变化不够明显。
    # 调大：更不敏感；调小：更敏感。
    update_min_d: 0.05
    # 命中项权重；决定与地图精确对齐的量测在总评分中的影响。
    # 调大：更强调精确命中；调小：命中项影响减弱。
    z_hit: 0.5
    # 超量程项权重；决定远距或无返回量测在评分中的影响。
    # 调大：更重视超量程量测；调小：更弱化这类量测。
    z_max: 0.05
    # 随机量测项权重；决定异常量测在评分中的影响。
    # 调大：对异常量测更宽容；调小：更依赖模型解释量测。
    z_rand: 0.5
    # 短量测项权重；决定近距离遮挡量测在评分中的影响。
    # 调大：更容易把短量测视作合理；调小：更容易把它视作异常。
    z_short: 0.05
    # 输入或输出话题名；决定该模块从哪里取数据或向哪里发数据。
    # 改错话题：上下游节点会直接断链。
    scan_topic: scan

# Travel 唯一 map -> odom 发布者。localizer 提供初始种子，AMCL 提供运行中先验地图校正。
prior_map_tf_authority:
  ros__parameters:
    use_sim_time: false
    map_frame: map
    odom_frame: odom
    localizer_transform_topic: /localizer/map_to_odom
    localizer_status_topic: /localizer/status
    odom_topic: /fastlio2/lio_odom
    amcl_pose_topic: /amcl_pose
    amcl_initialpose_topic: /amcl/initialpose
    initialpose_topic: /initialpose
    status_topic: /travel/prior_map_tf/status
    publish_hz: 20.0
    status_hz: 5.0
    tf_future_tolerance_s: 0.10
    max_odom_pose_skew_s: 0.15
    amcl_settle_s: 0.75
    amcl_stale_s: 3.0
    # AMCL 在走廊和桌椅附近会产生厘米/角度级来回波动；先对候选做短窗一致性检查。
    amcl_candidate_window_size: 5
    amcl_min_consistent_samples: 3
    amcl_window_reset_s: 1.5
    max_amcl_window_translation_spread_m: 0.05
    max_amcl_window_yaw_spread_rad: 0.04
    # 稳定候选最多每秒释放一次，避免 map -> odom 在 5Hz AMCL burst 中连续移动。
    min_correction_interval_s: 1.0
    max_position_variance: 0.25
    max_yaw_variance: 0.12
    # 拒绝走廊对称结构导致的大范围粒子跳变；可信小校正仍会逐步释放。
    max_target_base_jump_m: 0.75
    max_target_yaw_jump_rad: 0.45
    correction_alpha: 0.15
    translation_deadband_m: 0.05
    yaw_deadband_rad: 0.035
    max_translation_step_m: 0.03
    max_yaw_step_rad: 0.01
    # map -> odom 的小角度变化会被长 odom 杠杆臂放大，因此直接限制车体等效跳变。
    max_base_step_m: 0.04
    # 稳定 AMCL 目标只在 20Hz TF 发布周期连续追踪；三重速度上限避免厘米级离散地图跳变。
    max_translation_correction_speed_mps: 0.015
    max_yaw_correction_speed_rps: 0.006
    max_base_correction_speed_mps: 0.02

# 控制门同时输出可机读停车原因；0.40s 指令窗容忍偶发 MPPI 漏周期但不保持旧非零速度。
localization_cmd_gate:
  ros__parameters:
    status_timeout_s: 0.5
    authority_status_topic: /travel/prior_map_tf/status
    authority_timeout_s: 0.6
    obstacle_timeout_s: 0.5
    cmd_timeout_s: 0.40
    output_timeout_s: 0.25
    publish_hz: 20.0
    status_publish_hz: 5.0

# 碰撞后处理只移除无法越过线速度死区的混合平移分量，不再放大角速度。
post_collision_cmd_conditioner:
  ros__parameters:
    reference_timeout_s: 0.20
    slowdown_ratio_threshold: 0.80
    linear_deadband: 0.14
    in_place_linear_threshold: 0.02
    turning_angular_threshold: 0.16
    angular_zero_threshold: 0.01

# Foxglove may label the coarse-pose tool with its selected display frame even
# though /initialpose values are global-map coordinates. Before map->odom exists
# that frame cannot be transformed, so Travel normalizes the message to map.
initialpose_relocalize_bridge:
  ros__parameters:
    assume_initialpose_is_map: true

# BT Navigator 节点参数块；负责组织规划、控制和恢复动作。
# 改错节点名或层级：导航行为树会读不到参数或启动失败。
bt_navigator:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 全局参考坐标系；决定规划、行为或代价地图在 TF 树中的根坐标。
    # 改成错误坐标系：地图、路径和机器人位姿会整体错位。
    global_frame: map
    # 机器人基坐标系；决定导航模块认为“机器人本体”对应哪个 TF。
    # 改成错误坐标系：碰撞检查、控制和行为判断都会失真。
    robot_base_frame: base_link
    # 输入或输出话题名；决定该模块从哪里取数据或向哪里发数据。
    # 改错话题：上下游节点会直接断链。
    odom_topic: /fastlio2/lio_odom
    # 行为树循环周期毫秒数；决定导航行为树 tick 的频率。
    # 调大：行为树响应更慢但更省 CPU；调小：响应更快但线程负载更高。
    bt_loop_duration: 50
    # 动作和服务默认超时；用于判定下游节点是否失联。
    # 调大：对慢节点更宽容；调小：更快暴露超时但更容易误判。
    default_server_timeout: 20
    # 等待服务就绪的超时；决定启动阶段等待下游组件多久。
    # 调大：更能容忍慢启动；调小：启动失败会更快暴露。
    wait_for_service_timeout: 1000
    # NavigateToPose 行为树路径；Travel launch 会覆盖为安装目录中的 fail-stop BT。
    # 缺少该 key 时 RewrittenYaml 不会生成覆盖项，Nav2 会回退默认 recovery BT。
    default_nav_to_pose_bt_xml: ""
    # NavigateThroughPoses 行为树路径；Travel launch 会覆盖为安装目录中的 fail-stop BT。
    # 缺少该 key 时 RewrittenYaml 不会生成覆盖项，Nav2 会回退默认 recovery BT。
    default_nav_through_poses_bt_xml: ""
    # 行为树插件库列表；决定导航行为树能加载哪些 BT 节点。
    # 增删插件库：会改变可用行为或导致某些节点加载失败。
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_assisted_teleop_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node
      - nav2_is_battery_charging_condition_bt_node
# Navigate Through Poses 辅助节点参数块；负责该动作接口的基础 ROS 参数。
# 改错节点名或层级：对应动作接口会读不到参数。
bt_navigator_navigate_through_poses_rclcpp_node:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
# Navigate To Pose 辅助节点参数块；负责该动作接口的基础 ROS 参数。
# 改错节点名或层级：对应动作接口会读不到参数。
bt_navigator_navigate_to_pose_rclcpp_node:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
# Controller Server 节点参数块；负责局部轨迹跟踪与速度控制。
# 改错节点名或层级：局部控制器会回退默认值或启动失败。
controller_server:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # TF 容忍时间；决定该模块接受多大的 TF 时间偏差。
    # 调大：对 TF 延迟更宽松；调小：更严格但更容易因延迟失败。
    transform_tolerance: 0.5
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 15Hz 与 model_dt=1/15s 配对；实车 20Hz 持续漏周期，稳定 15Hz 比抖动的 20Hz 更平滑。
    controller_frequency: 15.0
    # 前向速度死区阈值；低于该值的前向速度会被视为零。
    # 调大：更能压制微抖动；调小：更容易保留细小前进速度。
    min_x_velocity_threshold: 0.001
    # 侧向速度死区阈值；低于该值的横向速度会被视为零。
    # 调大：更能压制横向噪声；调小：更容易保留微小侧移。
    min_y_velocity_threshold: 0.5
    # 角速度死区阈值；低于该值的角速度会被视为零。
    # 调大：更能抑制原地抖头；调小：更容易保留细小转向。
    min_theta_velocity_threshold: 0.001
    # 控制失败容忍度；决定局部控制短时失败时还能容忍多大误差。
    # 调大：系统更宽容、不易立刻失败；调小：更严格、更快进入恢复。
    failure_tolerance: 2.0
    # 进度检查器名称；用于选择 controller server 里启用哪一个卡住判定器。
    # 改错名称：控制器会找不到对应检查器并启动失败。
    progress_checker_plugin: "progress_checker"
    # 目标检查器列表；决定局部控制器可选择哪些到点判定器。
    # 增删检查器：会改变到点判定能力或导致引用失败。
    goal_checker_plugins: ["general_goal_checker"]
    # 控制器插件列表；决定当前局部控制使用哪些控制器实现。
    # 增删控制器：会改变局部跟踪能力或导致启动失败。
    controller_plugins: ["FollowPath"]
    # 进度检查器参数块；负责判断机器人是否长时间原地不动。
    # 改成其它结构或缺少子参数：卡住检测会异常。
    progress_checker:
      # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
      # 改成不存在或不兼容的插件：对应功能块会加载失败。
      plugin: "nav2_controller::PoseProgressChecker"
      # 最小有效移动半径；在该时间窗内位移小于此值会被视为没前进。
      # 调大：更容易判定卡住；调小：更不容易触发卡住。
      required_movement_radius: 0.10
      # 原地转向超过该角度也视为有效进展，避免可原地转弯底盘被平移检查器误判卡住。
      required_movement_angle: 0.15
      # 移动超时时间；决定多久没有明显进展就判定为卡住。
      # 调大：更宽容、更晚判定卡住；调小：更严格、更早进入恢复。
      movement_time_allowance: 15.0
    # 目标检查器参数块；负责位置和朝向的到点判定。
    # 改成其它结构或缺少子参数：到点判断会失真。
    general_goal_checker:
      # 状态保持开关；决定模块是否保留上一时刻内部状态。
      # 改为 true：连续性更好；改为 false：每次都独立处理。
      stateful: true
      # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
      # 改成不存在或不兼容的插件：对应功能块会加载失败。
      plugin: "nav2_controller::SimpleGoalChecker"
      # 平面位置到点容差；进入该半径内可视为位置达到目标。
      # 调大：更容易判定到点；调小：更严格、更接近目标才算到点。
      xy_goal_tolerance: 0.35
      # 朝向到点容差；角度误差小于该值可视为方向对正。
      # 调大：更容易判定朝向达标；调小：更严格、更要求精确对正。
      yaw_goal_tolerance: 0.20
    # MPPI 控制器参数块；负责采样候选轨迹并结合 costmap 代价做局部避障。
    # 改成其它结构或缺少子参数：局部跟踪行为会明显变化。
    FollowPath:
      # Rotation Shim 只处理大角度初始偏差；普通弯道和终点朝向交给 MPPI，避免多层控制器反复抢占。
      plugin: "nav2_rotation_shim_controller::RotationShimController"
      primary_controller: "nav2_mppi_controller::MPPIController"
      angular_dist_threshold: 0.65
      angular_disengage_threshold: 0.35
      forward_sampling_distance: 0.65
      rotate_to_heading_angular_vel: 0.24
      max_angular_accel: 0.65
      simulate_ahead_time: 1.0
      rotate_to_goal_heading: false
      # 使用上一条 shim 命令爬升，避免闭环反馈在静摩擦区每周期都重置为 0.65/15 rad/s。
      closed_loop: false
      # 保持约 1.6s horizon，同时降低单核采样与 critic 负载，消除持续漏周期。
      time_steps: 24
      model_dt: 0.0666667
      batch_size: 128
      vx_std: 0.16
      vy_std: 0.0
      wz_std: 0.10
      vx_max: 0.30
      vx_min: 0.0
      vy_max: 0.0
      wz_max: 0.65
      ax_max: 0.40
      ax_min: -0.8
      az_max: 2.0
      iteration_count: 1
      prune_distance: 4.0
      transform_tolerance: 0.5
      temperature: 0.3
      gamma: 0.015
      motion_model: "DiffDrive"
      publish_critics_stats: false
      visualize: false
      reset_period: 1.0
      regenerate_noises: false

      TrajectoryVisualizer:
        trajectory_step: 5
        time_step: 3

      critics:
        - "ConstraintCritic"
        - "CostCritic"
        - "GoalCritic"
        - "GoalAngleCritic"
        - "PathAlignCritic"
        - "PathFollowCritic"
        - "PathAngleCritic"
        - "VelocityDeadbandCritic"

      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.0

      CostCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.5
        critical_cost: 300.0
        consider_footprint: true
        collision_cost: 1000000.0
        near_goal_distance: 1.0

      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 0.6

      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.0
        threshold_to_consider: 0.5

      VelocityDeadbandCritic:
        enabled: true
        cost_power: 1
        # Only discourage commands below measured breakaway speed; do not force every small steering correction into a spin.
        cost_weight: 15.0
        deadband_velocities: [0.12, 0.0, 0.08]

      PathAlignCritic:
        enabled: true
        cost_power: 1
        # Lower path-center pressure lets MPPI take a smooth local detour around desks and corridor obstacles.
        cost_weight: 6.0
        max_path_occupancy_ratio: 0.05
        trajectory_point_step: 5
        threshold_to_consider: 0.5
        offset_from_furthest: 6
        use_path_orientations: false

      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 12.0
        offset_from_furthest: 5
        threshold_to_consider: 1.4

      PathAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.0
        offset_from_furthest: 4
        threshold_to_consider: 0.5
        max_angle_to_furthest: 1.0
        mode: 0
# 局部代价地图参数块；负责近场避障和局部环境表达。
# 改错节点名或层级：局部代价地图会读不到参数。
local_costmap:
  # 代价地图命名空间参数块；用于承载该 costmap 的具体 ROS 参数。
  # 改错这一层级：costmap 可能不会读取到下方配置。
  local_costmap:
    # ROS 参数入口；下方配置会直接注入对应节点。
    # 改错这一层级：节点可能完全读不到这些参数。
    ros__parameters:
      # 代价地图更新频率；决定障碍信息每秒刷新多少次。
      # 调大：环境更新更及时但 CPU 更高；调小：更省资源但环境变化反映更慢。
      update_frequency: 10.0
      # 代价地图发布频率；决定对外发布地图消息的频率。
      # 调大：下游拿到新地图更快；调小：带宽和 CPU 开销更低。
      publish_frequency: 2.0
      # 全局参考坐标系；决定规划、行为或代价地图在 TF 树中的根坐标。
      # 改成错误坐标系：地图、路径和机器人位姿会整体错位。
      global_frame: odom
      # 机器人基坐标系；决定导航模块认为“机器人本体”对应哪个 TF。
      # 改成错误坐标系：碰撞检查、控制和行为判断都会失真。
      robot_base_frame: base_link
      # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
      # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
      use_sim_time: false
      # 滚动窗口开关；决定代价地图是固定在世界坐标还是跟着机器人移动。
      # 改为 true：地图跟车移动；改为 false：地图固定在全局坐标。
      rolling_window: true
      # 地图宽度；决定 costmap 在 X 方向覆盖的物理范围。
      # 调大：覆盖更广但内存和更新开销更高；调小：更省资源但看不到更远区域。
      width: 6
      # 地图高度；决定 costmap 在 Y 方向覆盖的物理范围。
      # 调大：覆盖更广但内存和更新开销更高；调小：更省资源但看不到更远区域。
      height: 6
      # 地图分辨率；决定每个栅格代表的物理尺寸。
      # 调大：地图更粗、更省内存；调小：地图更细、更精确但更耗资源。
      resolution: 0.05
      transform_tolerance: 0.8
      # 650x500mm 整车外廓各增加 25mm 安全余量；polygon 仍允许 DiffDrive 原地转向。
      footprint: "[[0.35, 0.275], [0.35, -0.275], [-0.35, -0.275], [-0.35, 0.275]]"
      # 代价地图层列表；决定当前 costmap 会叠加哪些地图层。
      # 增删地图层：会直接改变障碍信息来源和代价图行为。
      plugins: ["obstacle_layer", "inflation_layer"]
      # 膨胀层参数块；负责给障碍物外围增加安全缓冲。
      # 改错结构或参数：障碍安全边界会偏大或偏小。
      inflation_layer:
        # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
        # 改成不存在或不兼容的插件：对应功能块会加载失败。
        plugin: "nav2_costmap_2d::InflationLayer"
        # 代价场参数；决定障碍物周围代价衰减和安全边界形状。
        # 调大：代价场变化更明显或更保守；调小：更激进或更平缓。
        cost_scaling_factor: 2.5
        # 代价场参数；决定障碍物周围代价衰减和安全边界形状。
        # 调大：代价场变化更明显或更保守；调小：更激进或更平缓。
        inflation_radius: 0.40
      # 障碍物层参数块；负责把传感器观测直接写入代价地图。
      # 改错结构或参数：代价地图会漏障碍或误报障碍。
      obstacle_layer:
        # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
        # 改成不存在或不兼容的插件：对应功能块会加载失败。
        plugin: "nav2_costmap_2d::ObstacleLayer"
        # 功能开关；决定当前参数块对应的能力是否真正参与运行。
        # 改为 true：该能力生效；改为 false：该能力被关闭。
        enabled: true
        # 观测源列表；决定该层从哪些传感器配置块读取数据。
        # 增删观测源：会直接改变写入地图的数据来源。
        observation_sources: pointcloud
        # PointCloud 观测参数块；负责描述点云如何写入地图。
        # 改错结构或参数：点云障碍投影会失真。
        pointcloud:
          # 输入或输出话题名；决定该模块从哪里取数据或向哪里发数据。
          # 改错话题：上下游节点会直接断链。
          topic: /fastlio2/body_cloud_nav2
          # 输入消息类型；决定当前观测源按哪种 ROS 消息格式解析。
          # 改成错误类型：数据解析会失败。
          data_type: "PointCloud2"
          transform_tolerance: 0.8
          # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
          # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
          max_obstacle_height: 1.0
          # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
          # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
          min_obstacle_height: 0.12
          # 是否允许清障；决定观测源能否清掉旧障碍。
          # 改为 true：地图更容易清掉旧障碍；改为 false：旧障碍更容易残留。
          clearing: true
          # 是否允许标障；决定观测源命中的障碍能否写入地图。
          # 改为 true：新障碍会被写入；改为 false：该源只清障不标障。
          marking: true
          # 最大障碍标记距离；超过该值的观测不会被写成障碍。
          # 调大：更远障碍也会入图；调小：只保留更近的障碍。
          obstacle_max_range: 3.5
          # 最小障碍标记距离；用于滤掉本体附近或近场噪声。
          # 调大：更容易滤掉近场点；调小：更容易把近场物体写入。
          obstacle_min_range: 0.45
          # 最大清障距离；决定射线清障能清到多远。
          # 调大：更远区域也能被清空；调小：清障范围缩短。
          raytrace_max_range: 4.0
          # 最小清障距离；决定离传感器太近的区域是否参与清障。
          # 调大：近场更不参与清障；调小：更贴近本体开始清障。
          raytrace_min_range: 0.1
          # 无穷远量测是否有效；决定无穷远点是否参与清障判断。
          # 改为 true：更容易把无返回视作空闲；改为 false：忽略无穷远量测。
          inf_is_valid: false
          observation_persistence: 0.0
          expected_update_rate: 0.2
          # 是否启用清障功能；用于总开关控制当前观测源清障。
          # 改为 true：允许清掉旧障碍；改为 false：该源不再清障。
          clearing_enabled: true
          # 是否启用标障功能；用于总开关控制当前观测源标障。
          # 改为 true：允许写入新障碍；改为 false：该源不再标障。
          marking_enabled: true
      # 静态层参数块；负责把静态地图障碍叠加到代价地图。
      # 改错结构或参数：静态地图可能无法正确叠加。
      static_layer:
        # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
        # 改成不存在或不兼容的插件：对应功能块会加载失败。
        plugin: "nav2_costmap_2d::StaticLayer"
        # 是否使用 transient local 订阅地图；决定晚加入节点能否拿到已发布地图。
        # 改为 true：更能接收到历史地图；改为 false：更依赖地图重新发布。
        map_subscribe_transient_local: true
      # 是否总是发送完整代价地图；决定 costmap 发布全量还是增量更新。
      # 改为 true：下游更稳但带宽更高；改为 false：更省带宽但对订阅端要求更高。
      always_send_full_costmap: false
# 全局代价地图参数块；负责全局规划使用的障碍表达。
# 改错节点名或层级：全局代价地图会读不到参数。
global_costmap:
  # 代价地图命名空间参数块；用于承载该 costmap 的具体 ROS 参数。
  # 改错这一层级：costmap 可能不会读取到下方配置。
  global_costmap:
    # ROS 参数入口；下方配置会直接注入对应节点。
    # 改错这一层级：节点可能完全读不到这些参数。
    ros__parameters:
      # 代价地图更新频率；决定障碍信息每秒刷新多少次。
      # 调大：环境更新更及时但 CPU 更高；调小：更省资源但环境变化反映更慢。
      update_frequency: 1.0
      # 代价地图发布频率；决定对外发布地图消息的频率。
      # 调大：下游拿到新地图更快；调小：带宽和 CPU 开销更低。
      publish_frequency: 1.0
      # 全局参考坐标系；决定规划、行为或代价地图在 TF 树中的根坐标。
      # 改成错误坐标系：地图、路径和机器人位姿会整体错位。
      global_frame: map
      # 机器人基坐标系；决定导航模块认为“机器人本体”对应哪个 TF。
      # 改成错误坐标系：碰撞检查、控制和行为判断都会失真。
      robot_base_frame: base_link
      # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
      # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
      use_sim_time: false
      # 全局规划与局部碰撞检查使用同一真实车体外廓。
      footprint: "[[0.35, 0.275], [0.35, -0.275], [-0.35, -0.275], [-0.35, 0.275]]"
      # 地图分辨率；决定每个栅格代表的物理尺寸。
      # 调大：地图更粗、更省内存；调小：地图更细、更精确但更耗资源。
      resolution: 0.02
      # 是否追踪未知区域；决定全局规划是否把未知空间单独表达出来。
      # 改为 true：未知区域更明确；改为 false：未知区域更容易被当作可通行。
      track_unknown_space: true
      # 滚动窗口开关；决定代价地图是固定在世界坐标还是跟着机器人移动。
      # 改为 true：地图跟车移动；改为 false：地图固定在全局坐标。
      rolling_window: false
      # 地图宽度；决定 costmap 在 X 方向覆盖的物理范围。
      # 调大：覆盖更广但内存和更新开销更高；调小：更省资源但看不到更远区域。
      width: 50
      # 地图高度；决定 costmap 在 Y 方向覆盖的物理范围。
      # 调大：覆盖更广但内存和更新开销更高；调小：更省资源但看不到更远区域。
      height: 50
      # 代价地图层列表；决定当前 costmap 会叠加哪些地图层。
      # 增删地图层：会直接改变障碍信息来源和代价图行为。
      plugins: ["static_layer", "inflation_layer"]
      # 静态层参数块；负责把先验 2D 栅格地图叠加到全局代价地图。
      # Travel 先验点云模式中，PCD 负责重定位，2D map 负责全局规划。
      static_layer:
        # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
        plugin: "nav2_costmap_2d::StaticLayer"
        # 是否使用 transient local 订阅地图；决定晚加入节点能否拿到已发布地图。
        map_subscribe_transient_local: true
        # 仅清除机器人当前真实 footprint 下的静态地图残留，避免起点落入旧黑点后 NavFn 自锁。
        # local costmap、MPPI footprint critic 和 Collision Monitor 仍保留实时障碍安全边界。
        footprint_clearing_enabled: true
      # 膨胀层参数块；负责给障碍物外围增加安全缓冲。
      # 改错结构或参数：障碍安全边界会偏大或偏小。
      inflation_layer:
        # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
        # 改成不存在或不兼容的插件：对应功能块会加载失败。
        plugin: "nav2_costmap_2d::InflationLayer"
        # Travel 全局规划只基于静态 2D 地图，使用小于 local costmap 的虚拟规划半径和膨胀半径，避免窄通道被静态膨胀完全切断。
        # 代价场参数；决定障碍物周围代价衰减和安全边界形状。
        # 调大：代价场变化更明显或更保守；调小：更激进或更平缓。
        cost_scaling_factor: 2.5
        # 代价场参数；决定障碍物周围代价衰减和安全边界形状。
        # 调大：代价场变化更明显或更保守；调小：更激进或更平缓。
        inflation_radius: 0.30
      # 是否总是发送完整代价地图；决定 costmap 发布全量还是增量更新。
      # 改为 true：下游更稳但带宽更高；改为 false：更省带宽但对订阅端要求更高。
      always_send_full_costmap: false
# Map Server 节点参数块；负责加载静态地图。
# 改错节点名或层级：地图服务器会读不到参数或地图加载失败。
map_server:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 地图文件路径；决定运行时加载哪张静态地图。
    # 改成错误路径：地图加载失败；改成其它地图：整个导航环境都会变化。
    yaml_filename: ""
# Map Saver 节点参数块；负责导出地图。
# 改错节点名或层级：地图保存服务会读不到参数。
map_saver:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 地图保存超时；决定导出地图时等待磁盘写入多久。
    # 调大：更能容忍慢写盘；调小：失败暴露更快。
    save_map_timeout: 5.0
    # 默认空闲阈值；决定导出地图时多大概率以下会写成空闲格。
    # 调大：更容易把区域判为空闲；调小：更保守。
    free_thresh_default: 0.25
    # 默认占用阈值；决定导出地图时多大概率以上会写成障碍格。
    # 调大：更难判成障碍；调小：更容易判成障碍。
    occupied_thresh_default: 0.65
    # 是否使用 transient local 订阅地图；决定晚加入节点能否拿到已发布地图。
    # 改为 true：更能接收到历史地图；改为 false：更依赖地图重新发布。
    map_subscribe_transient_local: true
# Planner Server 节点参数块；负责生成全局路径。
# 改错节点名或层级：全局规划器会回退默认值或启动失败。
planner_server:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # TF 容忍时间；决定该模块接受多大的 TF 时间偏差。
    # 调大：对 TF 延迟更宽松；调小：更严格但更容易因延迟失败。
    transform_tolerance: 0.5
    # 期望规划频率；用于监控全局规划线程是否跟上预期节奏。
    # 调大：更强调高频规划；调小：监控更宽松。
    expected_planner_frequency: 20.0
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 全局规划器列表；决定 planner server 可调用哪些规划器。
    # 增删规划器：会改变路径生成能力或导致引用失败。
    planner_plugins: ["GridBased"]
    # 全局规划器参数块；负责配置 NavFn 的搜索行为。
    # 改错结构或参数：全局路径质量和可达性会变化。
    GridBased:
      # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
      # 改成不存在或不兼容的插件：对应功能块会加载失败。
      plugin: "nav2_navfn_planner/NavfnPlanner"
      # 容差参数；决定当前算法接受多大误差仍视为有效。
      # 调大：更宽松；调小：更严格。
      tolerance: 0.5
      # 是否使用 A* 搜索；决定 NavFn 走 A* 还是更传统的搜索策略。
      # 改为 true：通常更偏向启发式搜索；改为 false：更接近传统全图搜索。
      use_astar: true
      # 是否允许穿越未知区域；决定规划器能否把未知空间当作可通行。
      # 改为 true：更容易找到穿越未知区的路；改为 false：路径更保守。
      allow_unknown: true
# Smoother Server 节点参数块；负责对全局路径做平滑处理。
# 改错节点名或层级：路径平滑器会读不到参数。
smoother_server:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 平滑器列表；决定 smoother server 提供哪些路径平滑器。
    # 增删平滑器：会改变路径后处理能力或导致引用失败。
    costmap_topic: global_costmap/costmap_raw
    footprint_topic: global_costmap/published_footprint
    robot_base_frame: base_link
    transform_tolerance: 0.1
    smoother_plugins: ["savitzky_golay_smoother"]
    # Savitzky-Golay 参数块；用于去掉 NavFn 路径中的细小锯齿和微小折线。
    # 改错结构或参数：平滑器可能加载失败或平滑效果不稳定。
    savitzky_golay_smoother:
      # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
      # 改成不存在或不兼容的插件：对应功能块会加载失败。
      plugin: "nav2_smoother::SavitzkyGolaySmoother"
      # 平滑窗口大小；必须为奇数，窗口更大时更能抑制路径细碎噪声。
      # 调大：更平滑；调小：更贴近原始路径。
      window_size: 7
      # 拟合多项式阶数；决定每个窗口内如何逼近原始路径。
      # 调大：更灵活但更容易保留细碎变化；调小：更保守更稳。
      poly_order: 3
      # 是否递归细化平滑结果；可进一步消除微小折线。
      # 改为 true：更平滑；改为 false：更接近原始路径。
      do_refinement: true
      # 递归细化次数；决定额外平滑迭代轮数。
      # 调大：更平滑但更可能抹掉细节；调小：更保守。
      refinement_num: 2
# Behavior Server 节点参数块；负责恢复行为和辅助动作。
# 改错节点名或层级：恢复行为会读不到参数或不可用。
behavior_server:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 输入或输出话题名；决定该模块从哪里取数据或向哪里发数据。
    # 改错话题：上下游节点会直接断链。
    costmap_topic: local_costmap/costmap_raw
    # 输入或输出话题名；决定该模块从哪里取数据或向哪里发数据。
    # 改错话题：上下游节点会直接断链。
    footprint_topic: local_costmap/published_footprint
    # 恢复行为循环频率；决定行为服务每秒更新多少次。
    # 调大：响应更快但 CPU 更高；调小：更省资源但动作更新更慢。
    cycle_frequency: 10.0
    # 恢复行为列表；决定 behavior server 可提供哪些动作。
    # 增删行为：会直接改变恢复和辅助动作集合。
    behavior_plugins: ["spin", "backup", "wait"]
    spin:
      plugin: "nav2_behaviors/Spin"
    # 仅由 Travel 恢复树调用的短距离低速后退；行为服务器会用 local costmap 做全车体碰撞预判。
    backup:
      plugin: "nav2_behaviors/BackUp"
    # Wait 恢复行为参数块；负责等待动作。
    # 改错结构或参数：等待行为可能不可用。
    wait:
      # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
      # 改成不存在或不兼容的插件：对应功能块会加载失败。
      plugin: "nav2_behaviors/Wait"
    # 全局参考坐标系；决定规划、行为或代价地图在 TF 树中的根坐标。
    # 改成错误坐标系：地图、路径和机器人位姿会整体错位。
    global_frame: odom
    # 机器人基坐标系；决定导航模块认为“机器人本体”对应哪个 TF。
    # 改成错误坐标系：碰撞检查、控制和行为判断都会失真。
    robot_base_frame: base_link
    # TF 容忍时间；决定该模块接受多大的 TF 时间偏差。
    # 调大：对 TF 延迟更宽松；调小：更严格但更容易因延迟失败。
    transform_tolerance: 0.1
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 前向模拟时长；决定恢复行为碰撞预判向前看多远。
    # 调大：更保守、更看远；调小：更灵敏但前瞻更短。
    simulate_ahead_time: 2.0
    # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
    # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
    max_rotational_vel: 0.50
    # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
    # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
    min_rotational_vel: 0.20
    # 恢复行为角加速度上限；限制转向提速的快慢。
    # 调大：转向响应更快但更猛；调小：更柔和但更慢。
    rotational_acc_lim: 0.80
# Robot State Publisher 参数块；负责基础状态发布的 ROS 参数。
# 改错节点名或层级：节点会读不到参数。
robot_state_publisher:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
# Waypoint Follower 节点参数块；负责航点任务执行。
# 改错节点名或层级：航点任务器会读不到参数。
waypoint_follower:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 航点任务循环频率；决定 waypoint follower 多快检查一次任务状态。
    # 调大：响应更快但线程负载更高；调小：更省资源但响应更慢。
    loop_rate: 20
    # 是否遇错即停；决定某个航点失败后是否中止整条任务。
    # 改为 true：更保守、失败即停；改为 false：失败后会继续后续航点。
    stop_on_failure: false
    # 航点任务执行器名称；决定到达航点后调用哪个附加动作模块。
    # 改错名称：航点执行器会找不到对应任务模块。
    waypoint_task_executor_plugin: "wait_at_waypoint"
    # 航点等待任务参数块；负责到点后的停留动作。
    # 改错结构或参数：到点等待行为会异常。
    wait_at_waypoint:
      # 插件类名；决定当前参数块最终加载哪种 Nav2 实现。
      # 改成不存在或不兼容的插件：对应功能块会加载失败。
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      # 功能开关；决定当前参数块对应的能力是否真正参与运行。
      # 改为 true：该能力生效；改为 false：该能力被关闭。
      enabled: true
      # 航点停留时长；决定机器人在每个航点等待多久。
      # 调大：停留更久、更稳；调小：通过更快。
      waypoint_pause_duration: 200
# Velocity Smoother 节点参数块；负责平滑速度指令。
# 改错节点名或层级：速度平滑器会读不到参数。
velocity_smoother:
  # ROS 参数入口；下方配置会直接注入对应节点。
  # 改错这一层级：节点可能完全读不到这些参数。
  ros__parameters:
    # 是否使用仿真时间；决定该节点读取 /clock 还是真实系统时间。
    # 改为 true：依赖外部仿真时钟；改为 false：直接使用系统时间。
    use_sim_time: false
    # 速度平滑频率；决定速度平滑器每秒输出多少次平滑指令。
    # 调大：响应更快但更敏感；调小：更平缓但响应更慢。
    smoothing_frequency: 20.0
    # 是否按比例缩放速度向量；决定三轴速度是否整体联动压缩。
    # 改为 true：更强调整体比例一致；改为 false：各轴按各自限幅处理。
    scale_velocities: false
    # 工作模式参数；决定模块按哪种反馈方式运行。
    # 改成更依赖反馈的模式：对传感器更敏感；改成更简单的模式：链路更直接但闭环更弱。
    feedback: "OPEN_LOOP"
    # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
    # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
    max_velocity: [0.30, 0.0, 0.65]
    # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
    # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
    min_velocity: [0.0, 0.0, -0.65]
    # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
    # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
    max_accel: [0.40, 0.0, 1.4]
    # 上下界参数；用于限制速度、高度、代价或其它量的允许范围。
    # 调大上界或调小下界：系统更激进；反向调整：系统更保守。
    max_decel: [-0.8, 0.0, -1.8]
    # 输入或输出话题名；决定该模块从哪里取数据或向哪里发数据。
    # 改错话题：上下游节点会直接断链。
    odom_topic: "odom_CBoar"
    # 里程计有效时长；超过该时间的反馈会被视为过期。
    # 调大：更能容忍慢反馈；调小：更严格但更容易判为过期。
    odom_duration: 0.1
    # 向量限制参数；分别约束 x、y、theta 三个方向的速度或加速度。
    # 调大：响应更快或限制更宽；调小：更稳更保守。
    deadband_velocity: [0.0, 0.0, 0.0]
    # 速度指令超时；超过该时间未收到新指令就会停下。
    # 调大：更宽容但风险更高；调小：更安全但更容易因抖动停住。
    velocity_timeout: 1.0
