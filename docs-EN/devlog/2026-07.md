# July 2026 Development Log

---

## 2026.07.22

### [Build Fixes & Log Cleanup]

#### File
- `Makefile`
- `src/frc/frc_bev/setup.py`
- `src/frc/frc_nodes/setup.py`
- `src/navigation/gps_waypoint_dispatcher/setup.py`
- `src/navigation/indoor_navigation_manager/setup.py`
- `src/navigation/waypoint_collector/setup.py`
- `src/navigation/waypoint_nav_tool/setup.py`
- `src/perception/hba/CMakeLists.txt`
- `src/perception/localizer/CMakeLists.txt`
- `src/perception/pgo/CMakeLists.txt`
- `src/perception/pgo_gps_fusion/CMakeLists.txt`
- `src/planning/global2local_tf/setup.py`
- `src/planning/global_path_planning/setup.py`
- `src/planning/gnss_global_path_planner/setup.py`
- `src/sensor_drivers/gnss/gnss_calibration/setup.py`
- `src/sensor_drivers/gnss/nmea_navsat_driver/setup.py`
- `src/sensor_drivers/wit_imu_traj/setup.py`
- `src/sensor_drivers/wit_ros2_imu/setup.py`
- `src/survey/survey_mode/setup.py`

#### Change
- **Makefile**: Appended logic to the `setup` target to automatically create a `COLCON_IGNORE` file in `src/third_party/navigation2/nav2_system_tests/` after `vcs import`
- Added an environment variable `PYTHONWARNINGS` into the `COLCON_BUILD` command to suppress setuptools warnings.
- **CMakeLists.txt (perception packages)**: Injected `cmake_policy(SET CMP0074 NEW)` immediately after the `project()` declarations.
- **setup.py**: Removed the deprecated `tests_require=['pytest']` parameter across all local ROS 2 Python packages.

#### Reason
- The `nav2_system_tests` package (pulled via `vcs import`) relies on Gazebo dependencies (`gazebo_ros_pkgs`), which are unused in the stack and cause a fatal build failure on the Jetson.
- CMake raised `CMP0074` policy warnings due to how it interprets the `PCL_ROOT` variable during `find_package(PCL)`.
- Newer versions of `setuptools` deprecate the `tests_require` keyword, which was flooding the build logs with `UserWarning: Unknown distribution option: 'tests_require'` soft warnings for every Python package.

#### Effect
- Safely skips compiling the Gazebo-dependent navigation tests, ensuring a successful build.
- Eliminates CMake PCL policy warnings during compilation.
- Significantly declutters the `colcon build` output by natively dropping deprecated test arguments and globally silencing residual third-party warnings.


## 2026.07.13

### Travel Global-start Release, Continuous TF Correction, And Bounded Backup

#### File

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_to_pose_recovery.xml`
- `src/bringup/behavior_trees/travel_nav_through_poses_recovery.xml`
- `src/bringup/scripts/prior_map_tf_authority.py`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/bringup/test/test_system_travel_launch.py`
- Matching commands, indoor-design, Nav2-tuning, and devlog files under `docs-CN/` and `docs-EN/`

#### Change

- The authority AMCL callback now accepts only a stable target; the 20Hz TF timer performs the actual `map -> odom` approach under `0.015m/s` translation, `0.006rad/s` yaw, and `0.02m/s` equivalent-base correction-rate limits, with the existing per-step caps retained as hard secondary limits.
- The authority subscribes to `/initialpose` to distinguish manual relocalization from background localizer recovery. A manual coarse pose keeps TF visible but reports `tf_active=false`; after a trusted TF exists, recovery from `ambiguous_global_candidates` cannot directly reseed it.
- Fix a vehicle regression when the localizer was already `LOCALIZED` before a coarse pose arrived. The authority no longer depends on a state edge: while a new manual pose is pending, `relocalization_accepted` immediately applies the latest localizer transform and restores `tf_active=true`. Background-only recovery still cannot reseed.
- The command gate still requires fresh localizer status with `sensors_ready=true`, but transient 3D candidate `LOST` no longer stops a vehicle whose authority TF already exists. Authority, cloud, or sensor failure still emits zero immediately.
- The global static layer enables `footprint_clearing_enabled`, clearing stale static occupancy only beneath the robot's current measured footprint so NavFn cannot self-lock its start in the inscribed zone.
- Both Travel recovery trees add one collision-checked `0.20m` backup at `0.08m/s`, followed by local-costmap clearing and immediate replanning. Normal MPPI retains `vx_min=0`.

#### Reason

- The latest `18-12-46`, `18-08-33`, and `18-05-01` bags missed only `3/4/3` controller periods at 15Hz, so compute was not the main stop cause. The first two runs produced `16/17` consecutive NavFn failures near obstacles, while the third produced three `Failed to make progress` events.
- At both first NavFn failures the goal cell was free, but the robot center cost was `99` and its measured footprint covered `76/114` lethal cells in the recorded static global grid. A physical route existed; the planner's start had been classified inside stale map occupancy/inflation.
- Actual `map -> odom` changed `13/16/18` times with about `0.211/0.232/0.226m` cumulative translation. Centimetre steps visibly jitter Foxglove and can move the global planning start into inflation while the physical vehicle remains clear.
- Two bags contained `34/111` `ambiguous_global_candidates` status samples, all with `sensors_ready=true`. The old stop was therefore a candidate-layer gating error, not a LiDAR or LIO failure.
- In the `19-03-43` and `19-05-40` regression bags, `/cmd_vel_nav` contained `899/252` nonzero commands while every one of the `3025/1330` `/cmd_vel_localized` samples was zero. The authority remained permanently in `MANUAL_RELOCALIZING`, proving that the stopped vehicle came from a missing already-localized completion path.

#### Effect

- Stable AMCL correction still removes long-term drift without moving the map in centimetre steps. A new coarse pose remains fail-closed, while background candidate ambiguity cannot overwrite a trusted TF.
- Manual relocalization remains fail-closed until ICP is actually accepted, but reliably leaves pending after success instead of permanently suppressing valid Nav2 commands.
- NavFn can expand from stale map specks/inflation beneath the current body. Static obstacles outside the footprint, live local-costmap points, MPPI footprint checks, Collision Monitor, and the physical e-stop remain active.
- When turning clearance is insufficient, recovery may back up slowly once and replan. If the rear path is unsafe, the behavior server's footprint collision prediction rejects the backup.

### Fix Travel In-place Turn Deadlock, Map Jitter, And MPPI Deadline Misses

#### File

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/config/master_params.yaml`
- `src/bringup/scripts/prior_map_tf_authority.py`
- `src/bringup/scripts/prior_map_tf_math.py`
- `src/bringup/test/test_prior_map_tf_math.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md` and `docs-EN/commands.md`
- `docs-CN/knowledge/indoor_mapping_navigation_design.md` and `docs-EN/knowledge/indoor_mapping_navigation_design.md`
- `docs-CN/knowledge/nav2_tuning.md` and `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Set Rotation Shim to `closed_loop=false` while retaining the `0.24rad/s` target and `0.65rad/s2` acceleration limit, allowing pre-rotation commands to ramp from the previous cycle's output.
- Change Travel MPPI from `20Hz/model_dt=0.05s, time_steps=32, batch_size=160` to matched `15Hz/model_dt=0.0666667s, time_steps=24, batch_size=128`. The horizon remains approximately 1.6 seconds while candidate points per cycle fall from 5120 to 3072.
- Add a five-sample AMCL candidate window, three-sample consistency minimum, SE(2) median, spread gates, and one-second correction interval to the TF authority. Translation/yaw deadbands are evaluated independently and become `0.05m/0.035rad`.
- Extend authority status with candidate-window size, translation/yaw spread, valid-candidate age, and actual-correction age, distinguishing accumulating, unstable, rate-limited, and stable-hold states.
- Correct `pointcloud_to_laserscan.scan_time` from `0.0333333s` to `0.1s`, matching the approximately 10Hz scan rate measured in the bags. Height filtering, rectangular vehicle self-filtering, angular resolution, and range limits are unchanged.

#### Reason

- In the `16-41-02` and `16-43-22` bags, `96~100%` of commands after the first `Failed to make progress` were fixed at `linear.x=0, angular.z=+-0.0325rad/s`. This equals the old closed-loop Rotation Shim one-cycle acceleration step. The chassis could not cross static friction, feedback stayed zero, and each cycle restarted from the same small command.
- Raw AMCL `map->odom` candidates accumulated about `0.90m/0.73m` and `26.4deg/26.9deg` of back-and-forth translation/yaw in the two bags, far above their net changes. The old authority corrected every passing sample and made the map visibly jitter.
- Controller logs still reported frequent 20Hz deadline misses. The Orin NX had total CPU headroom, but Humble MPPI critics score serially inside one controller callback, so per-cycle work and target rate must fall instead of widening velocity watchdogs.
- Median `/scan` period was approximately `0.1s` in both bags, while the old field incorrectly declared 30Hz.

#### Effect

- A large-angle pre-rotation ramps to `0.24rad/s` in about 0.4 seconds instead of remaining permanently at the first unexecutable acceleration step. Ordinary MPPI angular commands are still not globally amplified.
- Offline replay reduces actual authority TF changes from `31/35` to `7/6` and cumulative yaw from about `5.1deg` to `0.31/1.20deg`, while retaining about `5~6cm` of stable translation correction.
- MPPI horizon, motion limits, footprint, Collision Monitor, localization/cloud gates, and physical e-stop remain unchanged. Candidate-point work falls 40% to reduce deadline misses without relaxing safety boundaries.

### Travel Control De-oscillation, Replan-first Recovery, And AMCL Prior-map Correction

#### File

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_*_recovery.xml`
- `src/bringup/scripts/post_collision_cmd_conditioner.py`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/bringup/scripts/prior_map_tf_authority.py` and `prior_map_tf_math.py`
- `src/bringup/scripts/initialpose_relocalize_bridge.py`
- `src/bringup/launch/system_travel.launch.py`, `CMakeLists.txt`, and `package.xml`
- `src/perception/localizer/src/localizer_node.cpp`
- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/manager_node.py` and `foxglove_adapter_node.py`
- `scripts/launch_with_logs.sh` and `Makefile`
- `src/bringup/test/` and `src/perception/localizer/test/`
- Matching command, knowledge, and devlog files under `docs-CN/` and `docs-EN/`

#### Change

- Removed unconditional post-processing that raised every small angular command to `0.20rad/s`. A mixed Slow command now loses translation only when linear speed is below breakaway and angular speed is at least `0.16rad/s`; angular velocity is preserved exactly.
- Changed Rotation Shim takeover/release to `0.65/0.35rad` and disabled final-heading takeover. Reduced VelocityDeadbandCritic to weight 15 and `[0.12,0,0.08]`, with PathAlign/PathFollow weights `6/12`.
- Kept MPPI at `20Hz/model_dt=0.05s`, reduced `time_steps` from 40 to 32, and removed redundant PreferForwardCritic plus Humble's unsupported CostCritic `trajectory_point_step`. Local/global costmap publication falls to `2/1Hz`, while the local costmap still updates internally at 10Hz.
- Changed recovery to 1Hz replan-first behavior: clear the local costmap and immediately replan/smooth, permit only a `0.52rad` Spin after a subsequent `IsStuck`, and leave wait/both-map clearing last. BackUp remains disabled.
- Added a latched localizer candidate transform and `publish_tf` switch. In Travel, neither localizer nor AMCL broadcasts TF; `prior_map_tf_authority` alone owns `map->odom`, seeds AMCL from localizer, and gates AMCL candidates by covariance, synchronization, target jump, and equivalent base jump.
- Added `/travel/control_gate/status` and diagnostics for localization, point-cloud, collision, command-timeout, and pass states. The command timeout window changes from 0.25s to 0.40s.
- Travel Python nodes handle SIGINT/already-closed contexts at the spin boundary and check `rclpy.ok()` before shutdown, avoiding false process-crash reports on normal stop while still re-raising real exceptions when the context remains valid.
- A Foxglove coarse pose carrying a non-`map` frame such as `base_link` is no longer rejected. The bridge normalizes it to `map` under `/initialpose` global-pose semantics, forwards it to both localizer and AMCL, and records `/initialpose` in the Travel bag.

#### Reason

- In the `22-03-41` bag, post-processing changed 55.9% of commands and made 4433 pure rotations, amplifying side-to-side hesitation. The same session missed 58 20Hz periods during about 110.5s of active control; total CPU remained available, but serial critic evaluation missed a single-core deadline.
- Corridor scan-to-map residual grew from about `0.03~0.13m` to about `1.05m`, while a frozen localizer seed provided no runtime global correction. Letting AMCL or localizer directly compete for TF would instead create duplicate ownership and jump risk.
- The previous recovery order waited or spun 90 degrees before prioritizing stale local-obstacle clearing and replanning, leaving recoverable real obstacles blocked for too long.
- The current vehicle session showed five consecutive coarse poses rejected by the new strict `frame_id=map` check. Before initial localization there is no `map -> base_link`, so the Foxglove frame label cannot be corrected through TF.

#### Effect

- MPPI retains ordinary small-angle steering without the post-processor turning it into repeated in-place rotation. The controller can take more natural local detours while footprint, Collision Monitor, and physical e-stop safety remain.
- Candidate-point work per MPPI cycle falls 20%, with lower costmap serialization load, while controller frequency still satisfies Humble's model-step constraint.
- Travel now has one auditable prior-map TF authority. A bad AMCL match changes status without jumping TF; unhealthy localization or point clouds still fail closed at the velocity gate.

## 2026.07.12

### Travel Closed-loop Steering And In-place Obstacle Escape

#### File

- `src/bringup/config/nav2_travel.yaml` and `collision_monitor_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_*_recovery.xml`
- `src/bringup/scripts/post_collision_cmd_conditioner.py`
- `src/bringup/launch/system_travel.launch.py` and `CMakeLists.txt`
- `scripts/launch_with_logs.sh` and `Makefile`
- Matching tests and bilingual documentation

#### Change

- Reduce global replanning from 1Hz to 0.5Hz while keeping MPPI at 20Hz with fixed sampling noise and lower angular noise; Rotation Shim uses closed-loop odometry and hysteresis. After the `21-45-44` bag recorded 146 control deadline misses, reduce the sample batch from 200 to 160 while retaining the two-second horizon.
- Route Collision Monitor output through `/cmd_vel_safe_raw`. A final conditioner converts a deadband-limited mixed Slow turn into in-place rotation and raises every nonzero pure-rotation command unconditionally to at least `0.20rad/s`. `PoseProgressChecker` counts `0.15rad` of rotation as progress.
- Set global inflation to `0.30m`, still above the current footprint's `0.285m` inscribed radius and one map cell smaller than `0.35m`.

#### Reason

- The second `21-06-57` run stalled under a real obstacle slowdown. The `21-45-44` bag then proved Slow was almost inactive in that run; its stalls came from continuous ineffective `0.04rad/s` pure-rotation commands and a progress checker that recognized translation only.

#### Effect

- Reduces weaving and deadline misses from path refresh, sampling, and control computation. In-place rotation clears the chassis deadband and counts as progress. Global paths regain one `5cm` cell while the measured footprint, local costmap, Collision Monitor Stop, zero commands, and watchdog semantics remain unchanged.

### Travel Path Smoothing, Terminal Rotation, And TF Scheduling Fix

#### File

- `src/bringup/behavior_trees/travel_nav_*_recovery.xml`
- `src/bringup/config/nav2_travel.yaml` and `master_params.yaml`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/perception/fastlio2/src/lio_node.cpp`
- Matching tests and bilingual knowledge documentation

#### Change

- Apply Savitzky-Golay smoothing after every NavFn replan. The `20-58-24` vehicle bag proved the global-footprint recheck rejected every smoothed path, so the duplicate smoother check is disabled while the local costmap, MPPI footprint critic, and Collision Monitor retain three motion-safety layers. Local-costmap updates are reduced from 15Hz to 10Hz.
- Raise nonzero pure-rotation commands below the chassis effective range to `0.20rad/s`; add a configurable `0.05s` FAST-LIO2 TF future tolerance.

#### Reason

- The `20-37-08` bag showed `15.23rad` accumulated turning in the first 4.23m plan and 46–52 angular-command reversals. Inside the goal radius, the controller still emitted an unexecutable `0.04rad/s`, alongside controller TF future extrapolation.

#### Effect

- MPPI receives smoother geometry, terminal heading alignment clears static friction, and the 10Hz costmap plus TF tolerance reduce controller and millisecond TF scheduling pressure. Zero commands, collision stops, disabled BackUp, and command watchdogs remain unchanged.

### Automatic Per-session Travel Navigation Rosbag

#### File

- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`

#### Change

- Travel now records goals, status, TF, odometry, paths, costmaps, scan, and every velocity-command stage to `data/travel_bag/` in each log session by default.
- Added the raw-point-cloud `FYP_TRAVEL_BAG_PROFILE=debug` profile and the explicit `FYP_TRAVEL_RECORD_BAG=false` opt-out.

#### Reason

- Vehicle tests show weaving, short stops, and failed recovery; console logs alone cannot align planned paths, actual motion, obstacle input, and final serial velocity commands.

#### Effect

- Every Travel launch retains replayable evidence. The default lean profile avoids raw Livox CPU and storage load; debug is reserved for point-cloud investigations.

### [Rectangular Mapping Self-filter And Isolated-grid Cleanup]

#### File

- `src/perception/pointcloud_to_laserscan/`
- `src/bringup/config/pointcloud_to_laserscan_mapping.yaml`
- `src/bringup/launch/system_slam.launch.py`
- `src/bringup/test/test_system_slam_launch.py`
- `scripts/clean_occupancy_map.py`
- `scripts/test_clean_occupancy_map.py`
- Matching architecture, command, and indoor-design documentation under `docs-CN/` and `docs-EN/`

#### Change

- Added an optional rectangular exclusion box to pointcloud-to-LaserScan. Enabling it requires an explicit `target_frame`, and invalid bounds fail at startup.
- Added a SLAM-only profile that excludes `x=[-0.35,0.35]m, y=[-0.275,0.275]m` in `base_footprint`, matching the measured `650x500mm` body plus `25mm` per side.
- Added an 8-connected PGM cleanup tool. By default it replaces only isolated occupied components of one to three cells and restores free or unknown state from the boundary majority.

#### Reason

- The vehicle's `floor_4` map contained isolated black cells distributed along the driven trajectory, consistent with near-field body returns entering Slam Toolbox. The old projection had neither a body-envelope filter nor a conservative offline cleanup path.

#### Effect

- Newly built 2D maps no longer accumulate returns from inside the rectangular body envelope along the trajectory. The profile is SLAM-only and does not change Travel/Corridor live obstacle sensing.
- Saved maps can be written to a new PGM for inspection before replacement. Columns, furniture, and walls larger than three cells remain unchanged, and unknown space is not indiscriminately expanded into free space.

### [Travel Oscillation-stop And Transient-localization Fix]

#### File

- `src/perception/fastlio2/`, `src/perception/localizer/`
- `src/bringup/config/{master_params.yaml,nav2_travel.yaml,collision_monitor_travel.yaml}`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/navigation/indoor_navigation_manager/`
- Matching tests and bilingual documentation

#### Change

- Added a `0.80x0.60m` rectangular self-filter to the Nav2 obstacle cloud while keeping the `0.80x0.64m` stop polygon. Static wide-corridor sampling measured `0-6` sparse stop-zone returns per frame with a `2.39` average, so Stop/Slow `max_points` changes from 3 to 10.
- Added a narrow 3D self-reflection patch at `x=[0.40,0.82]m, y=[-0.35,-0.14]m, z=[0.25,0.42]m` for the front-right horizontal arc that remained body-fixed before and after vehicle motion.
- Added `0.10s` future tolerance to localizer `map->odom`, raised the consecutive-failure threshold to eight, and retained trusted DEGRADED localization for at most `5.0s`.
- During an active goal, transient localization loss immediately holds zero velocity but waits up to `5.0s` for recovery before canceling.
- A 100-message chassis sample contained isolated invalid `ctrl_mode=4/252` and `ps2_key=255/252` frames; the adapter now debounces mode for `1.0s` and cancels only if HOST remains absent.
- Reduced Travel MPPI from 500 through `300x48` to `200x40`, disabled critic-stat publication, and raised transient controller failure tolerance from `0.6s` to `2.0s`.
- Replaced Travel fail-stop trees with bounded recovery: planner/controller failures clear global/local costmaps, followed by up to five outer Wait, collision-checked Spin, and clearing retries. BackUp and path smoothing remain disabled.
- Continuous ICP withdrew valid `map->odom` after about 17 seconds of vehicle motion and caused TF extrapolation. Runtime continuous ICP is now disabled: startup pose still validates against the PCD map, then FAST-LIO2 propagates motion.

#### Reason

- The vehicle continuously missed its 20Hz control period and failed `map->odom` lookups by 10–35ms of future extrapolation, while front-right body-edge returns repeatedly toggled Collision Monitor Stop. One runtime ICP failure also canceled the goal immediately; together these appeared as left-right twitching followed by a stop.

#### Effect

- Sparse body returns no longer enter local avoidance, corrupt serial status no longer immediately cancels goals, and planner/controller failures can clear, spin, and continue planning. Accepted startup localization is no longer withdrawn by failing runtime ICP. Dense real-obstacle stopping, Spin footprint collision checking, input freshness, and host/firmware watchdogs remain active.

## 2026.07.11

### [Foxglove Default Goal-topic Compatibility]

#### File

- `foxglove_adapter_node.py`, `system_travel.launch.py`
- Matching tests and bilingual documentation

#### Change

- Added `/move_base_simple/goal` as a compatibility input to the navigation adapter and Bridge client-publish whitelist; the repository layout's `/foxglove/goal_pose` remains unchanged.
- Added Pose-receipt and Action rejection/failure logs so session logs show where a goal stops.

#### Reason

- The vehicle's default Foxglove 3D panel advertised `/move_base_simple/goal`, while the adapter only listened to `/foxglove/goal_pose`. The Pose therefore had publishers but no subscriber and never entered `/navigate_to_pose`.

#### Effect

- Both Foxglove layouts can send goals through identical structured-localization, concurrency, and Action gates. No velocity-topic permission was added.

### [Travel Localization Velocity-gate Installation Fix]

#### File

- `src/bringup/scripts/localization_cmd_gate.py`
- `src/bringup/test/test_system_travel_launch.py`

#### Change

- Corrected the localization velocity-gate script mode from `0644` to `0755` and added a contract requiring the ROS libexec helper to be executable.

#### Reason

- CMake created the correct install symlink, but ROS 2 reports a non-executable script as executable not found in libexec, aborting Travel before any runtime node starts.

#### Effect

- `launch_ros.actions.Node(executable="localization_cmd_gate.py")` now resolves and starts correctly; safety-gate behavior and parameters are unchanged.

### [Real-map 2D-to-3D Wall Alignment Improvement]

#### File

- `scripts/mapping_session_utils.py`, `scripts/save_mapping_session.py`
- Matching tests, indoor command documentation, and design documentation

#### Change

- Replaced direct XY projection of a complete PCD height band with strong wall cells selected by vertical span: default `0.06m` cells, at least three points, and `>=0.50m` span.
- Added an explicit identity seed, raised the quality sample cap to 12,000 points, and used exact local nearest neighbors through 0.50m spatial buckets.
- Alignment reports now record extraction parameters/counts, and quality evaluation and overlay rendering use the same wall sample.
- Added `--recalibrate-only` to repair an existing bundle without ROS save calls or replacing map/capture evidence, while rebuilding descriptor region labels with the new transform.

#### Reason

- The first `floor_4` vehicle bundle passed artifact, stationary, and frame gates, but whole-band PCD projection included floors, furniture, and dynamic clutter. It falsely failed at RMSE `0.159m`, p95 `0.279m`, and overlap `0.531` despite nearly shared map coordinates.
- Both maps start from a common origin in one session. Principal-axis/centroid-only seeds could move repeated long-corridor data into a wrong local optimum about 1.4m away.

#### Effect

- Without relaxing the `0.10m/0.15m/0.55` gates, the `floor_4` offline regression reaches RMSE `0.065m`, p95 `0.126m`, overlap `0.957`, and a near-identity transform.
- Floors and single-height clutter no longer dominate calibration. Real nonlinear deformation still fails the original quality gates and cannot bypass acceptance through this change.

### [Pre-vehicle Indoor Navigation Safety Review Fixes]

#### File

- `src/sensor_drivers/serial_twistctl/`, `src/firmware/rm_c_board/Core/Src/main.c`
- `src/perception/localizer/`, `src/navigation/indoor_navigation_manager/`
- `src/bringup/scripts/localization_cmd_gate.py`, `src/bringup/config/master_params.yaml`
- Matching tests and bilingual documentation

#### Change

- Added host 300 ms and STM32 500 ms velocity-command watchdogs; firmware UART input is now explicitly null-terminated.
- Added 0.5s obstacle-cloud and 0.25s velocity-command freshness to the localization gate; any failure continuously publishes zero.
- Fixed named-navigation cancellation loss before the underlying goal handle arrives and guaranteed reservation cleanup on exceptions.
- Corrected Scan Context yaw-shift sign; automatic global localization waits for 200 points and retries up to five times at 3s intervals.
- Projected localizer `map -> odom` strictly to planar XY+yaw instead of propagating ICP roll/pitch/z.

#### Reason

- Neither the host serial chain nor the vendored firmware stopped on command loss, so a safety-node or Jetson failure could retain the last nonzero chassis command.
- Humble Collision Monitor ignores an expired source instead of stopping solely because of source timeout.
- The cancellation race, inverted Scan Context rotation, and unconstrained ICP rotation could cause runaway goals, failed global localization, or tilted TF.

#### Effect

- Loss of localization, obstacle stream, Nav2 command, host serial input, or the host process clears the vendored firmware target velocity within 500 ms.
- Automatic localization can recover from an early failed frame, indoor global TF stays planar, and named-goal cancellation is preserved across both Action layers.
- The STM32 firmware must still be rebuilt and flashed after updating the Jetson; its watchdog is not active until that flash is complete.

### [Complete Foxglove Indoor Navigation Surface]

#### File

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/bringup/foxglove/indoor_navigation.json`
- `src/bringup/launch/system_travel.launch.py`
- `scripts/launch_with_logs.sh`, `Makefile`
- Matching package, tests, and bilingual documentation

#### Change

- Travel now starts official `foxglove_bridge:8765` by default, with `FYP_USE_FOXGLOVE=false` and `foxglove_port:=<port>` overrides.
- Added a Foxglove navigation adapter: clicked Pose to `NavigateToPose`, destination String to `NavigateNamedDestination`, and a Trigger service to cancel the current goal.
- The adapter publishes transient-local destination catalog, destination MarkerArray, and navigation status; it rejects goals while localization is unhealthy and cancels an active goal on degradation.
- Bridge client-publish/service access uses a minimum whitelist and omits parameter mutation, so Foxglove cannot publish `/cmd_vel*` or modify runtime parameters.
- Added an importable layout combining 3D map/clouds/costmaps/path/safety zones, localization/navigation status, named-goal publishing, region relocalization, cancel, logs, and Topic Graph.

#### Reason

- Foxglove natively supports Topics, Services, and clicked Pose publishing, but has no convenient panel for this custom ROS 2 Action. Manipulating Action transport endpoints directly would be unclear and unsafe.
- Operators need to replace RViz with a computer-side surface for localization, goals, and status without bypassing localization gates, Action cancellation, or the pre-serial velocity safety chain.

#### Effect

- Indoor Travel node topology, localization, clicked navigation, named navigation, and cancellation can be operated from one Foxglove layout.
- The adapter never accesses `/cmd_vel*`; a Foxglove connection cannot bypass Nav2, localizer, or Collision Monitor for direct motion control.
- Port `8765` is for controlled LAN/Tailscale access only and must not be exposed publicly. Jetson still needs `ros-humble-foxglove-bridge` and a vehicle smoke test.

### [Full Indoor Mapping and Named Navigation Phases 1-5]

#### File

- `scripts/mapping_session_utils.py`, `scripts/save_mapping_session.py`
- `src/perception/fastlio2/`, `src/perception/localizer/`, `src/perception/interface/`
- `src/navigation/indoor_navigation_manager/`
- `src/bringup/launch/system_{slam,travel}.launch.py`
- `src/bringup/config/{master_params,nav2_travel,collision_monitor_travel}.yaml`
- `src/bringup/behavior_trees/travel_*_fail_stop.xml`
- Matching bilingual architecture, commands, FAST-LIO2, Nav2 tuning, and indoor design documents

#### Change

- Added a provisional `[-0.20,1.80]m` structural localization cloud from FAST-LIO2 and made both PGO and localizer consume it, while preserving the low LaserScan slice and Nav2 obstacle stream.
- Upgraded map saving to a versioned `runtime-data/maps/indoor/<map_id>` bundle with pose graph, raw/downsampled PCD, Scan Context, regions/destinations, `T_map_2d_map_3d`, RMSE/p95/overlap report, and overlay; stationary, chassis, frame, integrity, or calibration failures return nonzero.
- Added structured localization status, automatic/region/manual relocalization, multi-candidate ambiguity rejection, 1Hz continuous ICP, overlap/jump gates, and `alpha=0.15` correction smoothing.
- Travel only loads fully accepted bundles. Its velocity chain now includes a fail-closed localization gate and Collision Monitor, with a `650x500mm + 25mm/side` polygon, footprint critic, path collision checks, and `0.20rad` terminal yaw tolerance.
- Added `NavigateNamedDestination` and its manager with aliases, map/backend checks, feedback, cancellation, concurrency rejection, and cancellation on localization degradation. SLAM now records a lean evidence bag by default.

#### Reason

- One low cloud window cannot simultaneously serve clean 2D mapping, 3D place recognition, and tall-obstacle detection; offline map and online localization also need identical structural semantics.
- Slam Toolbox 2D and PGO 3D maps use different optimizers and cannot be assumed to share a frame; an uncalibrated or incomplete bundle must not drive the vehicle.
- The vehicle has no fixed charger/start pose, so requiring an RViz seed every run is not the final workflow; automatic localization must also reject repeated-corridor ambiguity instead of false-localizing.
- The chassis turns in place, but a circular footprint misrepresents its rotational envelope, and a display-only localization warning is insufficient for fail-stop safety.

#### Effect

- Software phases 1-5 now expose the end-to-end interfaces. Production Travel accepts one `map_bundle`, and users can send a destination name without the RViz goal tool.
- The current height window, ICP/registration gates, footprint margin, and Collision Monitor zones are conservative starting values, not vehicle acceptance results. Build on Jetson with `--parallel-workers 1`, then run arbitrary-start, narrow-door, dynamic-obstacle, localization-loss stop, and ten-round-trip tests.
- Indoor/outdoor algorithm switching remains out of scope; do not add a second localization authority before indoor acceptance.

### [Pinned SLAM Profile and Travel MPPI Controller-Period Fix]

#### File

- `src/bringup/config/slam_toolbox_mapping.yaml`
- `src/bringup/launch/system_slam.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/test/test_system_slam_launch.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Copied the Slam Toolbox Humble async-mapper baseline into repository configuration, retained the launch-time `transform_timeout=1.0` and `tf_buffer_duration=60.0` values, and standardized `base_frame=base_footprint`.
- Changed `system_slam.launch.py` to load repository-owned parameters instead of the installed default YAML.
- Corrected Travel `controller_frequency` from `15Hz` to `20Hz` to match MPPI `model_dt=0.05s`.
- Added SLAM launch/parameter contract tests and updated the Travel parameter assertion.

#### Reason

- Installed Slam Toolbox defaults are outside repository version control, so reinstalling or changing dependencies can make mapping behavior irreproducible.
- Vehicle logs already proved that a `15Hz` control period exceeds the `0.05s` model step, causing MPPI configuration rejection and Nav2 lifecycle startup failure.

#### Effect

- This round keeps the Humble upstream matcher baseline and introduces no unvalidated matcher tuning; the TF base frame now matches FAST-LIO2.
- Travel keeps its existing speed, sample count, and critics while moving to a valid `20Hz` control loop; Jetson build and vehicle smoke testing remain required.

### [SLAM Mapping and Indoor Navigation Design Plan]

#### File

- `docs-CN/knowledge/indoor_mapping_navigation_design.md`
- `docs-EN/knowledge/indoor_mapping_navigation_design.md`
- `docs-CN/index.md`
- `docs-EN/index.md`

#### Change

- Added a bilingual design baseline for SLAM mapping and Travel indoor navigation, covering map assets, TF ownership, localization states, planning/control, safety, and the named-destination interface.
- Kept the clean Slam Toolbox 2D OccupancyGrid as the Nav2 navigation map and assigned PGO 3D PCD/keyframes to global relocalization; the map bundle connects them through an explicit `T_map_2d_map_3d` and quality report.
- Split development into baseline freeze, production map bundle, Travel safety/control, low-rate drift correction, arbitrary-start global localization, and named navigation, with a vehicle test matrix and definition of done.

#### Reason

- The current SLAM/Travel chain can save maps and navigate after an RViz-seeded ICP, but it does not yet provide strict 2D/3D calibration, automatic global localization, runtime drift correction, or a user-facing destination interface.
- A direct PGO projection is less clean than a Slam Toolbox map, so the design preserves both map strengths while removing the unsafe assumption that their frames are inherently identical.

#### Effect

- This change defines the development plan only; it does not alter runtime code or parameters.
- Future indoor work follows these phases, and automatic indoor/outdoor switching remains out of scope until the indoor definition of done is met.

### [Travel Indoor Prior-Map Navigation Stabilization]

#### File

- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_*_fail_stop.xml`
- `src/bringup/scripts/initialpose_relocalize_bridge.py`
- `src/bringup/scripts/nav2_cloud_retime.py`
- `src/perception/localizer/`

#### Change

- Added RViz initial-pose to ICP relocalization bridging, current-stamp Nav2 obstacle-cloud retiming, and Travel-specific fail-stop behavior trees.
- Assigned `map -> odom` to the ICP localizer, froze successful corrections for navigation, and configured static-map global planning with a near-field point-cloud local costmap.
- Replaced the legacy DWB profile with the indoor low-load MPPI profile: `vx_max=0.35`, `wz_max=0.65`, `controller_frequency=15 Hz`, and `batch_size=500`.

#### Reason

- Legacy Travel mixed live obstacles into global planning, allowed automatic motion recoveries indoors, and imposed excessive controller/costmap load on the Jetson.

#### Effect

- Operators must confirm a successful ICP relocalization before issuing a goal. If planning or local control cannot find a safe trajectory, Travel stops and returns failure instead of spinning, reversing, or clearing costmaps automatically.

---

## 2026.07.09

### [Documentation Update]

#### File
- `CONTRIBUTING-EN.md`
- `CONTRIBUTING.md`
- `README.md`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/devlog/2026-07.md`
- `docs-CN/index.md`
- `docs-CN/known_issues.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/handoff/rtk_test.md`
- `docs-EN/index.md`
- `docs-EN/known_issues.md`

#### Change
- Updated documents to current hardware status
- Simplified and removed commands based on new `.bashrc`
- Simplified contributing guide to improve focus on key requirements
- Updated status of some issues in the tracker

### [Corridor Default FGO Shadow Bag Evidence]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Corridor launch now starts the `rtk_fgo_localizer` shadow node by default and forces `publish_tf=false`, `nav2_use_fgo=false`.
- Added an `enable_fgo_shadow` launch argument, defaulting from `FYP_CORRIDOR_ENABLE_FGO_SHADOW`, so the shadow node can be disabled when needed.
- Added `/livox/imu`, `/odom_CBoar`, and `/rtk_fgo/*` to the default corridor lean bag; raw `/livox/lidar` and `/fastlio2/body_cloud*` remain debug-profile topics.
- Added launch text coverage to keep default FGO shadow from owning TF/Nav2 and to ensure future corridor bags contain the evidence needed for FGO replay.

#### Reason

- The 2026-07-09 offline replay showed the latest FGO shadow can enter `RTK_LOCKED` and keep committing on the 15:59, 17:55, 18:00, and 18:02 bags, but old corridor bags did not include `/odom_CBoar` or `/livox/imu`, so the full IMU/wheel/FGO path could not be evaluated.
- Deciding whether FGO can later support weak-RTK fallback needs vehicle bags with `/rtk_fgo/status`, `/rtk_fgo/factor_diagnostics`, chassis odom, and Livox IMU captured by default instead of relying on manual extra recording.

#### Effect

- The next `make launch-corridor` / `bash scripts/launch_with_logs.sh corridor` bag can be used directly for FGO shadow replay metrics.
- FGO shadow only publishes `/rtk_fgo/*`; it does not broadcast production `map->odom` and does not remap Nav2, so the current RTK-authoritative navigation chain is unchanged.
- Default bag size increases slightly, but raw Livox point clouds are still excluded; use `FYP_CORRIDOR_BAG_PROFILE=debug` when full point-cloud replay is required.

### [Corridor MPPI Near-Field Planning and RTK Raw Target Jump Gating]

#### File

- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added last-trusted raw target state to `rtk_map_odom_corrector`; `TARGET_JUMP` gating now compares the current raw target with the last trusted raw target instead of comparing it with the smoothed/limited output.
- Appended the raw-target-to-output gap to `/localization_authority/diagnostics`, making future bags able to separate RTK raw jumps from output smoothing lag.
- Raised corridor Nav2 `failure_tolerance` to `1.5s`, so a short MPPI optimizer failure around local obstacles does not abort on the first failed cycle.
- Narrowed the corridor local costmap from `30m x 30m` to `12m x 12m`, and reduced STVL marking `obstacle_range` from `15m` to `5m`.
- Raised corridor `CostCritic.cost_weight` from `4.5` to `7.0`, giving dynamic obstacle cost more priority over strict path hugging.

#### Reason

- In the 2026-07-09 17:55 bag, RTK stayed mostly `q=4` and `gps_global_aligner` raw alignment moved only slightly, but `/localization_authority/status` produced many `TARGET_JUMP` samples; the root cause was the old gate treating raw-target-to-lagging-output distance as an RTK jump while smoothing/step limiting was behind.
- The 18:00/18:02 failures concentrated more on MPPI local planning: the pedestrian-blocked run should be treated as a correct local-costmap stop-and-try-avoid behavior, while the other run ended from transient optimizer/progress failure.
- The visible weaving in the 17:55 run coincided with frequent angular sign changes, `map->odom` freeze/catch-up behavior, and MPPI path tracking; lowering angular speed alone does not remove that root cause.

#### Effect

- Stable but lagging `map->odom` output should no longer push RTK authority into long false `TARGET_JUMP` holds, reducing late-run weaving and self-stops.
- Corridor keeps fail-stop pedestrian behavior, while the local costmap now focuses on near-field obstacles so far sparse returns and a large rolling grid interfere less with MPPI.
- The next bag can use the diagnostics raw/output gap to decide whether remaining failures are still authority lag or have moved to pure MPPI/chassis execution.

### [RTK Authority map_base Jump-Gate Offline Validation]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/architecture.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added `compute_authority_target_delta()`, binding authority target-jump translation/yaw checks to raw RTK `map_base` instead of raw `map->odom` target.
- Added `_last_raw_map_base` to `rtk_map_odom_corrector`; the node records the last trusted raw RTK `map_base` after each normal publish.
- Kept raw `map->odom` target low-pass filtering, per-step limiting, and raw/output gap diagnostics for the actual published `map->odom`.
- Added a unit test for the case where raw RTK `map_base` moves only about `0.32m`, but raw `map->odom` target translation exceeds `6m` because the vehicle is about `67m` from the odom origin and yaw changes.

#### Reason

- Offline replay across 6 corridor bags on the Linux analysis machine showed the old `map->odom` target gate would reject hundreds of samples in bags such as `2026-07-09-14-23-47`, `15-45-27`, and `17-55-23`; at the same time, raw RTK `map_base` max step stayed around `0.20-0.36m`, so RTK vehicle position was not truly jumping by meters.
- The root cause is `map->odom = map_base * inverse(odom_base)`: when the vehicle is tens of meters from the odom origin, a few degrees of yaw correction can amplify raw `map->odom` target translation into the tens of meters, which does not mean the RTK vehicle pose jumped.

#### Effect

- The 17:55-style failure where RTK `map_base` is smooth but authority freezes in long `TARGET_JUMP` windows should be removed; offline replay of all 6 bags produced `0` rejects with the new map_base gate.
- `map->odom` still releases through the existing low-pass filter and `max_base_yaw_step_m` limiter, so the amplified target is not published into TF in one step.

### [RTK Corridor Obstacle Cloud Isolation and map->odom Target Smoothing]

#### File

- `src/perception/fastlio2/src/map_builder/commons.h`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/perception/fastlio2/config/lio.yaml`
- `src/bringup/config/master_params.yaml`
- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/knowledge/fastlio2.md`

#### Change

- Added `/fastlio2/body_cloud_nav2_obstacles` at the FAST-LIO2 publish layer with a `[-0.20, 1.20]m` relative-height window for Nav2 local costmap marking/clearing.
- Kept `/fastlio2/body_cloud` on the existing low `[-0.33, 0.30]m` window; PGO/LIO still consume the original topic.
- Pointed corridor Nav2 STVL mark/clear sources to `/fastlio2/body_cloud_nav2_obstacles` and added that topic to the debug bag profile.
- Added target deadband + low-pass smoothing between the raw RTK target and the existing per-step limiter in `rtk_map_odom_corrector`, defaulting to `alpha=0.20`, `0.05m` translation deadband, and `0.25deg` yaw deadband.
- Added corridor/FAST-LIO2/RTK authority contract tests so future edits cannot silently reconnect Nav2 to the low obstacle cloud or remove target smoothing.

#### Reason

- In the recent field run, the user stood in front of the vehicle but the vehicle did not avoid them. One root cause is that Nav2 reused `/fastlio2/body_cloud`, which had already been publish-filtered down to `0.30m` for PGO/LIO stability, so most standing-pedestrian returns never reached the costmap.
- The later "snake-like" path is consistent with small RTK/heading target jitter continuously entering `map->odom`. Per-step limiting alone still chases the noisy target every frame, so the target needs deadband and low-pass filtering before release.

#### Effect

- The corridor local costmap can see more complete standing pedestrians and outdoor barriers while PGO/LIO retain their lightweight low-window cloud, avoiding extra graph/mapping load and elevated clutter.
- RTK authority remains the single `map->odom` owner, but small global target jitter is absorbed before TF publication, which should reduce late-run side-to-side weaving.
- If the next run still misses obstacles, record with `FYP_CORRIDOR_BAG_PROFILE=debug` and inspect `/fastlio2/body_cloud_nav2_obstacles` plus local costmap marking; if weaving remains, compare raw target jump in `/localization_authority/diagnostics` against output TF.

## 2026.07.08

### [Makefile and bashrc improvements]

#### File

- `Makefile`
- `docs-CN/commands.md`
- `docs-CN/handoff/rtk_test.md`
- `docs-EN/commands.md`
- `docs-EN/handoff/rtk_test.md`
- `scripts/.bashrc`

#### Change

- Moved `source /opt/ros/humble/setup.bash && colcon build ...` into a single variable at the top `COLCON_BUILD`
- Replaced repeated launch targets with pattern rules
- Added build-bringup and build-fastlio2 for builing packages on the most recent changes
- Updated .bashrc aliases like `ss` to quickly source the workspace
- Added `mbuild` function in .bashrc to run `make build-*` and automatically source after finishing build
- Simplified commands documentation to avoid unnecesary commands
- Changed the bashrc file name to `.bashrc` (previous was `bashrc.sh`)

#### Reason

- The Makefile was getting too messy with repeated commands following the same pattern; the new layout is cleaner and easier to modify
- New aliases like `s1`, `ss`, `gs`, `rc` cover some common commands to avoid typing long commands every time
- Running any test on the robot required the same repetitive commands such as `cd ~/XJTLU-autonomous-vehicle`, `source /opt/ros/humble/setup.bash`, and `source install/setup.bash`. The new changes automatically run them to avoid clutter.

#### Effect
- Adding new launch modes no longes requires modifying the makefile
- Running `ss` is equivalent of sourcing ros2 humble and install/setup.bash
- Launching any mode no longer requires changing folders or sourcing workspace
- Building with `mbuild` no longer requires sourcing after building

### [Git docs]

#### File

- `docs-EN/handoff/rtk_test.md`
- `docs-CN/handoff/rtk_test.md`

#### Change

Added common git commands to handle branches in the Jetson and rebuilding for new updates

### [FAST-LIO2 Outdoor Point-Structure Retention Tune]

#### File

- `src/bringup/config/master_params.yaml`
- `src/perception/fastlio2/config/lio.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/knowledge/fastlio2.md`

#### Change

- Changed the primary FAST-LIO2 outdoor profile from `lidar_filter_num=6` to `4`, and from `lidar_max_range=15.0m` to `25.0m`.
- Updated the package fallback `fastlio2/config/lio.yaml` as well, so standalone FAST-LIO2 launch does not fall back to the old sparse profile.
- Added a corridor launch contract test requiring both master and fallback FAST-LIO2 profiles to keep enough outdoor structure points.

#### Reason

- Recent corridor bags showed only a few hundred points left after FAST-LIO2 input filtering in some frames; plaza-like areas already have sparse distant structure, and the old `6/15m` profile can further remove useful plane/edge constraints.
- This change intentionally leaves the Livox hardware timestamp path untouched for now, so CPU overload, time synchronization, and point density are not changed at the same time.
- Because the Jetson may still be CPU-constrained, this uses a moderate `4/25m` profile instead of immediately switching to the heavier `3/25m` profile.

#### Effect

- Outdoor corridor runs keep more distant walls, trees, poles, and other matchable structure, reducing the chance of `NO Effective Points` and LIO degeneracy.
- FAST-LIO2 compute load increases versus the old profile, but remains more conservative than `lidar_filter_num: 3`; the next field run should watch `/fastlio2/degeneracy`, FAST-LIO2 IMU sample counts, and tegrastats together.

### [RTK Corridor Runtime Cleanup and Single Robot Description]

#### File

- `Makefile`
- `scripts/launch_with_logs.sh`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`

#### Change

- Added `[r]tk_map_odom_corrector`, `[s]erial_reader_node`, `[j]oint_state_publisher`, nav-gps `gps_anchor_localizer` / `route_server` / `goal_manager_node`, and slam `async_slam_toolbox_node` / `map_saver_server` to both the INT and KILL `pkill` lists used by `make kill` / `make kill-runtime`.
- Added the same cleanup targets to `scripts/launch_with_logs.sh`, covering both startup cleanup and exit cleanup for these easy-to-leave-behind nodes.
- Removed the duplicate outer `robot_description.launch.py` include from `system_gps_corridor.launch.py`; corridor now relies on the robot description already started by the inner `system_explore.launch.py`.
- Added corridor runtime cleanup and single-robot-description contract tests so the RTK authority, chassis feedback, joint-state publisher, and cross-mode navigation nodes cannot silently drop out of cleanup again.

#### Reason

- The 2026-07-08 18:00, 18:02, and 18:07 corridor logs showed old `rtk_map_odom_corrector` processes continuing to run after their sessions ended and still publishing `map -> odom`.
- During the latest run, at least three correctors were alive at the same time, causing competing `map -> odom` publications and meter-scale TF jumps; the route then failed on waypoint 4 with `ODOM_DIVERGENCE_ABORT|STEP_1.08m_IN_0.00s`.
- A second audit found that corridor also starts `serial_reader_node` and `joint_state_publisher` through the explore/robot_description launch tree; stale `serial_reader_node` is part of the chassis feedback path and can pollute the next runtime session.
- The outer corridor launch also included `robot_description.launch.py` directly, duplicating the robot/joint state publishers already included by explore; the duplicate static transforms usually do not steer the vehicle by themselves, but they make the ROS graph, `/tf_static`, and joint-state publishing noisier.
- When switching back from nav-gps or slam into corridor, stale route/map/slam nodes can also leave action servers, map services, or TF diagnostics in a less clean state.

#### Effect

- `make kill`, `make kill-runtime`, and `launch_with_logs.sh` startup/exit cleanup now terminate the RTK authority `map -> odom` publisher, chassis feedback node, joint-state publisher, and cross-mode navigation leftovers.
- Corridor startup now keeps only one robot description publisher set, reducing duplicate node names, duplicate `/tf_static`, and field-debugging noise.
- The next corridor run should no longer see multiple stale correctors competing to publish `map -> odom`, and it has less risk of old chassis feedback or nav-gps/slam processes leaking into a new session.

### [Decouple RTK Corridor Global Planning From Local Obstacle Avoidance]

#### File

- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added `nav2_corridor_rtk.yaml` as the corridor-specific Nav2 base; launch still overlays the existing RTK small-speedup, terminal stop-hold, and no-physical-recovery profile at startup.
- Added the empty `default_nav_through_poses_bt_xml` rewrite slot to `nav2_corridor_rtk.yaml` so launch can inject the corridor no-physical-recovery through-poses behavior tree.
- Changed the corridor global costmap to route-level RTK planning only: `inflation_layer` is the only plugin, unknown tracking is disabled, and the global planning radius/inflation are reduced.
- Kept STVL, FRC, and inflation in the local costmap with the real robot radius and live Livox obstacles, preserving automatic obstacle avoidance.
- Changed the `gps_route_runner` default RTK subgoal spacing from `30m` to `5m`, reducing global planning failures when long goals cross rolling-window edges.
- Added launch / YAML / route-runner contract tests so corridor does not silently fall back to Explore's heavier global obstacle setup.

#### Reason

- The 2026-07-08 corridor failure looked more like Nav2 global planning sensitivity to long RTK subgoals plus rolling global costmap unknown/obstacle layers than an RTK fix loss.
- A later same-day retest showed `bt_navigator` was still loading Nav2's upstream `navigate_through_poses_w_replanning_and_recovery.xml` and then waiting for a missing `spin` action server because corridor only loads the `wait` behavior; the root cause was that the new corridor YAML missed the through-poses rewrite key.
- In the RTK-authoritative architecture, the global path only needs to advance the robot toward the next RTK subgoal; live obstacle avoidance belongs in local costmap + MPPI.
- If the global costmap also owns wide-area dynamic obstacles, unknown space, and long-distance subgoals, outdoor plazas and boundary regions can fail before the local controller gets a chance to avoid obstacles.

#### Effect

- Corridor is less likely to fail NavFn directly on global costmap edges or unknown space.
- Nav2 lifecycle can load the corridor through-poses no-recovery tree correctly and no longer fails during startup while waiting for the absent `spin` action server.
- Automatic obstacle avoidance is not disabled; Livox/FRC obstacles still feed the local costmap used by MPPI.
- RTK routes advance through shorter subgoals, which also leaves a cleaner interface for later indoor/outdoor switching or prior-map relocalization without rewriting the Nav2 main chain.

## 2026.07.07

### [Jetson ~/.bashrc Environment Tracking and Optimization]

#### File

- `scripts/bashrc.sh`
- `docs-EN/commands.md`
- `docs-CN/commands.md`

#### Change

- Created `scripts/bashrc.sh` to track and version-control the Jetson's `~/.bashrc` configuration within the repository
- Customized the configuration to automatically display a comprehensive overview of the robot's current state upon login
- Added sourcing for frequently used robot commands
- Documented a standardized, step-by-step workflow for safely overwriting and updating the Jetson's local `~/.bashrc` using Vim

#### Reason

- Local environment configurations on the Jetson often drifted or were lost when multiple developers modified them directly, making environment replication difficult
- Team members needed a quick, automated way to inspect the robot's diagnostics and connectivity status immediately upon logging in via SSH or a local terminal

#### Effect

- The `~/.bashrc` file is now fully version-controlled; all environment-level custom commands can be reviewed and tracked via git history
- Opening any new terminal or SSH session now greets the user with an intuitive summary of the robot's real-time state
- Establishes a clean workflow that prevents untracked, ad-hoc modifications directly on the Jetson

### [Corridor Nav2 Startup Bootstrap TF Gate Fix]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added RTK authority helpers for selecting between external alignment and stored bootstrap alignment; when external alignment has not appeared yet, the corrector reuses the bootstrap alignment it already established.
- `rtk_map_odom_corrector` may now publish bootstrap `map->odom` before `/rtk/status` reaches RTK Fixed. This output is still reported as `RTK_BOOTSTRAP` and exists only to give Nav2 lifecycle a valid `map` frame.
- Added regression tests for reusing bootstrap alignment without external alignment and for allowing bootstrap TF publication before Fixed status during Nav2 startup.

#### Reason

- The 2026-07-07 10:11 corridor startup stalled at `GPS stable, waiting for Nav2...`; logs showed `planner_server` repeatedly failing global costmap activation with `Invalid frame ID "map"`, while `gps_global_aligner` timed out waiting for `map->base_link`.
- The root cause was a startup dependency loop: Nav2 costmaps need `map->base_link` to activate; the global aligner needs `map->base_link` before it can publish external alignment; and the corrector previously did not publish bootstrap `map->odom` before the RTK Fixed gate, so the `map` frame never appeared.

#### Effect

- During corridor startup, any valid fix/heading/odom bootstrap can create `map->odom` early enough for Nav2 lifecycle to activate instead of hanging on a missing `map` frame.
- Route execution is still gated by `gps_route_runner` stable-fix and alignment checks; non-Fixed bootstrap only solves startup TF availability and does not loosen the driving gate.

### [RTK Corridor Small Speedup and Yaw Reacquisition]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `src/bringup/config/master_params.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- RTK authority input gating now supports `YAW_REACQUIRE`: when RTK/heading/alignment are fresh, translation jump is still within the safe limit, and yaw target jump is inside the `45deg` reacquisition window, the corrector no longer freezes `map->odom`; it keeps publishing through the existing step limiter.
- `rtk_map_odom_corrector` adds `allow_yaw_reacquire` and `max_yaw_reacquire_jump_deg`, and corridor defaults lower `max_yaw_step_deg` from `1.0` to `0.5`.
- The corridor launch-time Nav2 profile raises `vx_max` from `0.45` to `0.55`, while lowering `wz_max` from `0.65` to `0.50`, `wz_std` from `0.14` to `0.10`, and `az_max` from `2.0` to `1.0`; velocity smoother angular limits are tightened accordingly.
- Added regression/contract tests for RTK yaw reacquisition and the corridor profile.

#### Reason

- The successful `2026-07-07-08-48-14` bag showed moving `vx` median `0.428m/s`, with p95/max already at the old `0.45m/s` limit, so a small speedup is justified.
- In the same bag, moving `|wz|` p95 was `0.537rad/s` and angular velocity changed sign about `0.53/s`, indicating the visible left-right oscillation comes mostly from aggressive yaw correction rather than insufficient linear speed.
- The bag also showed a `TARGET_YAW_JUMP` window of about `40deg` at the lateral-error peak; freezing TF through that window leaves Nav2 chasing a stale `map->odom`.

#### Effect

- The next corridor field run should be slightly faster on straight segments while using more conservative angular velocity and angular acceleration, reducing side-to-side oscillation.
- Yaw target jumps up to 40-45 degrees are reacquired smoothly through a smaller yaw step; jumps outside the window or accompanied by large translation jumps still degrade instead of pulling bad localization directly into TF.
- Explore mainline parameters are unchanged; this tuning is injected only by `system_gps_corridor.launch.py` into the temporary corridor Nav2 parameter file.

### [RTK Corrector Shutdown-Safe Publication Fix]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`

#### Change

- `rtk_map_odom_corrector_node` now checks that the rclpy context is still valid at timer callback entry.
- Mode/status/diagnostics publications and `map->odom` TF broadcasting now go through shutdown-safe helpers that ignore publish attempts after the context is invalid.
- Added a regression test requiring the corrector to keep the shutdown-safe publish path.

#### Reason

- A Jetson single-node smoke test found that timeout/launch shutdown can invalidate the rclpy context before a timer callback finishes publishing, triggering `publisher's context is invalid`.
- The exception does not indicate a localization math error, but it creates misleading node-crash logs during corridor timeout or clean shutdown and can hide the real navigation state.

#### Effect

- During corridor shutdown, timeout, or monitor-triggered launch stop, the corrector no longer throws publish exceptions from a shutdown race.
- Normal RTK-authoritative computation, gates, step limiting, and TF publication behavior are unchanged while the node is running.

### [Corridor RTK-Authoritative `map->odom` Localization Layer]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/setup.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `src/bringup/config/master_params.yaml`
- `src/bringup/config/pgo_corridor_no_gps.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs/superpowers/specs/2026-07-07-rtk-authoritative-map-odom-design.md`
- `docs/superpowers/plans/2026-07-07-rtk-authoritative-map-odom.md`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added `rtk_map_odom_corrector_node`, which computes and publishes the only corridor `map->odom` from RTK fix, dual-antenna heading, `/gps_corridor/enu_to_map`, and the current `odom->base_link`
- The corrector enters `RTK_BOOTSTRAP` when external `ENU->map` is not available yet, publishing a temporary `map->odom` from current RTK/heading/odom so the aligner can start after PGO TF is disabled
- Added ROS-free `rtk_authority.py` helpers for `T_map_odom = T_map_base * inverse(T_odom_base)`, RTK map-pose projection, single-step limiting, and input gating
- Set corridor's PGO override to `publish_tf: false` so PGO and RTK authority cannot publish competing `map->odom` transforms
- Added `/localization_authority/mode`, `/localization_authority/status`, and `/localization_authority/diagnostics` to the corridor rosbag
- Documented the future indoor/outdoor handoff interface: an accurate outdoor RTK pose can seed prior-map relocalization, and a later prior-map authority can reuse the same `map->odom` interface

#### Reason

- Field analysis confirmed that about 1 m of LIO drift is enough to move Nav2's `map->base_link` off the GPS route and stop navigation; simply widening Nav2 tolerance or only adding GPS factors to PGO is not enough for stable vehicle runs
- RTK Fixed plus dual-antenna heading is more trustworthy outdoors than the current open-area LIO global drift, so it should own the corridor global pose authority
- Once PGO TF is disabled, the stack needs an initial `map->odom` that does not depend on aligner output so `gps_global_aligner` can finish bootstrap
- The planned indoor/outdoor transition depends on handing off from accurate outdoor RTK pose to indoor prior-map relocalization, so this change introduces an extensible authority state and diagnostic interface instead of a corridor-only hack

#### Effect

- In corridor mode, Nav2's `map->base_link` is pulled back by RTK-authoritative `map->odom` instead of fully following meter-level LIO global drift
- FAST-LIO2 still provides local `odom->base_link` continuity and point-cloud obstacle input
- If RTK, heading, alignment, Fixed status, or target jump gates fail, the authority degrades and stops updating rather than pulling TF with a bad GPS sample
- A future prior-map relocalizer can reuse `/localization_authority/*` without rewriting the Nav2 or route-runner main chain

## 2026.07.06

### [Fix Corridor RTK Nav2 False Success And Recovery `/cmd_vel` Contention]

#### File

- `scripts/monitor_corridor_status.py`
- `src/bringup/behavior_trees/navigate_through_poses_w_replanning_5hz_no_motion_recovery.xml`
- `src/bringup/behavior_trees/navigate_to_pose_w_replanning_5hz_no_motion_recovery.xml`
- `src/bringup/config/nav2_explore.yaml`
- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_monitor_corridor_status.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_global_aligner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/perception/pgo_gps_fusion/src/pgo_node.cpp`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change

- Added corridor NavigateToPose / NavigateThroughPoses behavior trees without `Spin` / `BackUp`, and made `system_explore.launch.py` accept mode-specific `nav_to_pose_bt_xml` and `nav_through_poses_bt_xml`
- Added the empty `default_nav_through_poses_bt_xml` default slot to `nav2_explore.yaml`, so the launch-time through-poses BT rewrite actually reaches `bt_navigator`
- In corridor-generated Nav2 params, raised BT `default_server_timeout` to 1000ms, changed `bt_loop_duration` to 50ms, and limited `behavior_server` to `wait`
- When `gps_route_runner` detects a Nav2 false success, the foreground monitor now reports `Nav2 误报到达但路线无进展` instead of letting aligner shutdown noise hide the real failure
- `gps_global_aligner` now treats rcl context shutdown as a clean shutdown instead of publishing misleading `ALIGNER_ABORTED`
- PGO republishes the latest `map->odom` at timer rate when no new cloud is available, so Nav2 can still query a current transform during low-rate or dropped LIO frames

#### Reason

- In the 2026-07-06 outdoor RTK corridor run, Nav2's 20ms action acknowledgement timeout was too short: FollowPath was still executing while the BT entered recovery, interleaving forward, reverse, and in-place spin commands on `/cmd_vel`
- The 2026-07-06 22:33 vehicle log showed `bt_navigator` was still loading Nav2's upstream `navigate_through_poses_w_replanning_and_recovery.xml`; the root cause was that the base YAML lacked the through-poses rewrite key, so RewrittenYaml could not replace the missing parameter
- In the same run, PGO `map->odom` publication followed valid LIO frames; when LIO dropped frames, Nav2 controller lookups at current time failed and then reported `Reached the goal`
- After `gps_route_runner` detected `NAV2_FALSE_SUCCESS_ABORT`, launch shutdown caused an aligner rcl context exception that masked the real route failure in the foreground console

#### Effect

- Corridor RTK validation no longer runs physical recovery spin/backup; failures stop the vehicle and report route-progress context
- The NavigateThroughPoses action also uses the corridor no-recovery BT, avoiding Nav2 lifecycle startup failure from a missing `spin` action server when `behavior_server` only loads `wait`
- Nav2 FollowPath no longer enters recovery just because action acknowledgement is tens of milliseconds late under Jetson load
- `map->odom` remains queryable at current time during low-rate or short LIO gaps, reducing transform-too-old and Nav2 false-success risk
- The foreground console points directly to `NAV2_FALSE_SUCCESS_ABORT`, avoiding the misleading “aligner startup failed” message

### [NTRIP Account Setup Script Update]

#### File

- `scripts/setup_ntrip.sh`
- `scripts/setup_ntrip.py`
- `Makefile`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `~/.bashrc`
- `docs-EN/handoff/rtk_test.md`
- `docs-CN/handoff/rtk_test.md`

#### Change
- Deleted unused `scripts/setup_ntrip.sh`
- Updated c with more intuitive login flow, asking explicitly for username and password, IP/mountpoint, testing the connection, and storing credentials automatically
- Added `make` launch modes for logging in with updated python script
- Updated commands with description of each `make ntrip-*` launch purpose
- `~/.bashrc` has been modified to show the NTRIP status on every terminal login

#### Reason
- We frequently change NTRIP credentials, and the current setup requires reauthentication on every new terminal as credentials do not persist
- Login commands are difficult to use and do not test the connection right away
- Previous login script was not easy to use since the credentials message format changes from vendor to vendor, which lead to previous script being unused
- The newer script is much easier to use, and allows to login with one single command, without needing to source or authenticate on every new terminal

#### Effect
- All NTRIP credentials are now handled from `scripts/setup_ntrip.py`
- Credentials persist in the robot, and are stored in `~/.ntrip_config.json`, `~/.ntrip_credentials.json`, `/tmp/um982_cors.yaml`, `/tmp/ntrip_env.sh`.
- Current NTRIP status is now shown on every terminal login
- RTK testing commands are greatly simplified

## 2026.07.02

### [URDF added in launch files]

#### File

- `.gitignore`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `src/bringup/config/master_params.yaml`
- `src/bringup/launch/robot_description.launch.py`
- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_explore_gps.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/launch/system_nav_gps.launch.py`
- `src/bringup/launch/system_rtk_basic.launch.py`
- `src/bringup/launch/system_slam.launch.py`
- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/urdf/rosbot/actuator/caster.urdf.xacro`
- `src/bringup/urdf/rosbot/actuator/wheel.urdf.xacro`
- `src/bringup/urdf/rosbot/base.urdf.xacro`
- `src/bringup/urdf/rosbot/common_inertia.xacro`
- `src/bringup/urdf/rosbot/plugins/gazebo_control_plugin.xacro`
- `src/bringup/urdf/rosbot/rosbot.urdf.xacro`
- `src/bringup/urdf/rosbot/sensor/camera.urdf.xacro`
- `src/bringup/urdf/rosbot/sensor/imu.urdf.xacro`
- `src/bringup/urdf/rosbot/sensor/laser.urdf.xacro`

#### Change

- Added partial URDF files from previous years development
- Added new `robot_description.launch.py` launch file to load the URDF files
- Modified all launch files in `src/bringup/launch/` to call the new `robot_description` launch file
- Modified `.gitignore` to ignore `*:Zone.Identifier` files that are autogenerated when transferring files from Windows to Linux
- In `src/bringup/config/master_params.yaml`, modified the `body_frame` to use `base_footprint` (previous was `base_link`)

#### Reason

- To add URDF files to the project and have them render properly in Foxglove, providing a clear view of the robot's position and orientation
- All launch files in `src/bringup/launch/` must be updated to include the `robot_description` launch file to load the URDF files
- `.gitignore` to ignore the `Zone.Identifier` files that do not contribute to the project
- URDF TF tree joins `base_footprint` as the parent of `base_link`. To ensure only one parent, the new body frame must be `base_footprint`.

#### Effect

- Robot can be visualized in Foxglove
- All launch files in `src/bringup/launch/` include the `robot_description` launch file
- `.gitignore` ignores the `Zone.Identifier` files
- The TF tree has been changed, where the robot frame is now `base_footprint` instead of `base_link`

#### Master Params Change Explanation

- **Justification**: In the newly added URDF structure, `base_footprint` acts as the root link representing the ground projection of the robot, while `base_link` (the physical chassis center) is its child. If FAST-LIO2's `body_frame` parameter remained `base_link` (publishing `odom -> base_link`), `base_link` would have multiple parents: `odom` (dynamic) and `base_footprint` (static from URDF). In ROS TF trees, multiple parents are illegal and break frame resolution. Changing the `body_frame` to `base_footprint` produces a valid and standard chain: `odom -> base_footprint -> base_link`.
- **Downstream Compatibility**: All downstream nodes (such as `route_server`, `gps_global_aligner`, `gps_route_runner`, `goal_manager`, and Nav2 costmap layers) that continue to query transforms to/from `base_link` are unaffected because the TF tree resolves the transform transparently.
- **Verification**: Height filtering in FAST-LIO2 (`publish_cloud_height_filter_enabled`) is based directly on the IMU state vector height, which is independent of the `body_frame` string configuration, keeping point cloud filtering fully correct.

A detailed breakdown of the parameter change is written below:


#### 1. Which files this parameter change affects
* **Parameter Source**: [master_params.yaml](/src/bringup/config/master_params.yaml) is the central parameter entry point.
* **LiDAR Odometry**: FAST-LIO2 ([lio_node.cpp](/src/perception/fastlio2/src/lio_node.cpp)) reads this parameter. It now publishes the dynamic coordinate transformation as `odom -> base_footprint` (previously `odom -> base_link`) and tags the topic outputs `/fastlio2/body_cloud`, `/fastlio2/body_filtered`, and `/fastlio2/lio_odom` under `base_footprint`.
* **Pointcloud to Laserscan**: The [pointcloud_to_laserscan_node](/src/bringup/launch/system_slam.launch.py#L64-L74) subscribes to `/fastlio2/body_cloud`. Since its `target_frame` parameter is empty (`""`), it inherits the frame ID of the input cloud. Thus, `/scan` is now published with `header.frame_id = base_footprint`.
* **Downstream Consumers**: Nodes subscribing to `/scan` (such as Nav2 costmaps and SLAM Toolbox) query TF transforms from `base_footprint` to their reference frames (`odom`/`map`).


#### 2. Whether it would potentially break any existing function
**No, it will not break any existing functionality.**
* **TF Connectivity**: Even though nodes like `route_server`, `gps_global_aligner_node.py`, and `gps_route_runner_node.py` query transforms relative to `base_link`, the TF tree remains continuous. Because `base_footprint` and `base_link` are connected via a static transform, TF dynamically and transparently resolves queries from `map`/`odom` to `base_link`.
* **2D Navigation/Alignment**: The translation between `base_footprint` and `base_link` in [base.urdf.xacro](file:///Ubuntu-22.04/home/yoonzh22/robotrepo/XJTLU-autonomous-vehicle-rtk/src/bringup/urdf/rosbot/base.urdf.xacro#L107-L111) is purely vertical ($Z \approx 22.9\text{ cm}$, with $X=0, Y=0, \text{yaw}=0$). Because 2D path planning, alignment, and navigation nodes only use the $XY$ coordinates and $\text{yaw}$ heading, they receive identical physical values.
* **Point Cloud Z-Filtering**: FAST-LIO2's point cloud height filtering (`publish_cloud_height_filter_enabled`) computes relative heights using the IMU state vector's estimated height `m_kf->x().t_wi.z()`, which is independent of the string configuration of `body_frame`. Point cloud filtering remains fully correct.


#### 3. How the new TF tree looks like, and if it is properly respected
The URDF is now successfully loaded by `robot_state_publisher` across all active launch modes. The resulting TF tree layout is:

- **Without Global Map (Basic / SLAM / Explore modes)**
```mermaid
graph TD
    odom[odom (FAST-LIO2 World Frame)] -->|Dynamic Transform| base_footprint[base_footprint (Ground Projection)]
    base_footprint -->|URDF Static: base_joint| base_link[base_link (Chassis Center)]
    base_link -->|URDF Static| laser_link[laser_link (LiDAR)]
    base_link -->|URDF Static| imu_link[imu_link (IMU)]
    base_link -->|URDF Static| camera_link[camera_link]
    base_link -->|URDF Static| wheels[Wheels & Casters]
```

- **With Global Map (GPS Navigation / PGO modes)**
```mermaid
graph TD
    map[map (Global Map Frame)] -->|Dynamic Transform (PGO)| odom[odom]
    odom -->|Dynamic Transform| base_footprint[base_footprint]
    base_footprint -->|URDF Static| base_link[base_link]
    base_link -->|URDF Static| links[laser_link / imu_link / camera_link / wheels]
```

This tree is fully respected by the ROS 2 environment because it forms a clean, single-parent tree from the root frame (`map` or `odom`) down to the leaves.

#### 4. Whether we should test for any edge case that may be problematic because of the change
* **Edge Case 1: Standalone Sensor/Odometry Tests**:
  If a developer runs FAST-LIO2 alone (e.g., `ros2 launch fastlio2 lio_no_rviz.py`) without launching the URDF node (`robot_description.launch.py`), the TF tree will stop at `base_footprint`. Any downstream node expecting `base_link` will time out. 
  *Recommendation*: Always run URDF publisher alongside FAST-LIO2 during manual tests if downstream modules are active.
* **Edge Case 2: Vertical Visualization Offset ("Floating Robot")**:
  FAST-LIO2 publishes `odom -> base_footprint` using the IMU's estimated position (which starts at height $Z=0$ in the state vector but is physically located at the LiDAR height $\approx 30\text{ cm}$ above the ground). 
  In the URDF, `base_link` is $22.9\text{ cm}$ above `base_footprint`, and the LiDAR is $7\text{ cm}$ above `base_link`.
  As a result:
  * In the physical world, the LiDAR is $\approx 30\text{ cm}$ above the ground.
  * In the TF tree, the LiDAR is $29.9\text{ cm}$ above `base_footprint` (which is at the IMU height).
  This causes a visual artifact where the robot's physical model (chassis/wheels) appears to float slightly above the point cloud ground points in RViz/Foxglove. This is purely visual and does not affect 2D navigation, but is a known artifact of using generic LIOs with custom URDF frames.

#### 5. Whether the change is justified, given the URDF was newly added in this branch
**Yes, the change is 100% justified and necessary.**
If `body_frame` had remained `base_link`:
1. `robot_state_publisher` would publish the static transform `base_footprint -> base_link`.
2. FAST-LIO2 would publish `odom -> base_link`.
3. This would mean `base_link` has multiple parent frames (`base_footprint` and `odom`), which is illegal in the ROS TF library. It would cause severe flickering, errors, and loss of TF tree integrity.

Making `base_footprint` the target of the dynamic odometry transform is the standard ROS convention for mobile robots, providing a clean, compliant coordinate tree.

### [RTK FGO Diagnostics and Heading Anchor Validation]

#### File

- `scripts/evaluate_rtk_fgo_bag.py`
- `scripts/test_rtk_fgo_metrics.py`
- `src/bringup/config/rtk_fgo.yaml`
- `src/perception/rtk_fgo_localizer/CMakeLists.txt`
- `src/perception/rtk_fgo_localizer/include/rtk_fgo_localizer/diagnostic_snapshot.hpp`
- `src/perception/rtk_fgo_localizer/include/rtk_fgo_localizer/frame_anchor.hpp`
- `src/perception/rtk_fgo_localizer/include/rtk_fgo_localizer/heading_stability.hpp`
- `src/perception/rtk_fgo_localizer/src/diagnostic_snapshot.cpp`
- `src/perception/rtk_fgo_localizer/src/frame_anchor.cpp`
- `src/perception/rtk_fgo_localizer/src/heading_stability.cpp`
- `src/perception/rtk_fgo_localizer/src/rtk_fgo_node.cpp`
- `src/perception/rtk_fgo_localizer/test/test_diagnostic_snapshot.cpp`
- `src/perception/rtk_fgo_localizer/test/test_frame_anchor.cpp`
- `src/perception/rtk_fgo_localizer/test/test_heading_stability.cpp`
- `docs-EN/knowledge/rtk_fgo.md`
- `docs-CN/knowledge/rtk_fgo.md`

#### Change

- Added structured `/rtk_fgo/factor_diagnostics` fields for RTK GGA quality, satellite count, HDOP, position and heading innovations, implied fix speed, and input ages.
- Extended `scripts/evaluate_rtk_fgo_bag.py` to summarize those diagnostic fields in replay JSON metrics.
- Added `FrameAnchor::enuYawToMapYaw()` and now rotate RTK heading measurements into the FGO `map` frame before heading innovation checks and yaw-factor insertion.
- Added wraparound-safe heading stability checks for frame-anchor bootstrap and moved `max_heading_spread_deg` under the `frame_anchor` parameter group.
- During the `frc` merge, aligned `rtk_fgo.yaml` and the mapping-session frame check defaults with the new `base_footprint` dynamic odometry frame.

#### Reason

- Replay of `reloc_enu_align_20260629_183500` showed that heading innovations were being computed between FAST-LIO/FGO map-frame yaw and raw RTK ENU yaw, while RTK positions were already rotated through the frame anchor.
- The same replay showed a transient dual-antenna heading jump during anchor bootstrap. The YAML already documented a heading-spread threshold, but the node did not read or enforce it.
- Existing replay metrics did not expose enough RTK quality, age, and innovation detail to distinguish satellite quality problems from frame-convention and bootstrap problems.

#### Effect

- Clean replay metrics improved from `3` strong candidates, `0` locked samples, and `419` heading rejects to `129` strong candidates, `102` locked samples, and `69` heading rejects.
- Position innovation after stable-heading bootstrap stayed bounded in the replay (`median 1.38 m`, `max 2.96 m`) instead of rising to the bad-anchor case (`median 16.25 m`, `max 25.84 m`).
- FGO remains shadow-only by default; the replay result supports further field validation but is not yet a reason to let FGO own production `map -> odom`.
- The `base_footprint` merge adaptation keeps future experimental FGO TF publication compatible with the URDF single-parent TF tree and keeps map-saving checks consistent with FAST-LIO2's new child frame.

### [Restored Travel Prior Point-Cloud Map Navigation Chain]

#### File

- `docs/superpowers/plans/2026-07-02-prior-pointcloud-travel-navigation.md`
- `scripts/launch_with_logs.sh`
- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/test/test_system_travel_launch.py`
- `src/perception/localizer/launch/localizer_launch.py`
- `src/perception/localizer/src/localizer_node.cpp`
- `src/perception/localizer/test/test_localizer_contract.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/known_issues.md`
- `docs-EN/known_issues.md`

#### Change

- Restored `travel` as an experimental prior-map navigation chain: `map_yaml` is injected into Nav2 `map_server`, and `pcd_map` is passed to `localizer` for startup PCD loading.
- `localizer` can now load a PCD map at startup while keeping `/localizer/relocalize` for field map reloads and initial-pose updates.
- `nav2_travel.yaml` now lets `localizer` own `map -> odom`, sets AMCL `tf_broadcast=false`, restores the global costmap `static_layer`, and disables global rolling window mode.
- `launch_with_logs.sh` now forwards extra launch arguments and lets `travel` honor `FYP_USE_RVIZ`.
- During the `frc` merge, kept the URDF launch in Travel and aligned the Travel TF chain with `odom -> base_footprint -> base_link`.

#### Reason

- Standard Nav2 prior-map navigation uses a 2D OccupancyGrid for global planning, but this use case also needs prior PCD relocalization at startup/runtime; both assets must be wired into the same `travel` chain.
- `map -> odom` must have exactly one publisher. Travel mode must prevent AMCL, PGO, and `localizer` from competing for that TF.
- The old `travel` config left `map_server.yaml_filename` empty, and `localizer` existed only as a standalone tool rather than a system launch component.

#### Effect

- Operators can start the prior-map chain with `bash scripts/launch_with_logs.sh travel map_yaml:=... pcd_map:=...`.
- Runtime TF convention is `localizer: map -> odom`, `FAST-LIO2: odom -> base_footprint`, and URDF static `base_footprint -> base_link`; PGO is off by default or runs only with `publish_tf=false` for visualization/map-saving support.
- Local source-contract tests and the Jetson ROS 2 Humble build for `localizer`/`bringup` pass; indoor prior-map vehicle navigation still needs further validation.

### [Completed Travel Runtime Cleanup for localizer and ros2 launch]

#### File

- `Makefile`
- `scripts/launch_with_logs.sh`
- `src/bringup/test/test_system_travel_launch.py`

#### Change

- Added `[r]os2 launch` and `[l]ocalizer_node` to the `make kill-runtime` cleanup pattern.
- Added the same `[r]os2 launch` and `[l]ocalizer_node` coverage to `scripts/launch_with_logs.sh` startup/exit cleanup.
- Added a regression test requiring travel runtime cleanup to include direct `ros2 launch` processes and `localizer_node`.

#### Reason

- Field travel testing showed old `system_slam.launch.py`, old `system_travel.launch.py`, and multiple `/localizer/localizer_node` processes could remain alive together.
- Stale localizers make the ROS graph, services, and TF tree ambiguous; Nav2 controller logs showed missing `base_link -> odom` TF, after which the navigation lifecycle shut down and RViz goals did not move the vehicle.

#### Effect

- Future `make kill-runtime` and `scripts/launch_with_logs.sh` startup cleanup now cover the travel localizer and direct ROS launch parent processes.
- Reduces repeated-node, stale-TF-publisher, and non-executing Nav2-goal failures after switching between slam/travel in field tests.

### [Gated Travel localizer TF Until Valid Relocalization]

#### File

- `src/perception/localizer/src/localizer_node.cpp`
- `src/perception/localizer/config/localizer.yaml`
- `src/perception/localizer/test/test_localizer_contract.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change

- `localizer` no longer broadcasts `map -> odom` immediately after startup PCD preload; `/localizer/relocalize` must succeed first.
- Removed repeated TF broadcasts from the timer non-update branch using the same `last_message_time`.
- Added `max_tf_input_age_s`, skip obviously stale input stamps, and track the last published TF stamp to avoid duplicate or backward `map -> odom` transforms.
- Updated Travel run documentation to verify `odom -> base_footprint` first, relocalize next, then check `map -> odom`.

#### Reason

- Field logs showed `TF_OLD_DATA ignoring data from the past for frame odom at time 1782993113.484190`, while `tf2_echo odom base_link` showed FAST-LIO had recovered to small meter-scale motion.
- Root cause: `localizer` could publish `map -> odom` before validated relocalization, and its 10 ms timer repeatedly rebroadcast the same old stamp during the 1 Hz ICP update interval, polluting Nav2's TF buffer.

#### Effect

- Travel startup no longer injects unvalidated identity or stale-stamped `map -> odom` into TF.
- The required operating order is stricter: start travel -> verify FAST-LIO `odom -> base_footprint` -> call `/localizer/relocalize` -> wait for `relocalize_check` true -> send Nav2 goals.

### [RTK Corridor Low-Speed Acceptance Profile and Diagnostic Bag Additions]

#### File

- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/package.xml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added a `nav2_params_file` launch argument to `system_explore.launch.py` so parent modes can override the Nav2 parameter source.
- `system_gps_corridor.launch.py` now generates a temporary Nav2 parameter file from `nav2_explore.yaml` at launch time and injects an RTK corridor low-speed acceptance profile: `vx_max=0.45`, `wz_max=0.65`, `controller_frequency=20.0`, `batch_size=500`, with matching velocity smoother limits.
- Corridor rosbag recording now includes `/livox/lidar`, `/livox/imu`, and `/fastlio2/body_cloud`.
- Added launch contract tests to keep the corridor profile overrides and diagnostic bag topics from being removed accidentally.

#### Reason

- In the 2026-07-06 outdoor RTK corridor rosbag, `/rtk/status` stayed at `RTK Fixed q=4`, so RTK quality was not the first suspect.
- The same bag showed rightward trajectory drift and a `/fastlio2/lio_odom` jump of about `1.18m/0.19s`; first-pass acceptance needs lower linear and angular speeds so LIO degeneration and controller tuning are not conflated.
- The 2026-07-06 20:06 field log showed `controller_frequency=15Hz` makes MPPI fail configuration because the control period is larger than `model_dt=0.05s`; Nav2 lifecycle then aborts and the `navigate_to_pose` action server is unavailable.
- The original bag did not include raw Livox point cloud, IMU, or body cloud topics, making localization-degeneration replay difficult.

#### Effect

- `corridor` mode keeps the Explore MPPI structure but defaults to more conservative RTK low-speed acceptance parameters.
- Future outdoor bags include RTK, FAST-LIO2 odom, raw Livox inputs, and body cloud, making it easier to separate right drift caused by RTK, LIO, or Nav2 control.
- Explore mainline speed parameters are unchanged; the profile is injected only by `system_gps_corridor.launch.py`.

### [RTK Corridor Waits for Nav2 Lifecycle Active Before Route Goals]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/nav2_lifecycle_ready.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_nav2_lifecycle_ready.py`
- `src/navigation/gps_waypoint_dispatcher/package.xml`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change

- `gps_route_runner` now waits for both the `navigate_to_pose` action server and the core Nav2 lifecycle nodes (`controller_server`, `planner_server`, `behavior_server`, `bt_navigator`) to report `active`.
- Added a small lifecycle readiness helper plus regression tests for the case where `bt_navigator` is not active yet.
- Added `lifecycle_msgs` as an explicit package dependency.

#### Reason

- The 2026-07-06 20:14 field log showed `gps_route_runner` sending the first corridor subgoal at `1783340067.215`, while `bt_navigator` only started activating at `1783340067.248`.
- The same bag had `/plan` count `0` and `/cmd_vel` count `0`, confirming the failure happened before global planning or control.
- Waiting only for the action server was too weak because the server could be discoverable before the Nav2 lifecycle activation sequence completed.

#### Effect

- Corridor route goals are no longer sent during the final Nav2 lifecycle activation window.
- A failed first waypoint caused by this startup race should now turn into a short additional wait at `WAITING_FOR_NAV2` instead of `FAILED_WAYPOINT_*_STATUS_6`.

### [RTK Corridor Aborts on Map/GPS Divergence Instead of False Success]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/bringup/config/master_params.yaml`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change

- Added a route safety check that projects the current RTK `/fix` through the active ENU-to-map alignment and compares it with `map -> base_link`.
- Added `map_gps_divergence_warn_m=2.0` and `map_gps_divergence_abort_m=5.0` parameters.
- If divergence exceeds the abort threshold, `gps_route_runner` publishes `MAP_GPS_DIVERGENCE_ABORT|...`, sends zero `/cmd_vel`, and exits instead of marking the waypoint reached.

#### Reason

- The 2026-07-06 20:26 log showed the runner reporting `WAYPOINT_REACHED` without publishing `/gps_corridor/goal_map`, `/gps_corridor/path_map`, `/plan`, or `/cmd_vel`.
- The route itself was valid: `start_ref -> waypoint` was about `13.69m`, and startup RTK was only about `0.91m` from `start_ref`.
- The actual failure was FAST-LIO/TF divergence: latest `odom -> base_footprint` was about `(582.57, -870.68, -2667.62)` while RTK still placed the vehicle near the route start, so projection-based progress was clamped to the segment end.

#### Effect

- Corridor mode no longer reports false success when localization has diverged far from RTK.
- The next field run should fail loudly with a map/GPS divergence status if FAST-LIO blows up again, which separates localization failure from route-runner or Nav2 behavior.

### [RTK Corridor Lean Bag and Livox Packet Log Gate]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/sensor_drivers/livox_ros_driver2/src/lddc.cpp`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Corridor bag recording now defaults to a lean profile containing RTK, FAST-LIO2 odom, TF, corridor status, goals, costmaps, `/cmd_vel`, and `/plan`.
- Raw `/livox/lidar`, `/livox/imu`, and `/fastlio2/body_cloud` recording moved behind `FYP_CORRIDOR_BAG_PROFILE=debug`.
- Livox packet-scale console and CSV logging is now guarded by `LIVOX_VERBOSE_PACKET_LOGS`; missing or unreadable `log_switch.yaml` no longer enables packet logging by default.
- Added launch/source contract tests for the lean bag profile and Livox packet-log gate.

#### Reason

- The 2026-07-06 20:40 corridor run reached Nav2 and published `/cmd_vel`, but aborted on odom watchdog jumps around `0.79m/0.017s` and `1.10m/0.01s`.
- The same run showed Nav2 control-loop overruns, BT tick-rate overruns, transform-cache drops, and tegrastats with four active CPU cores near 99-100%.
- Even with `runtime-data/config/log_switch.yaml` setting `livox_ros_driver2.enable_logging: false`, `launch_stdout.log` still contained per-packet `Publishing IMU data`, point-cloud dumps, and `Pointcloud log file is not open!` spam.
- Recording raw Livox streams during acceptance runs added avoidable I/O and serialization load exactly when FAST-LIO2 and Nav2 needed timing headroom.

#### Effect

- Normal corridor acceptance runs keep the same safety and diagnostic visibility while reducing stdout, bagging, and disk pressure on the Jetson.
- Full raw Livox replay remains available by explicitly launching with `FYP_CORRIDOR_BAG_PROFILE=debug`; packet console/CSV logging remains available for short bench tests with `LIVOX_VERBOSE_PACKET_LOGS=1`.
- Future CPU-saturation regressions should be caught by the corridor launch contract test before field testing.

### [RTK Corridor Fresh TF Gate and FAST-LIO2 Low-IMU Drop]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/perception/fastlio2/src/map_builder/commons.h`
- `src/perception/pgo_gps_fusion/src/pgo_node.cpp`
- `src/bringup/config/master_params.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/knowledge/fastlio2.md`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change

- Added `tf_pose_max_age_s=0.75` to `gps_route_runner`; stale `map -> base_link` transforms are ignored before route start, subgoal generation, and odom-watchdog checks.
- Added a pure `summarize_tf_freshness()` helper and regression tests so old TF samples cannot be treated as current robot pose.
- Added `min_imu_samples_per_lidar=3` to FAST-LIO2; synchronized LiDAR packages with fewer IMU samples are dropped before `m_builder->process()`.
- `pgo_gps_fusion` now defaults to quiet when `log_switch.yaml` lacks `pgo_node`, hard-coded `[DIAG]` stderr prints are gated behind `PGO_VERBOSE_DIAG=1`, and per-frame synced-cloud INFO logging is throttled.

#### Reason

- The 2026-07-06 21:02 corridor bag showed `/plan=0` and only 4 `/cmd_vel` messages. Nav2 reported `worldToMap failed` and `The goal sent to the planner is off the global costmap`.
- At the same moment, RTK projected into map was still around `(0,0)`, while FAST-LIO2/TF had drifted to about `(-50,-20)` before the route goal was sent.
- FAST-LIO2 processed under-constrained packages such as `0 IMU samples, 925 LIDAR points` and `1 IMU samples, 1086 LIDAR points` while the chassis serial feedback still reported zero velocity.
- The route runner could accept an old TF sample during startup waiting, then see a tens-of-meters TF jump only after sending the Nav2 goal.

#### Effect

- Corridor mode should now fail before goal dispatch when only stale TF is available or when live TF diverges from RTK, instead of handing Nav2 a goal outside the rolling global costmap.
- FAST-LIO2 no longer advances the filter on LiDAR frames with too little IMU support, reducing stationary odom blow-ups caused by bad LiDAR/IMU sync windows.
- PGO diagnostic output remains available for bench debugging with `PGO_VERBOSE_DIAG=1` but no longer floods field-run logs by default.

### [RTK Corridor TF-Stale Grace Window]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/bringup/config/master_params.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change

- Raised corridor `tf_pose_max_age_s` from `0.75` to `3.0`.
- Added `odom_watchdog_tf_stale_abort_s=3.0`; TF stale watchdog cancellation now requires both enough stale samples and enough continuous stale duration.
- Added `summarize_tf_watchdog_gap()` tests so short FAST-LIO2 TF gaps do not immediately cancel a freshly accepted Nav2 goal.
- Moved FAST-LIO2's "Processing sync package" log after the low-IMU drop guard so dropped packages are not reported as processed.

#### Reason

- The 2026-07-06 21:27 corridor run on commit `1b895e9` no longer showed the old off-costmap start pose: Nav2 began from `(0.02, -0.01)` and produced `/plan=1`.
- The route still failed because the runner canceled the goal after about `0.29s` with `ODOM_DIVERGENCE_ABORT|...|TF_STALE`.
- FAST-LIO2 logs showed short LiDAR/IMU sync gaps and dropped packages during startup, so a `0.75s` freshness window plus count-only TF stale abort was too aggressive for this field condition.

#### Effect

- Corridor mode keeps the stale-TF safety gate but should no longer abort immediately during brief FAST-LIO2 TF update gaps right after goal dispatch.
- If TF remains stale for several seconds, the watchdog still cancels Nav2 and publishes zero `/cmd_vel`.
- Field logs now distinguish genuinely processed FAST-LIO2 packages from low-IMU packages that were dropped before filtering.

### [FAST-LIO2 Rejects IMU-Only Prediction Without LiDAR Correction]

#### File

- `src/perception/fastlio2/src/map_builder/ieskf.h`
- `src/perception/fastlio2/src/map_builder/ieskf.cpp`
- `src/perception/fastlio2/src/map_builder/lidar_processor.h`
- `src/perception/fastlio2/src/map_builder/lidar_processor.cpp`
- `src/perception/fastlio2/src/map_builder/map_builder.h`
- `src/perception/fastlio2/src/map_builder/map_builder.cpp`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/knowledge/fastlio2.md`

#### Change

- `IESKF::update()` now returns whether the frame had a valid LiDAR measurement update.
- `LidarProcessor::process()` skips incremental map insertion when the update is invalid.
- `MapBuilder::process()` restores the pre-prediction state and covariance when a mapping-frame LiDAR update is invalid.
- `lio_node` skips TF/odom/cloud publication for rejected frames and logs a throttled warning after mapping has started.

#### Reason

- The 2026-07-06 21:40 corridor run on commit `269c308` was safely stopped before route goal dispatch, but FAST-LIO2 had already drifted from near zero to about `(-503, -88, -306)` while the vehicle had not received nonzero `/cmd_vel`.
- Logs showed repeated `NO Effective Points` and sparse valid LiDAR corrections. The old pipeline kept IMU prediction even when LiDAR correction was invalid, then exposed that predicted state as normal TF/odom.
- Route safety correctly aborted with `MAP_GPS_DIVERGENCE_ABORT`, but the underlying LIO output still needed to stop publishing IMU-only drift as localization.

#### Effect

- During low-feature or poorly synchronized startup, FAST-LIO2 no longer advances the published TF/odom stream using IMU-only prediction when LiDAR correction is invalid.
- Corridor route start should now wait for a genuinely corrected `map -> base_link` pose instead of receiving a physically impossible FAST-LIO2 drift pose.

### [RTK Corridor Nav2 Success Verification]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Corridor's generated Nav2 parameter file now forces `general_goal_checker.stateful=false`.
- `gps_route_runner` now verifies physical route progress after a Nav2 `SUCCEEDED` result; if progress is still far from the subgoal, it publishes `NAV2_FALSE_SUCCESS_ABORT`, sends zero `/cmd_vel`, and stops the route.
- Added regression/source-contract tests for both the Nav2 rewrite and the route-runner success guard.

#### Reason

- The 2026-07-06 21:51 corridor run on commit `2880423` reached route execution and issued the first 14m subgoal, but Nav2 repeatedly logged `Reached the goal!` while the next `Begin navigating from current location` still showed the robot near the start.
- The repeated false success caused the runner to resend the same subgoal several times and eventually exposed unstable controller commands and an odom watchdog abort.

#### Effect

- A stale/stateful Nav2 goal-checker latch can no longer make far-away corridor subgoals complete immediately.
- If Nav2 still reports success without actual route progress, corridor mode fails safely with an explicit `NAV2_FALSE_SUCCESS_ABORT` status instead of entering a resend loop.

### [RTK Corridor Terminal Stop Hold and 0.65m/s Step-Up]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/config/master_params.yaml`
- `scripts/monitor_corridor_status.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-EN/commands.md`
- `docs-CN/commands.md`
- `docs-EN/knowledge/gps_planning.md`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/nav2_tuning.md`
- `docs-CN/knowledge/nav2_tuning.md`

#### Change

- Added `terminal_stop_hold_s=1.2` and `terminal_stop_publish_hz=20.0` to `gps_route_runner`.
- Before final `SUCCEEDED`, the runner now publishes `STOPPING_BEFORE_EXIT`, sends zero `/cmd_vel` for the configured hold, then publishes `SUCCEEDED`; selected terminal failure paths also publish the stop hold before the terminal status.
- The quiet monitor now prints the stop-hold status instead of treating the pause as a hang.
- Raised the corridor linear profile from `vx_max=0.55` to `0.65`, with `vx_std=0.18`, `ax_max=0.70`, `ax_min=-1.2`, and velocity smoother limits `[0.65, 0.0, 0.50]`, `[0.70, 0.0, 0.9]`, `[-1.2, 0.0, -1.0]`; angular limits remain conservative.

#### Reason

- The successful 2026-07-07 10:39 corridor bag stayed RTK Fixed for all `/rtk/status` samples and saturated the old `0.55m/s` speed ceiling for most of the route, while yaw commands were already smooth (`|wz|` p95 about `0.094rad/s`).
- The same bag showed Nav2 declaring success while the final forward command was still decaying (`0.073m/s` 33ms before success and `0.028m/s` 17ms after success), then quiet launch exited after only a short zero-command tail.
- Because `monitor_corridor_status.py` exits as soon as it sees `SUCCEEDED`, any stop hold after the success status would be killed too early; the hold must therefore run before publishing the terminal success status.

#### Effect

- The next corridor run should leave an obvious 1.2s zero-speed tail in `/cmd_vel` and `serial_twistctl` before the launch wrapper tears down runtime nodes.
- The vehicle gets a modest straight-line speed increase while keeping the smoother yaw behavior from the 10:39 successful run.
- The change remains isolated to corridor mode's generated Nav2 profile and `gps_route_runner` terminal behavior; Explore, Travel, and nav-gps parameter files are not retuned by this entry.

### [RTK Corridor PGO TF Owner Double Disable]

#### File

- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/config/pgo_corridor_no_tf.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change

- Added a `pgo_config_file` argument to Explore launch and forwarded it to PGO launch as `pgo_config`.
- Corridor launch now passes both the new legacy flat config `pgo_corridor_no_tf.yaml` and the existing ROS parameter override `pgo_corridor_no_gps.yaml` to PGO.
- Added `pgo_corridor_no_tf.yaml` with explicit `publish_tf: false` and `gps.enable: false`, keeping `map -> odom` ownership with `rtk_map_odom_corrector` in corridor mode.
- Extended the corridor launch test to cover both PGO override paths: legacy config and extra ROS params.

#### Reason

- The 2026-07-07 17:13, 17:19, 17:23, and 17:30 corridor bags showed mostly or fully RTK-fixed data and smooth `/fastlio2/lio_odom`, while `/tf` contained millisecond-scale conflicting `map -> odom` jumps.
- In the 17:30 session, every `/rtk/status` sample was `q=4` and `/fastlio2/lio_odom` had no jump above 0.5m, but `/tf` had 44 conflicting `map -> odom` jumps above 0.15m within 30ms, with a maximum of about 0.64m.
- Those conflicting global transforms make Nav2 see `map -> base_link` jump back and forth, which can trigger planner failures or the route-runner odom watchdog.

#### Effect

- In corridor mode, PGO should no longer compete with RTK authority for `map -> odom`.
- If the same TF conflict still appears on the Jetson, first check whether the workspace was rebuilt/re-sourced or whether another stale node is still publishing `map -> odom`.

### [RTK Corridor Degraded map->odom Hold]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change

- Added degraded-hold behavior to `rtk_map_odom_corrector`: when input quality, RTK fixed state, invalid fix/heading, or target/yaw-jump gating rejects the latest input, the node freezes the global pose update but keeps rebroadcasting the last trusted `map -> odom`.
- Added a source-contract regression test covering the four degraded exit paths that must rebroadcast the last trusted TF.
- Documented the RTK degraded-hold semantics in the architecture docs.

#### Reason

- The 2026-07-08 15:42 corridor run reached route execution, issued the first goal at `(x=7.39, y=13.72)`, then `gps_route_runner` reported `ODOM_DIVERGENCE_ABORT|TF_STALE_3.00s` and ended as `FAILED_WAYPOINT_1_SUBGOAL_1_STATUS_6`.
- In the bag, `odom -> base_link` kept publishing, but `map -> odom` stopped refreshing after RTK/yaw was rejected by `TARGET_YAW_JUMP` gating, and the Nav2 controller saw map/odom extrapolation failures.

#### Effect

- When RTK or heading is briefly untrusted, corridor mode does not commit the bad sample into the global pose and also does not let `map -> odom` expire.
- Nav2 can keep local control through short RTK-degraded windows using a continuous TF tree; if the vehicle physically does not move, progress/odom watchdogs can still fail later with the more accurate cause.

### [RTK Corridor Nav2 Success Shortfall Tolerance]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change

- Added a `nav2_success_shortfall_tolerance_m` route parameter, defaulting to `max(0.75, waypoint_xy_tolerance_m)`.
- The post-Nav2-`SUCCEEDED` route-progress check now uses the independent shortfall tolerance; failure status text also includes `tolerance=...` for field diagnosis.
- Added pure logic tests covering the accepted `shortfall=0.48m` case and the still-rejected `shortfall=13.71m` no-progress case.

#### Reason

- The 2026-07-08 16:08 corridor run reached waypoint 1/6, then reached `15.29m / 15.77m` progress toward waypoint 2/6, leaving only `0.48m` shortfall, but the old guard reused `waypoint_xy_tolerance_m=0.35m` and reported `NAV2_FALSE_SUCCESS_ABORT`.
- That guard was introduced to reject no-progress false successes with `progress=0.00m` and meter-scale shortfall; using the same 0.35m threshold for along-track projection shortfall is stricter than Nav2's circular goal checker plus small RTK/aligner jumps.

#### Effect

- A normal near-goal Nav2 success is no longer killed by sub-0.5m along-track projection shortfall, allowing the route to continue to later waypoints.
- Real no-progress or meter-scale-shortfall false successes are still stopped by `NAV2_FALSE_SUCCESS_ABORT`.

### [RTK Corridor In-Place Right-Angle Turn Profile]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- When corridor generates the temporary Nav2 params, the progress checker is relaxed from the default `0.15m / 10s` to `0.10m / 15s` so short in-place heading alignment at right-angle turns is not treated as immediate no-progress.
- The corridor MPPI profile changes from `vx_max=0.65`, `wz_max=0.50`, `az_max=1.0` to `vx_max=0.75`, `wz_max=0.90`, and `az_max=2.6`.
- Sampling standard deviations change to `vx_std=0.20` and `wz_std=0.24`, making effective in-place turn trajectories easier for MPPI to sample.
- Velocity smoother limits are aligned to `[0.75, 0.0, 0.90]`, `[0.75, 0.0, 2.0]`, and `[-1.2, 0.0, -2.2]`.

#### Reason

- In the 2026-07-09 11:14 and 11:16 route `2` runs, the second leg after the first reached subgoal required an almost 90-degree heading change; for 10s Nav2 commanded almost only slow in-place rotation, with `/cmd_vel` and `serial_twistctl` averaging about `vcx≈0.001m/s` and `wc≈0.13rad/s`.
- In the same window `/fastlio2/lio_odom` moved only about `0.10-0.14m`, then `controller_server` reported `Failed to make progress`.
- The vehicle mechanically uses 90-degree omnidirectional wheels, but the current host-to-STM32 command contract still exposes only `vcx,wc` and no lateral `vcy`; this change is therefore not lateral cornering, but stronger in-place heading alignment through the existing `vcx,wc` chain.

#### Effect

- At right-angle turns MPPI is more likely to select larger `wc` in-place heading commands, and the progress checker no longer fails before a reasonable turn window elapses.
- Straight-line speed steps from `0.65m/s` to `0.75m/s`, while linear acceleration stays moderate instead of returning to the Explore `1.0m/s` aggressive profile.
- The change remains isolated to corridor's generated Nav2 profile; Explore, Travel, nav-gps, and the STM32 protocol are not changed by this entry.

### [RTK Corridor MPPI Yaw Saturation Rollback]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Kept `progress_checker=0.10m / 15.0s` so right-angle turns can still spend a short window aligning heading in place.
- Rolled the corridor MPPI profile back from `vx_max=0.75`, `wz_max=0.90`, `az_max=2.6` to `vx_max=0.70`, `wz_max=0.70`, `az_max=1.8`.
- Reduced sampling standard deviations from `vx_std=0.20`, `wz_std=0.24` to `vx_std=0.18`, `wz_std=0.16`.
- Aligned velocity smoother limits to `[0.70, 0.0, 0.70]`, `[0.70, 0.0, 1.4]`, and `[-1.2, 0.0, -1.8]`.

#### Reason

- The 2026-07-09 13:27 two-point route completed, but its second leg already showed `/cmd_vel` angular saturation near `0.90rad/s` and sign changes; completion alone did not mean the profile was smooth.
- In the 2026-07-09 13:37 and 13:40 failure windows, RTK stayed fixed and neither CPU load nor `map -> odom` jumps looked like the primary cause; MPPI kept commanding large angular/forward motion while RTK and `/fastlio2/lio_odom` showed little actual vehicle displacement or heading change.
- The previous profile moved from "too weak to turn" to the opposite edge where angular velocity saturated too easily; increasing yaw authority further would make slip, lower-controller limiting, or local oscillation more likely.

#### Effect

- Corridor keeps stronger in-place heading authority than the older `wz_max=0.50` profile, but MPPI should no longer live near the `0.90rad/s` angular limit.
- The next field test should check whether right-angle turns still complete and whether `/cmd_vel`, `serial_twistctl`, and RTK heading still show "large command but little motion" mismatch.
- The change remains isolated to corridor's generated Nav2 profile; Explore, Travel, nav-gps, and the chassis protocol are unchanged.

### [RTK Corrector Long-Range Yaw Lever Limit]

#### File

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `src/bringup/config/master_params.yaml`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added `limit_map_to_odom_step_for_base()`, which limits `map->odom` translation and yaw angle as before, but also bounds the equivalent `map->base_link` translation caused by yaw release using the current `odom->base_link` radius.
- Added a `max_base_yaw_step_m` parameter to `rtk_map_odom_corrector`; the corridor default is `0.12m`.
- Added a pure-logic regression test for the case where an `odom->base` radius near `67m` turns a fixed `0.5deg` yaw step into about `0.58m` of `map->base` lateral jump.

#### Reason

- The 2026-07-09 14:00 corridor run reached waypoint `6/7`, then route runner reported `ODOM_DIVERGENCE_ABORT|6|1|STEP_1.18m_IN_0.01s`.
- Offline bag analysis showed RTK stayed `q=4`; RTK max step was about `0.17m/0.20s`, FAST-LIO2 max step was about `0.22m`, and standalone `map->odom` translation steps were about `0.20m`, but the composed `map->base_link` TF still had jumps up to `0.79m/0.008s`.
- The root cause is that `map->odom` yaw is released around the odom origin; when the vehicle is about `67m` from that origin, a fixed `0.5deg` yaw step creates about `67 * sin(0.5deg) ≈ 0.58m` of base translation through lever-arm amplification, enough to trip the route-runner odom watchdog.

#### Effect

- RTK yaw reacquisition can still progress instead of freezing `map->odom` for a long time.
- As the vehicle moves farther from the odom origin, the per-cycle yaw release automatically becomes smaller, preventing Nav2 from seeing meter-scale `map->base_link` jumps caused by TF composition.
- If the odom watchdog still triggers in the next field run, first check for `map->odom` translation target jumps, FAST-LIO2 pose jumps, or `/odom_CBoar` serial feedback parsing anomalies.

### [RTK Corridor MPPI Straight-Line Speedup And Yaw Oscillation Suppression]

#### File

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Raised the corridor straight-line profile from `vx_max=0.70` to `vx_max=0.85`, with matching `ax_max=0.85` and velocity smoother linear limits.
- Kept `wz_max=0.70`, but slightly reduced `wz_std` from `0.16` to `0.15` and lowered `az_max` from `1.8` to `1.4`.
- Raised MPPI `temperature` from `0.3` to `0.45` and enabled `regenerate_noises=true`.
- Aligned the baseline `nav2_corridor_rtk.yaml` values with the corridor launch-time override so the checked-in YAML matches the runtime profile.

#### Reason

- The 2026-07-09 14:23 seven-waypoint run completed, but the rosbag showed `/cmd_vel` linear p95 already pinned at `0.70m/s`, with about 63% of moving samples near the cap. The slow feel was therefore mostly a corridor profile limit.
- In the same bag `abs(wz)` p95 was about `0.64rad/s`, with yaw sign changes around `0.53/s`; jumping straight back to `1.0m/s` or increasing angular authority would amplify the observed weave.
- RTK stayed fixed and Jetson CPU still had headroom. `regenerate_noises` reduces persistent bias from fixed sampled noise, while `temperature=0.45` makes MPPI selection less winner-take-all.

#### Effect

- The next field run should raise straight-line speed from the `0.70m/s` cap to `0.85m/s`, while keeping angular speed and angular acceleration conservative enough to avoid returning to yaw saturation.
- The change remains isolated to the corridor RTK profile; Explore, Travel, nav-gps, and the chassis protocol are unchanged.
- If the next bag still shows visible weaving, analyze `map->odom` / `map->base_link` smoothing before lowering `wz_max` further.

### [RTK nav-gps Route-Graph Destination Mode Uses Corridor Stable Chain]

#### File

- `src/bringup/launch/system_nav_gps.launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `scripts/build_scene_runtime.py`
- `scripts/launch_with_logs.sh`
- `src/bringup/test/test_system_nav_gps_launch.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- `nav-gps` no longer starts the old Livox / PGO / Nav2 / DWB stack directly. It reuses `system_explore.launch.py` while passing the corridor no-TF/no-GPS PGO configuration, the corridor RTK MPPI profile, and no-physical-recovery BTs.
- `rtk_map_odom_corrector` now supports `scene_points_file` and `use_scene_identity_alignment`, allowing it to load the fixed origin from `scene_points.yaml` and use fixed ENU-to-map identity alignment for scene route graphs.
- `build_scene_runtime.py` writes `rtk_map_odom_corrector` scene origin, `scene_points_file`, `alignment_topic=/gps_scene/enu_to_map`, and `use_scene_identity_alignment=true` into `master_params_scene.yaml`.
- `nav-gps` starts the RTK FGO shadow node by default, still with `publish_tf=false` and `nav2_use_fgo=false`; the default lean bag records RTK, FAST-LIO2, chassis, FGO, TF, goal/costmap, and `/cmd_vel` evidence.
- `launch_with_logs.sh` now passes `FYP_USE_RVIZ` through to `nav-gps`, making headless vehicle launches straightforward.

#### Reason

- `corridor` has already validated that RTK-authoritative `map->odom`, the MPPI profile, the dedicated high-window obstacle cloud, and map->odom smoothing are better suited to the current outdoor vehicle than the old PGO/LIO-owned global TF chain.
- The user wants to move from recorded waypoint routes to QGIS/GPS route-graph "choose a destination" navigation; keeping the old `nav2_gps.yaml` DWB + PGO `map->odom` path would reintroduce known problems into `nav-gps`.
- Scene route-graph coordinates are already defined by the fixed ENU origin, so RTK authority must not redefine `map` from the startup pose as corridor bootstrap can; it must use identity alignment from the scene fixed origin.

#### Effect

- `nav-gps` is now the RTK route-graph destination entry point: `gps_anchor_localizer` owns `NAV_READY` and anchors, `route_server` owns graph paths, and `rtk_map_odom_corrector` owns the only production `map->odom`.
- After importing or modifying a scene / QGIS map, rerun `python3 scripts/build_scene_runtime.py`; old runtime scenes will not contain the new RTK authority scene parameters.
- The old `nav2_gps.yaml` remains as the historical GPS MVP profile, but vehicle tuning should now inspect the corridor RTK MPPI profile and `system_nav_gps.launch.py` launch-time overrides.

### [QGIS GPS Map Compilation And Pose-Start Route Graph]

#### File

- `scripts/compile_qgis_scene.py`
- `scripts/test_compile_qgis_scene.py`
- `scripts/build_scene_runtime.py`
- `scripts/nav_gps_menu.py`
- `src/bringup/launch/system_nav_gps.launch.py`
- `src/bringup/test/test_system_nav_gps_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/scene_runtime.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/goal_manager_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_goal_manager_route_graph.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_scene_projection_fallback.py`
- `src/sensor_drivers/gnss/gnss_calibration/gnss_calibration/gps_anchor_localizer_node.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Added a QGIS GeoJSON route-graph compiler that turns `feature_type=route` LineStrings into `scene_gps_bundle.yaml` and densifies route-graph edges at the default `5m` spacing.
- The compiler writes only the fixed origin, endpoints, and junctions as sparse anchors; the reachable road network itself is represented by the densified route graph nodes and edges, avoiding one anchor per route point.
- The compiler generates `destinations` from named buildings near the route graph, route endpoints, and junctions so the `nav-gps` menu can select a destination.
- `route_server` now enables nearest-node search in `nav-gps`; the goal manager sends `ComputeRoute(use_poses=true)` from the current `map` pose, letting the route server snap the vehicle to the nearest traversable graph node.
- `nav_gps_menu.py` also watches RTK authoritative localization status and only checks `compute_route` / `follow_path`; the vehicle `nav-gps` entry point no longer needs the old anchor `NAV_READY` gate or `navigate_to_pose` readiness before sending a destination.
- `build_scene_runtime.py`, `scene_runtime.py`, and `gps_anchor_localizer_node.py` now have a local-ENU fallback for `pyproj`; when `pyproj` is installed, they still use the original exact projection path.

#### Reason

- The user wants to use the recorded QGIS GPS map directly and pick a destination like a navigation app, instead of only replaying a manually collected waypoint sequence.
- The old anchor model works for a small set of named points, but it is unnatural for a dense road network to require the vehicle to start near one of those anchors; the practical behavior should be "if the vehicle is near the road graph, enter from the nearest segment."
- Raw QGIS route points can be too far apart; without densification, A* / route-server output can contain long graph edges that Nav2 tracks as oversized straight segments.
- Some local / analysis machines may not have `python3-pyproj` installed; if scene compilation or node imports fail on that missing dependency, map navigation testing is blocked before the actual route logic runs.

#### Effect

- The current `qgis_4_package/3.geojson` compiles into about `557` route graph nodes, `558` edges, a maximum edge length of about `4.99m`, and `12` selectable destinations.
- `nav-gps` can plan from any vehicle pose close to the route graph, rather than relying on a small anchor set for startup; anchors now mainly provide diagnostics and scene semantics.
- On the vehicle, compile the QGIS map first, then run `python3 scripts/build_scene_runtime.py`; after that use `make launch-nav-gps` and select a destination from the menu.
- `pyproj` remains the recommended dependency, but if it is temporarily missing, QGIS scene compilation and nav-gps scene loading no longer crash immediately; the campus-scale fallback primarily keeps testing and startup usable.

## 2026.07.12

### [Fix Foxglove Pose Goal Crashing The Adapter]

#### File

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/navigation/indoor_navigation_manager/test/test_foxglove_adapter_contract.py`

#### Change

- Changed the pose-goal receipt log to the single-string call supported by the `rclpy` logger.
- Added an AST contract test that rejects adapter logger calls with extra positional arguments.

#### Reason

- After Foxglove published a goal, the adapter called the Python `rclpy` logger with printf-style positional arguments. The callback raised `TypeError`, terminated the node, and never forwarded the goal to Nav2.

#### Effect

- Goals arriving on `/move_base_simple/goal` or `/foxglove/goal_pose` no longer crash the adapter while logging and can proceed through localization gating and `NavigateToPose` forwarding.

### [Support Foxglove Goals Outside The map Frame]

#### File

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/navigation/indoor_navigation_manager/package.xml`
- `src/navigation/indoor_navigation_manager/test/test_foxglove_adapter_contract.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change

- Added a TF buffer/listener to the pose-goal adapter and transform `PoseStamped` goals from source frames such as `base_link` or `odom` into `map` before sending them to Nav2.
- Declared the `tf2_ros` and `tf2_geometry_msgs` runtime dependencies and extended the contract tests.

#### Reason

- The vehicle's Foxglove 3D panel published goals with `header.frame_id=base_link`; the previous adapter only accepted `map`, so it rejected each goal before `NavigateToPose` received it.

#### Effect

- Foxglove layouts no longer need to force the goal frame to `map`. A goal enters Nav2 whenever the current TF tree can resolve its source frame into `map`; an unavailable transform remains fail-closed with an explicit rejection reason.

### [Fix Travel Path Smoothing Failures And Sub-Breakaway Commands]

#### File

- `src/bringup/behavior_trees/travel_nav_to_pose_fail_stop.xml`
- `src/bringup/behavior_trees/travel_nav_through_poses_fail_stop.xml`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/config/collision_monitor_travel.yaml`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Removed `SmoothPath` from the Travel fail-stop trees and pass NavFn paths directly to MPPI, while continuing to prohibit Spin, BackUp, and costmap-clearing recovery.
- Changed the Collision Monitor slowdown zone from a body-enclosing rectangle to a forward `x=0.45~0.85m, y=+-0.30m` buffer and raised its ratio from 35% to 60%; the stop zone is unchanged.
- Adjusted Travel MPPI to `vx_max=0.45m/s`, `vx_std=0.20`, `ax_max=0.70m/s2`, and `regenerate_noises=true`, with matching linear velocity and acceleration limits in velocity smoother.

#### Reason

- Of three vehicle goals, two NavFn paths entered obstacles after Savitzky-Golay smoothing and failed immediately. The third short path reached the controller, but the slowdown zone remained active and serial received only about `0.02~0.034m/s`; chassis feedback stayed at zero until `Failed to make progress` fired after 15 seconds.
- Live cloud measurement showed zero points in the stop zone but about 400 rear/side points in the old slowdown rectangle. A forward-only Travel MPPI controller should not permanently throttle forward motion because of rear or side returns.

#### Effect

- Plannable NavFn paths no longer fail because an extra smoothing step moves them into collision, and the command chain no longer applies a fixed 35% scale when the forward buffer is clear.
- The forward slowdown zone still reduces speed, while the close-body stop zone, localization/cloud freshness gate, 300ms host watchdog, and 500ms firmware watchdog all remain fail-closed.

### [Travel Chassis-Mode Gate And Final Serial Smoothing]

#### File

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/navigation/indoor_navigation_manager/package.xml`
- `src/navigation/indoor_navigation_manager/test/test_foxglove_adapter_contract.py`
- `src/sensor_drivers/serial_twistctl/include/serial_twistctl/twist_command.hpp`
- `src/sensor_drivers/serial_twistctl/src/serial_twistctl_node.cpp`
- `src/sensor_drivers/serial_twistctl/test/test_twist_command.cpp`
- `src/sensor_drivers/serial_twistctl/test/test_watchdog_contract.py`
- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/package.xml`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- The Foxglove adapter now subscribes to `/chassis/status`, accepts goals only in `CTRL_MODE_HOST`, and cancels an active goal when the chassis leaves host mode.
- Added an optional final acceleration limiter to `serial_twistctl`. Zero, deceleration, and zero-crossing remain immediate; only same-direction magnitude increases are limited. The watchdog also resets limiter state.
- Travel enables final recovery limits of `0.30m/s2` linear and `0.80rad/s2` angular, and reduces MPPI to `vx_max=0.30m/s`, `vx_std=0.16`, and `ax_max=0.40m/s2`.

#### Reason

- Vehicle `/chassis/status` reported `ctrl_mode=2`; even while the Jetson continuously sent `vcx=0.45m/s`, the STM32 kept zero feedback because motors were disabled. The previous adapter still accepted goals, creating a risk that an active goal would execute as soon as motors were enabled.
- Serial evidence showed `0.035 -> 0.105 -> 0.175 -> 0.245 -> 0.000 -> 0.385 -> 0.450m/s`. Collision Monitor inserts Stop downstream of velocity smoother, so the upstream smoother retains its high state and releases that old speed immediately after Stop, producing surging and stutter.

#### Effect

- Goals are explicitly rejected while motors are disabled or the chassis remains in gamepad mode, and a mode change cancels active navigation instead of leaving background velocity commands active.
- Safety stops remain immediate and deceleration is not delayed; after Stop releases, commands must ramp again from zero rather than jumping back to a retained high speed.
- The limiter defaults off and is enabled explicitly by Travel launch only, leaving Corridor, Explore, and other serial use cases unchanged.

### [Travel In-Place Alignment And Reliable Breakaway Range]

#### File

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/package.xml`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change

- Wrapped Travel MPPI in Rotation Shim. A path-heading error above `0.35rad` now rotates in place at `0.30rad/s` before MPPI takes over, with the same in-place alignment used for the final goal heading.
- Enabled `VelocityDeadbandCritic` with `[0.14, 0.0, 0.18]` and weight `35.0` to penalize linear/angular trajectories below the vehicle's reliable breakaway range.
- Declared explicit runtime dependencies on the Rotation Shim and MPPI controller plugins.
- Extended the Travel configuration contract test and bilingual operating/tuning documentation.

#### Reason

- With `ctrl_mode=0` held and localization/collision gates healthy, the vehicle serial log continuously sent `vcx=0.04~0.13m/s` and `wc=0.09~0.18rad/s`, while the chassis moved only a few centimeters and felt mechanically stuck.
- Differential-drive conversion of small simultaneous linear and angular commands leaves one wheel near zero and the other still slow, so the chassis PID holds the near-zero wheel in static friction. Since the vehicle can turn in place, aligning first and then driving is a better match for its mechanics.

#### Effect

- With a significant initial heading error, opposite wheel speeds first produce a reliable in-place alignment before forward tracking begins. MPPI also avoids spending extended time in speeds the real chassis cannot execute consistently.
- `vx_max=0.30m/s`, Collision Monitor, localization and chassis-mode gates, final acceleration limiting, and all fail-stop behavior remain unchanged.
