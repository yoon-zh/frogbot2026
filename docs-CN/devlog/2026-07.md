# 2026年7月开发日志

---

## 2026.07.22

### [构建修复与日志清理]

#### 变动文件
- `Makefile`
- `src/frc/frc_bev/setup.py`
- `src/frc/frc_nodes/setup.py`
- `src/navigation/gps_waypoint_dispatcher/setup.py`
- `src/navigation/indoor_navigation_manager/setup.py`
- `src/navigation/waypoint_collector/setup.py`
- `src/navigation/waypoint_nav_tool/setup.py`
- `src/perception/hba/CMakeLists.txt`
- `src/perception/localizer/CMakeLists.txt`
- `src/perception/pgo/CMakeLists.txt`
- `src/perception/pgo_gps_fusion/CMakeLists.txt`
- `src/planning/global2local_tf/setup.py`
- `src/planning/global_path_planning/setup.py`
- `src/planning/gnss_global_path_planner/setup.py`
- `src/sensor_drivers/gnss/gnss_calibration/setup.py`
- `src/sensor_drivers/gnss/nmea_navsat_driver/setup.py`
- `src/sensor_drivers/wit_imu_traj/setup.py`
- `src/sensor_drivers/wit_ros2_imu/setup.py`
- `src/survey/survey_mode/setup.py`

#### 变更
- **Makefile**：在 `setup` 目标（target）中追加了逻辑，以便在执行 `vcs import` 后自动在 `src/third_party/navigation2/nav2_system_tests/` 目录下创建 `COLCON_IGNORE` 文件。
- 在 `COLCON_BUILD` 命令中添加了环境变量 `PYTHONWARNINGS`，用于抑制 setuptools 的警告信息。
- **CMakeLists.txt（感知软件包）**：在 `project()` 声明紧随其后的位置插入了 `cmake_policy(SET CMP0074 NEW)`。
- **setup.py**：在所有本地 ROS 2 Python 软件包中移除了已废弃的 `tests_require=['pytest']` 参数。

#### 原因
- 通过 `vcs import` 拉取的 `nav2_system_tests` 软件包依赖于 Gazebo 组件（`gazebo_ros_pkgs`），这些组件在当前软件栈中并未被使用，并且会导致在 Jetson 上构建时发生致命错误。
- CMake 在执行 `find_package(PCL)` 时，因对 `PCL_ROOT` 变量的解析方式而触发了 `CMP0074` 策略警告。
- 较新版本的 `setuptools` 已废弃 `tests_require` 关键字，导致每个 Python 软件包在编译时都会向构建日志中大量输出 `UserWarning: Unknown distribution option: 'tests_require'` 提示。

#### 效果
- 安全地跳过了依赖 Gazebo 的导航测试编译，确保构建能够顺利完成。
- 彻底消除了编译期间与 CMake PCL 策略相关的警告。
- 通过原生移除已废弃的测试参数并全局静音残留的第三方警告，大幅精简并清爽了 `colcon build` 的终端输出日志。

## 2026.07.13

### Travel 全局起点解锁、连续 TF 校正与受控后退

#### 变动文件

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_to_pose_recovery.xml`
- `src/bringup/behavior_trees/travel_nav_through_poses_recovery.xml`
- `src/bringup/scripts/prior_map_tf_authority.py`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/`、`docs-EN/` 对应命令、室内设计、Nav2 调参和开发日志

#### 变更内容

- authority 的 AMCL 回调只接受稳定目标，实际 `map -> odom` 改为在 20Hz TF timer 中连续追踪；新增 `0.015m/s` 平移、`0.006rad/s` yaw 和 `0.02m/s` 车体等效位移三重校正速度上限，并保留原有单步硬上限。
- authority 订阅 `/initialpose` 区分手动重定位与后台 localizer 自动恢复。手动粗位姿期间保持 TF 可视但报告 `tf_active=false`；已有可信 TF 后，`ambiguous_global_candidates` 恢复不再直接重新播种。
- 修复实车回归：当 localizer 在收到粗位姿前已经是 `LOCALIZED` 时，不再依赖状态边沿；只要新粗位姿 pending 且状态原因成为 `relocalization_accepted`，立即使用最新 localizer transform 重新播种并恢复 `tf_active=true`。纯后台恢复仍不会播种。
- 控制门仍要求 localizer 状态新鲜且 `sensors_ready=true`，但不再把已有 authority TF 下的 3D 候选短时 `LOST` 当作独立停机条件；authority、点云或传感器失效仍立即输出零速。
- global static layer 开启 `footprint_clearing_enabled`，只清机器人当前真实 footprint 下的静态地图残留，避免 NavFn 起点落入内切膨胀区后自锁。
- 两棵 Travel 恢复树新增一次碰撞检查的 `0.20m / 0.08m/s` 短后退，后退后清 local costmap 并立即重规划；正常 MPPI 继续保持 `vx_min=0`。

#### 修改原因

- 最新 `18-12-46`、`18-08-33`、`18-05-01` 三个 bag 的 15Hz 控制循环分别只漏 `3/4/3` 次，算力不是主要停机原因；前两段却在障碍附近连续 `16/17` 次 NavFn 规划失败，第三段触发 3 次 `Failed to make progress`。
- 两次 NavFn 首次失败时目标格均为空闲，但机器人中心代价均为 `99`，真实 footprint 在记录的全局静态图中分别覆盖 `76/114` 个致命格。物理道路存在，规划失败是起点被静态残留/坐标偏移判进障碍，而不是目标不可达。
- 三段实际 `map -> odom` 分别修改 `13/16/18` 次，累计平移约 `0.211/0.232/0.226m`；厘米级离散校正会让 Foxglove 抖动，并在运动中把全局规划起点推入膨胀区。
- 两段 localizer 分别有 `34/111` 帧 `ambiguous_global_candidates`，但这些帧 `sensors_ready` 全为 true；旧控制门因此停机属于定位候选层误门控，不是雷达或 LIO 失效。
- `19-03-43` 与 `19-05-40` 回归包中 `/cmd_vel_nav` 分别有 `899/252` 条非零命令，但 `/cmd_vel_localized` 的 `3025/1330` 条全部为零；authority 在粗位姿后永久停在 `MANUAL_RELOCALIZING`，证明不动来自手动重定位完成条件漏掉“已处于 LOCALIZED”场景。

#### 带来影响

- 稳定 AMCL 校正仍可消除长期漂移，但不再以厘米级阶跃移动地图；新的粗位姿仍 fail-closed，后台候选歧义不会覆盖可信 TF。
- 手动重定位仍在真正 ICP 接受前 fail-closed，但成功后会可靠退出 pending，不再把 Nav2 的有效命令永久截断。
- NavFn 可以从车体当前占用的旧地图黑点/膨胀区重新展开搜索；footprint 外的静态障碍、local costmap 实时点云、MPPI footprint critic、Collision Monitor 和物理急停均保持有效。
- 转向空间不足时可低速后退一次再规划，避免只在障碍前原地转动或永久等待；后方不可行时 behavior server 的 footprint 碰撞预判会拒绝后退。

### Travel 原地转向死锁、地图抖动与 MPPI 漏周期修复

#### 变动文件

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/config/master_params.yaml`
- `src/bringup/scripts/prior_map_tf_authority.py`
- `src/bringup/scripts/prior_map_tf_math.py`
- `src/bringup/test/test_prior_map_tf_math.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`、`docs-EN/commands.md`
- `docs-CN/knowledge/indoor_mapping_navigation_design.md`、`docs-EN/knowledge/indoor_mapping_navigation_design.md`
- `docs-CN/knowledge/nav2_tuning.md`、`docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

- Rotation Shim 改为 `closed_loop=false`，保留 `0.24rad/s` 目标角速度和 `0.65rad/s2` 加速度限制，让预旋转命令根据上一周期输出连续爬升。
- Travel MPPI 从 `20Hz/model_dt=0.05s, time_steps=32, batch_size=160` 改为匹配的 `15Hz/model_dt=0.0666667s, time_steps=24, batch_size=128`；预测时域仍约 1.6 秒，每周期候选点从 5120 降到 3072。
- AMCL authority 新增 5 帧候选窗口、至少 3 帧一致性、SE(2) 中值、窗口分散度门限和 1 秒校正间隔；平移/yaw 死区从联合判定改为独立判定，并调整为 `0.05m/0.035rad`。
- authority 状态新增候选窗口样本数、平移/yaw 分散度、有效候选与实际校正 age，区分 accumulating、unstable、rate-limited 和 stable-hold。
- `pointcloud_to_laserscan.scan_time` 从 `0.0333333s` 修正为 `0.1s`，与 rosbag 实测约 10Hz 扫描频率一致；不改变高度窗、矩形车体自滤波、角分辨率或量程。

#### 修改原因

- `16-41-02` 和 `16-43-22` 包中，第一个目标首次报 `Failed to make progress` 后，`96~100%` 控制命令固定为 `linear.x=0, angular.z=+-0.0325rad/s`。该值等于旧闭环 Rotation Shim 的单周期角加速步长；真实底盘无法越过静摩擦，反馈持续为零，导致每周期重新从同一小命令开始。
- 两个包的 raw AMCL `map->odom` 候选分别累计来回移动约 `0.90m/0.73m` 和 `26.4deg/26.9deg`，而净变化远小于累计变化；旧 authority 对每个过门限样本立即校正，造成地图持续抖动。
- controller 日志仍高频报告 20Hz deadline miss。Orin NX 总 CPU 有余，但 Humble MPPI critics 在单个控制回调内串行评分，必须降低单周期工作量和目标频率，而不是依赖更宽的速度看门狗。
- 两个包的 `/scan` 实测中位周期约 `0.1s`，旧 `scan_time=0.0333333s` 错误声明为 30Hz。

#### 带来影响

- 大角度预旋转会在约 0.4 秒内逐步达到 `0.24rad/s`，不再永久停在底盘不可执行的第一加速步；普通 MPPI 小角速度仍不会被全局放大。
- 两包离线回放中，authority 实际 TF 改值从 `31/35` 次降到 `7/6` 次，累计 yaw 从约 `5.1deg` 降到 `0.31/1.20deg`，同时仍释放约 `5~6cm` 稳定平移校正。
- MPPI 预测时域、速度边界、footprint、Collision Monitor、定位/点云门和物理急停均保持不变；候选点评分量降低 40%，用于减少漏周期而不放宽安全边界。

### Travel 控制减摆、恢复重规划与 AMCL 先验地图校正

#### 变动文件

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_*_recovery.xml`
- `src/bringup/scripts/post_collision_cmd_conditioner.py`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/bringup/scripts/prior_map_tf_authority.py`、`prior_map_tf_math.py`
- `src/bringup/scripts/initialpose_relocalize_bridge.py`
- `src/bringup/launch/system_travel.launch.py`、`CMakeLists.txt`、`package.xml`
- `src/perception/localizer/src/localizer_node.cpp`
- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/manager_node.py`、`foxglove_adapter_node.py`
- `scripts/launch_with_logs.sh`、`Makefile`
- `src/bringup/test/`、`src/perception/localizer/test/`
- `docs-CN/`、`docs-EN/` 对应命令、知识和开发日志

#### 变更内容

- 撤销所有小角速度无条件补到 `0.20rad/s` 的后处理；Slow 混合指令只有在线速度低于死区且角速度至少 `0.16rad/s` 时清除平移，角速度原样保留。
- Rotation Shim 改为 `0.65/0.35rad` 接管/退出，关闭终点朝向接管；VelocityDeadbandCritic 降为权重 15 和 `[0.12,0,0.08]`，PathAlign/PathFollow 权重降为 `6/12`。
- MPPI 保持 `20Hz/model_dt=0.05s`，`time_steps` 从 40 降到 32，移除冗余 PreferForwardCritic 和 Humble 不支持的 CostCritic `trajectory_point_step`。local/global costmap 发布频率降为 `2/1Hz`，local 内部更新仍为 10Hz。
- 恢复树改为 1Hz：先清局部图并立即重规划/平滑；再次失败且 `IsStuck` 成立时只转 `0.52rad`，最后才等待/清双图，仍禁止 BackUp。
- localizer 新增锁存候选变换输出和 `publish_tf` 开关；Travel 中 localizer/AMCL 都不发 TF，由 `prior_map_tf_authority` 独占 `map->odom`。authority 自动用 localizer 种子初始化 AMCL，并对 AMCL 候选执行协方差、同步、目标跳变和车体等效跳变门控。
- 控制门新增 `/travel/control_gate/status` 与 diagnostics，分别报告定位、点云、碰撞、指令超时或正常放行；指令超时窗从 0.25s 调整为 0.40s。
- Travel 涉及的 Python 节点在 spin 边界处理 SIGINT/已关闭 context，并在 shutdown 前检查 `rclpy.ok()`，避免正常停止被误报为进程崩溃；context 仍有效时的真实异常不会被吞掉。
- Foxglove 粗位姿若携带 `base_link` 等非 `map` frame，bridge 不再直接拒绝；它按 `/initialpose` 的全局位姿语义规范化为 `map`，同时转发 localizer/AMCL，并把 `/initialpose` 纳入 Travel rosbag。

#### 修改原因

- `22-03-41` rosbag 中后处理器改写 55.9% 命令，其中 4433 条成为纯旋转，是左右犹豫的主要放大器；同一会话 MPPI 在约 110.5s 活动控制中漏 58 个 20Hz 周期，CPU 总量有余但串行 critic 回调受单核截止时间限制。
- 走廊后段 scan-to-map 残差从约 `0.03~0.13m` 增长到约 `1.05m`，而固定 localizer 种子没有运行中全局校正；直接让 AMCL 或 localizer 抢 TF 又会引入双发布者和跳变风险。
- 旧恢复顺序先 Wait/90 度 Spin，未优先清掉局部陈旧障碍并重规划，导致可绕开的真实障碍前长时间停住。
- 实车当前会话连续 5 次粗位姿都被新增的严格 `frame_id=map` 校验拒绝；初始定位前没有 `map -> base_link`，无法用 TF 修正 Foxglove 的 frame 标签。

#### 带来影响

- 普通小角度由 MPPI 连续控制，不再被后处理放大成来回原地转向；局部路径可更自然绕障，同时保留 footprint、Collision Monitor 和物理急停安全边界。
- 单周期 MPPI 候选点评分量下降 20%，costmap 序列化负载下降；控制频率仍满足 Humble 的模型步长约束。
- Travel 建立唯一、可审计的先验地图 TF authority。坏 AMCL 匹配只改变状态、不拉跳 TF；定位或点云不健康仍由速度门 fail-closed。

## 2026.07.12

### Travel 闭环减摆与障碍前原地脱困

#### 变动文件

- `src/bringup/config/nav2_travel.yaml`、`collision_monitor_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_*_recovery.xml`
- `src/bringup/scripts/post_collision_cmd_conditioner.py`
- `src/bringup/launch/system_travel.launch.py`、`CMakeLists.txt`
- `scripts/launch_with_logs.sh`、`Makefile`
- 对应测试与中英文文档

#### 变更内容

- 全局重规划从 1Hz 降到 0.5Hz；MPPI 保持 20Hz 并使用固定采样噪声和较低角噪声，Rotation Shim 使用闭环里程计和迟滞。`21-45-44` 包出现 146 次控制周期超时后，将采样批量从 200 降到 160，保留 2 秒预测时域。
- Collision Monitor 输出改为 `/cmd_vel_safe_raw`，新增末级条件器：受 Slow 影响且落入底盘死区的混合低速转弯改为原地转向；所有非零纯旋转命令无条件补到至少 `0.20rad/s`。进度插件改为 `PoseProgressChecker`，`0.15rad` 转向也算有效进展。
- global inflation 最终使用 `0.30m`，仍高于当前 footprint 的 `0.285m` 内切半径，比 `0.35m` 少一个地图栅格。

#### 修改原因

- `21-06-57` 包第二段被真实障碍持续触发 Slow 后卡住；`21-45-44` 包则证明本次 Slow 几乎未介入，停滞来自持续 `0.04rad/s` 的无效纯旋转命令和只认可平移的进度检查。

#### 带来影响

- 降低路径刷新、随机采样和控制计算造成的左右扭动与周期超时；原地转向能越过底盘死区并持续计入进展。全局路径可多使用一个 `5cm` 栅格，同时保留真实 footprint、local costmap、Collision Monitor Stop、零命令和看门狗语义。

### Travel 路径平滑、终点转向与 TF 调度修复

#### 变动文件

- `src/bringup/behavior_trees/travel_nav_*_recovery.xml`
- `src/bringup/config/nav2_travel.yaml`、`master_params.yaml`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/perception/fastlio2/src/lio_node.cpp`
- 对应测试与中英文知识文档

#### 变更内容

- 每次 NavFn 重规划后执行 Savitzky-Golay 路径平滑；`20-58-24` 实车包证明全局 footprint 复检会误拒绝全部平滑路径，因此关闭 smoother 重复检查，保留 local costmap、MPPI footprint critic 与 Collision Monitor 三层运动安全。local costmap 更新率从 15Hz 降到 10Hz。
- 非零纯原地转向命令低于底盘有效区时补到 `0.20rad/s`，FAST-LIO2 TF 增加 `0.05s` 可配置前向容差。

#### 修改原因

- `20-37-08` rosbag 中首条 4.23m 路径累计折转 15.23rad，车辆角速度换向 46–52 次；到点半径内仍持续输出底盘无法执行的 `0.04rad/s`，并伴随 controller TF future extrapolation。

#### 带来影响

- MPPI 跟踪几何更连续的全局路径；终点原地对向能越过静摩擦区；10Hz costmap 和 TF 容差降低控制循环与毫秒级 TF 调度压力。零速、碰撞停车、BackUp 禁用和速度看门狗保持不变。

### Travel 每会话自动导航 rosbag

#### 变动文件

- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`

#### 变更内容

- Travel 默认在日志会话的 `data/travel_bag/` 自动录制目标、状态、TF、里程计、路径、代价地图、扫描和各级速度命令。
- 增加 `FYP_TRAVEL_BAG_PROFILE=debug` 原始点云诊断档与 `FYP_TRAVEL_RECORD_BAG=false` 显式关闭开关。

#### 修改原因

- 实车室内导航存在轨迹扭动、短停和恢复失败，仅靠控制台日志无法对齐规划路径、实际轨迹、障碍输入与最终下发速度。

#### 带来影响

- 每次 Travel 启动都保留可回放证据；默认 lean 档避免原始 Livox 点云的 CPU、磁盘负担，debug 档只在点云专项排查时启用。

### [建图矩形车体自滤波与孤立栅格清理]

#### 变动文件

- `src/perception/pointcloud_to_laserscan/`
- `src/bringup/config/pointcloud_to_laserscan_mapping.yaml`
- `src/bringup/launch/system_slam.launch.py`
- `src/bringup/test/test_system_slam_launch.py`
- `scripts/clean_occupancy_map.py`
- `scripts/test_clean_occupancy_map.py`
- `docs-CN/`、`docs-EN/` 对应架构、命令和室内设计文档

#### 变更内容

- 点云转 LaserScan 节点新增可选矩形排除框；启用时强制要求明确的 `target_frame`，非法边界在启动时拒绝。
- SLAM 专用 profile 在 `base_footprint` 中排除 `x=[-0.35,0.35]m, y=[-0.275,0.275]m`，对应实测 `650x500mm` 车体每侧 `25mm` 余量。
- 新增 PGM 8 连通域清理工具，默认只替换 1–3 格的孤立占用块，并按边界多数恢复为自由或未知。

#### 修改原因

- 实车 `floor_4` 地图出现沿行驶轨迹分布的孤立黑点，形态符合车体近场反射写入 Slam Toolbox 的结果；旧投影既没有车体外廓过滤，也没有离线保守清理手段。

#### 带来影响

- 新建 2D 地图不会再把矩形车体内部回波沿轨迹累计为障碍；该 profile 只用于 SLAM，不改变 Travel/Corridor 实时障碍感知。
- 已保存地图可先输出到新 PGM 检查后再替换。大于 3 格的柱子、家具和墙体保持不变，未知区不会被统一扩大成自由区。

### [Travel 运行抖停与定位瞬断修复]

#### 变动文件

- `src/perception/fastlio2/`、`src/perception/localizer/`
- `src/bringup/config/{master_params.yaml,nav2_travel.yaml,collision_monitor_travel.yaml}`
- `src/bringup/scripts/localization_cmd_gate.py`
- `src/navigation/indoor_navigation_manager/`
- 对应测试和双语文档

#### 变更内容

- Nav2 障碍云新增 `0.80x0.60m` 矩形本体滤波；实车复测后 Stop 框保持 `0.80x0.64m`。宽路静态采样中 Stop 区散点每帧 `0~6`、平均 `2.39`，因此 Stop/Slow 的 `max_points` 从 3 调整为 10。
- 对车辆移动前后都固定存在的右前水平弧形回波增加 `x=[0.40,0.82]m, y=[-0.35,-0.14]m, z=[0.25,0.42]m` 三维自反射补丁。
- localizer 的 `map->odom` 增加 `0.10s` 未来容差；连续失败阈值改为 8 次，DEGRADED 最多保持最近可信定位 `5.0s`。
- 活动目标遇到短时定位不可用时立即零速保持，但等待 `5.0s` 恢复后才取消目标。
- 100 条底盘状态采样出现单帧非法 `ctrl_mode=4/252`、`ps2_key=255/252`；适配器新增 `1.0s` 模式去抖，持续退出 HOST 才取消目标。
- Travel MPPI 从 `500` 经 `300x48` 继续降到 `200x40`，关闭 critics stats，并把控制失败容忍从 `0.6s` 调到 `2.0s`。
- 用有限恢复行为树替换 Travel fail-stop 树：规划/控制失败分别清全局/局部代价，外层最多 5 次执行 Wait、碰撞检查 Spin 和清图重试；仍禁止 BackUp 与路径平滑。
- 实车运动约 17 秒后连续 ICP 会撤掉有效 `map->odom` 并导致 TF extrapolation；当前关闭运行时连续 ICP，仅在初始位姿时验证 PCD 配准，随后由 FAST-LIO2 传播运动。

#### 修改原因

- 实车控制循环持续错过 20Hz，并出现 `map->odom` 向未来外推 10–35ms 失败；Collision Monitor 同时被右前车体边缘回波反复触发。单次运行时 ICP 失败又会立即取消目标，叠加后表现为左右扭动后停车。

#### 带来影响

- 稀疏本体回波不再进入局部避障，串口状态乱码不会立即取消目标，规划/控制失败能自动清图、原地转向并继续找路；已接受的初始定位也不再被失败的运行时 ICP 撤销。密集真实障碍停车、Spin footprint 碰撞检查、输入 freshness 和串口/固件 watchdog 均保留。

## 2026.07.11

### [Foxglove 默认目标话题兼容]

#### 涉及文件

- `foxglove_adapter_node.py`、`system_travel.launch.py`
- 对应测试和双语文档

#### 变更内容

- 导航适配器新增 `/move_base_simple/goal` 兼容订阅，并将其加入 Bridge client-publish 白名单；仓库布局的 `/foxglove/goal_pose` 保持不变。
- 增加 Pose 目标接收和 Action 拒绝/失败日志，便于从 session log 定位目标停在哪一层。

#### 变更原因

- 实车 Foxglove 默认 3D 面板 advertise `/move_base_simple/goal`，而原适配器只监听 `/foxglove/goal_pose`，导致 Pose 有 publisher 但无 subscriber，目标从未进入 `/navigate_to_pose`。

#### 带来影响

- 两种 Foxglove 布局均可发目标，且都经过相同的结构化定位、并发和 Action 门控；没有新增任何速度话题权限。

### [Travel 定位速度门安装修复]

#### 涉及文件

- `src/bringup/scripts/localization_cmd_gate.py`
- `src/bringup/test/test_system_travel_launch.py`

#### 变更内容

- 将定位速度门脚本权限从 `0644` 修正为 `0755`，并增加 ROS libexec helper 必须可执行的契约测试。

#### 变更原因

- CMake 已创建正确的 install symlink，但 ROS 2 会把不可执行脚本报告为 libexec 中 executable not found，导致 Travel 在启动任何运行节点前中止。

#### 带来影响

- `launch_ros.actions.Node(executable="localization_cmd_gate.py")` 可正常解析并启动；安全门逻辑和参数没有变化。

### [真实地图 2D↔3D 墙面对齐改进]

#### 涉及文件

- `scripts/mapping_session_utils.py`、`scripts/save_mapping_session.py`
- 对应测试、室内命令文档和设计文档

#### 变更内容

- 用 XY 栅格内垂直跨度提取强墙面单元，替代整个高度带 PCD 的直接 XY 投影；默认 `0.06m` 栅格、至少 3 点、跨度 `>=0.50m`。
- 平面配准显式加入 identity 初值，将质量样本上限提高到 12000 点，并以 0.50m 空间桶计算精确局部最近邻。
- 对齐报告记录墙面提取参数和点数，质量检查与叠加图统一使用同一批墙面点。
- 新增 `--recalibrate-only`，可在不调用 ROS 保存服务、不覆盖原地图和现场采集证据的前提下修复已有 bundle，并同步重建描述子区域标签。

#### 变更原因

- `floor_4` 首份实车地图的文件、静止和 frame 门均通过，但整层 PCD 投影混入地面/家具/动态杂点，导致 RMSE `0.159m`、p95 `0.279m`、重叠率 `0.531`，错误拒绝了整体坐标接近一致的地图。
- 两套地图同会话从共同原点启动，原算法只使用主轴/质心初值，重复长走廊可能把搜索带入约 1.4m 的错误局部最优。

#### 带来影响

- 在不放宽 `0.10m/0.15m/0.55` 门槛的前提下，`floor_4` 离线回归达到 RMSE `0.065m`、p95 `0.126m`、重叠率 `0.957`，变换收敛到接近 identity。
- 地面和单高度杂点不再主导标定；真实非线性形变仍会被原质量门拒绝，不能通过该改动绕过验收。

### [室内导航实车前安全审查修复]

#### 涉及文件

- `src/sensor_drivers/serial_twistctl/`、`src/firmware/rm_c_board/Core/Src/main.c`
- `src/perception/localizer/`、`src/navigation/indoor_navigation_manager/`
- `src/bringup/scripts/localization_cmd_gate.py`、`src/bringup/config/master_params.yaml`
- 对应测试与双语文档

#### 变更内容

- 新增主机 300ms 与 STM32 500ms 双层速度命令 watchdog；固件 UART 数据显式补字符串终止符。
- 定位速度门新增障碍点云 0.5s 和速度命令 0.25s freshness，任一失效持续输出零速。
- 修复命名导航在底层 goal handle 返回前取消丢失的竞态，并保证异常路径释放目标占用。
- 修正 Scan Context yaw shift 符号；自动全局定位等待至少 200 点并以 3s 间隔最多重试 5 次。
- localizer 将 `map -> odom` 严格投影为 XY+yaw 平面变换，不再传播 ICP roll/pitch/z。

#### 变更原因

- 原串口链和仓库固件都没有命令断流停车，安全节点/Jetson 崩溃可能让底盘保持最后非零命令。
- Humble Collision Monitor 会忽略过期 source，而不是因为 source timeout 自动停车。
- 取消竞态、Scan Context 旋转反号和无平面约束的 ICP 都可能造成实车目标失控、自动定位失败或 TF 倾斜。

#### 带来影响

- 定位、障碍流、Nav2 命令、主机串口或上位机进程任一断流都会在最多 500ms 内把仓库固件目标速度清零。
- 自动定位可从首帧失败恢复，室内全局 TF 保持平面约束，命名目标取消不再跨 Action 层丢失。
- Jetson 代码更新后仍必须重新编译并烧录 STM32 固件；烧录前不得宣称固件 watchdog 已生效。

### [Foxglove 室内导航完整操作面]

#### 涉及文件

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/bringup/foxglove/indoor_navigation.json`
- `src/bringup/launch/system_travel.launch.py`
- `scripts/launch_with_logs.sh`、`Makefile`
- 对应 package、测试与双语文档

#### 变更内容

- Travel 默认启动官方 `foxglove_bridge:8765`，支持 `FYP_USE_FOXGLOVE=false` 或 `foxglove_port:=<port>` 覆盖。
- 新增 Foxglove 导航适配器：点击 Pose 转 `NavigateToPose`，地点字符串转 `NavigateNamedDestination`，Trigger Service 取消当前目标。
- 适配器发布 transient-local 地点目录、地点 MarkerArray 和导航状态；定位未健康时拒绝目标，运行中定位降级会取消目标。
- Bridge client-publish/service 使用最小白名单并移除参数修改 capability，Foxglove 不能直接发布 `/cmd_vel*` 或改运行参数。
- 新增可导入 layout，组合 3D 地图/点云/costmap/路径/安全区、定位/导航状态、地点发送、区域重定位、取消、日志和 Topic Graph。

#### 变更原因

- Foxglove 原生支持 Topic/Service 和点击 Pose，但没有适合当前自定义 ROS 2 Action 的目标面板；直接操作 Action 底层端点既不直观也不安全。
- 需要在电脑端替代 RViz 完成定位、发目标和状态观察，同时保持定位门控、Action 取消和串口前速度安全链不被绕过。

#### 带来影响

- 室内 Travel 可在一个 Foxglove layout 中完成节点拓扑、定位、点击导航、地点名导航和取消操作。
- 适配器永不访问 `/cmd_vel*`；Foxglove 连接不会获得绕过 Nav2、localizer 或 Collision Monitor 的直接运动控制权限。
- `8765` 只允许在受控局域网/Tailscale 使用，不应映射到公网；Jetson 仍需安装 `ros-humble-foxglove-bridge` 并完成实车 smoke。

### [室内建图与地点导航阶段 1-5 全量实现]

#### 涉及文件

- `scripts/mapping_session_utils.py`、`scripts/save_mapping_session.py`
- `src/perception/fastlio2/`、`src/perception/localizer/`、`src/perception/interface/`
- `src/navigation/indoor_navigation_manager/`
- `src/bringup/launch/system_{slam,travel}.launch.py`
- `src/bringup/config/{master_params,nav2_travel,collision_monitor_travel}.yaml`
- `src/bringup/behavior_trees/travel_*_fail_stop.xml`
- 对应双语 architecture、commands、FAST-LIO2、Nav2 tuning 和室内设计文档

#### 变更内容

- FAST-LIO2 新增 `[-0.20,1.80]m` 初始高度窗的定位结构云；PGO 与 localizer 统一消费该云，原低窗 LaserScan 云与 Nav2 障碍云职责不变。
- 地图保存升级为 `runtime-data/maps/indoor/<map_id>` 版本化 bundle，包含 pose graph、原始/降采样 PCD、Scan Context、regions/destinations、`T_map_2d_map_3d`、RMSE/p95/重叠率报告和叠加图；静止、底盘、frame、完整性或标定失败会返回非零。
- localizer 新增结构化状态、自动/区域/手动重定位、多候选歧义拒绝、1Hz 连续 ICP、重叠率/跳变门控和 `alpha=0.15` 平滑修正。
- Travel 只加载整体门槛通过的 bundle；速度链增加定位 fail-closed gate 和 Collision Monitor，并使用 `650x500mm + 25mm/侧` polygon、footprint critic、路径碰撞检查及 `0.20rad` yaw 终点容差。
- 新增 `NavigateNamedDestination` Action 和地点管理器，支持别名、map/backend 校验、反馈、取消、并发拒绝与定位降级取消；SLAM 默认记录 lean 证据 bag。

#### 变更原因

- 低窗点云无法同时满足干净 2D 建图、三维地点识别和高障碍检测；必须拆分感知职责并保证离线地图与在线定位使用同一结构语义。
- 2D Slam Toolbox 图和 3D PGO 图来自不同优化器，不能默认同坐标系；未经显式标定和完整性检查的地图不得驱动车辆。
- 车辆没有固定充电桩/固定起点，单靠 RViz 每次给初值不符合最终使用方式；同时自动定位必须在重复走廊歧义时拒绝而非误定位。
- 当前车体可原地转向，但圆形 footprint 会错误描述旋转外廓；定位丢失仅显示告警也不足以满足 fail-stop 安全要求。

#### 带来影响

- 软件阶段 1-5 已具备端到端接口，正式 Travel 入口改为单个 `map_bundle`，用户可直接发送地点名而不依赖 RViz 目标工具。
- 当前高度窗、ICP/配准门槛、footprint 余量和 Collision Monitor 区域是保守初值，不代表实车已验收；必须在 Jetson 用 `--parallel-workers 1` 构建，并完成任意起点、窄门、动态障碍、定位丢失停车和十次往返测试。
- 室内外算法切换仍不在本轮范围；在室内验收完成前不增加第二定位权威源。

### [SLAM 参数固化与 Travel MPPI 控制周期修复]

#### 涉及文件

- `src/bringup/config/slam_toolbox_mapping.yaml`
- `src/bringup/launch/system_slam.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/test/test_system_slam_launch.py`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

- 将 Slam Toolbox Humble async mapper 基线复制到仓库配置，保留原 launch 的 `transform_timeout=1.0`、`tf_buffer_duration=60.0`，并统一 `base_frame=base_footprint`。
- `system_slam.launch.py` 改为读取仓库内配置，不再依赖系统安装目录中的默认 YAML。
- Travel `controller_frequency` 从 `15Hz` 修正为 `20Hz`，与 MPPI `model_dt=0.05s` 匹配。
- 增加 SLAM launch/参数合约测试，并更新 Travel 参数断言。

#### 变更原因

- 系统安装的 Slam Toolbox 默认参数不受仓库版本控制，重装或依赖变化可能导致建图行为不可复现。
- 现场日志已经证明 `15Hz` 控制周期大于 `0.05s` 模型步长，会让 MPPI 拒绝配置并导致 Nav2 lifecycle 启动失败。

#### 带来影响

- SLAM 建图算法参数本轮保持 Humble 上游基线，不引入未经实车验证的 matcher 调参；TF 基坐标与 FAST-LIO2 保持一致。
- Travel 仍保持原速度、采样量和 critic，仅提高控制循环到有效的 `20Hz`；需要在 Jetson 上完成构建和实车 smoke。

### [SLAM 建图与室内导航设计开发方案]

#### 涉及文件

- `docs-CN/knowledge/indoor_mapping_navigation_design.md`
- `docs-EN/knowledge/indoor_mapping_navigation_design.md`
- `docs-CN/index.md`
- `docs-EN/index.md`

#### 变更内容

- 新增 SLAM 建图和 Travel 室内导航的双语设计基线，明确地图资产、TF 所有权、定位状态机、规划控制、安全和地点名导航接口。
- 确定 Slam Toolbox 的干净 2D OccupancyGrid 继续作为 Nav2 导航地图，PGO 3D PCD/关键帧用于全局重定位；地图包通过显式 `T_map_2d_map_3d` 和质量报告关联两者。
- 将开发拆分为基线固化、正式地图包、Travel 安全控制、低频防漂移、任意起点全局定位和地点名导航六个阶段，并定义实车测试矩阵与完成标准。

#### 变更原因

- 当前 SLAM/Travel 已能保存地图并通过 RViz 初值执行 ICP 导航，但尚未具备严格 2D/3D 配准、自动全局定位、运行中漂移校正和面向用户的地点接口。
- 直接投影 PGO 点云生成 2D 图不如 Slam Toolbox 干净，因此需要保留双地图优势，同时消除默认同坐标系带来的隐患。

#### 带来影响

- 本次仅建立开发方案，不改变任何运行代码或参数。
- 后续室内研发按文档阶段实施；室内链路达到完成定义前，不引入室内外自动切换。

### [Travel 室内先验地图导航稳定化]

#### 涉及文件

- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/behavior_trees/travel_nav_*_fail_stop.xml`
- `src/bringup/scripts/initialpose_relocalize_bridge.py`
- `src/bringup/scripts/nav2_cloud_retime.py`
- `src/perception/localizer/`

#### 变更内容

- 增加 RViz 初始位姿到 ICP 重定位桥接、Nav2 障碍点云当前时间戳重发布，以及 Travel 专用 fail-stop 行为树。
- 由 ICP localizer 独占 `map -> odom`，导航期间冻结成功校正；全局规划只使用静态地图，局部代价地图使用近场点云。
- 将历史 DWB profile 替换为低负载室内 MPPI：`vx_max=0.35`、`wz_max=0.65`、`controller_frequency=15 Hz`、`batch_size=500`。

#### 变更原因

- 历史 Travel 把实时障碍混入全局规划，允许室内自动运动恢复，并给 Jetson 带来过高的控制器和 costmap 负载。

#### 带来影响

- 操作员必须确认 ICP 重定位成功后才能发送目标；规划或局部控制找不到安全轨迹时，Travel 会停车并返回失败，不会自动旋转、倒车或清图。

---

## 2026.07.09

### [文档更新]

#### 变动文件
- `CONTRIBUTING-EN.md`
- `CONTRIBUTING.md`
- `README.md`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/devlog/2026-07.md`
- `docs-CN/index.md`
- `docs-CN/known_issues.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/handoff/rtk_test.md`
- `docs-EN/index.md`
- `docs-EN/known_issues.md`

#### 变更内容

- 根据当前硬件状态更新了相关文档
- 基于新的 `.bashrc` 简化并移除了部分命令
- 简化了贡献指南，以便更好地突出核心要求
- 更新了问题追踪器 (tracker) 中部分 issue 的状态

### [Corridor 默认 FGO shadow 录包证据]

#### 涉及文件

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

- Corridor launch 默认启动 `rtk_fgo_localizer` shadow node，并固定传入 `publish_tf=false`、`nav2_use_fgo=false`。
- 新增 `enable_fgo_shadow` launch 参数，默认读取 `FYP_CORRIDOR_ENABLE_FGO_SHADOW`，用于需要时关闭 shadow FGO。
- Corridor lean bag 默认增加 `/livox/imu`、`/odom_CBoar` 和 `/rtk_fgo/*`；原始 `/livox/lidar` 与 `/fastlio2/body_cloud*` 仍保留在 debug profile。
- 增加 launch 文本测试，锁定默认 FGO shadow 不接管 TF/Nav2，同时确保下一次 corridor bag 包含 FGO replay 必需证据。

#### 变更原因

- 2026-07-09 离线回放证明最新版 FGO shadow 能在 15:59、17:55、18:00、18:02 bag 中进入 `RTK_LOCKED` 并持续 commit，但旧 corridor bag 缺少 `/odom_CBoar` 和 `/livox/imu`，无法评估完整 IMU/wheel/FGO 路径。
- 后续是否让 FGO 参与 RTK 弱信号 fallback，需要实车默认录到 `/rtk_fgo/status`、`/rtk_fgo/factor_diagnostics`、底盘 odom 和 Livox IMU，而不能每次靠手工补录。

#### 带来影响

- 下一次 `make launch-corridor` / `bash scripts/launch_with_logs.sh corridor` 的 bag 可直接用于 FGO shadow replay 指标分析。
- FGO shadow 只发布 `/rtk_fgo/*`，不广播生产 `map→odom`，也不 remap Nav2，因此不会改变当前 RTK authoritative 导航链路。
- 默认 bag 体积会小幅增加，但仍不录原始 Livox 点云；需要完整点云回放时继续使用 `FYP_CORRIDOR_BAG_PROFILE=debug`。

### [Corridor MPPI 近场规划与 RTK raw target 跳变门控]

#### 涉及文件

- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

- `rtk_map_odom_corrector` 新增上一帧可信 raw target 状态；`TARGET_JUMP` gate 改为比较当前 raw target 与上一帧可信 raw target，不再比较当前 raw target 与已平滑/限幅后的输出。
- `/localization_authority/diagnostics` 末尾追加 raw target 与输出之间的 gap，便于下一次 bag 直接区分 RTK raw 跳变和输出平滑滞后。
- Corridor Nav2 的 `failure_tolerance` 提高到 `1.5s`，避免 MPPI 遇到短暂无可行控制时第一帧 optimizer failure 就 abort。
- Corridor local costmap 从 `30m x 30m` 收敛到 `12m x 12m`，STVL marking `obstacle_range` 从 `15m` 收敛到 `5m`。
- Corridor `CostCritic.cost_weight` 从 `4.5` 提高到 `7.0`，提高动态障碍代价相对贴线的优先级。

#### 变更原因

- 2026-07-09 17:55 bag 中 RTK 大多数时间保持 `q=4`，`gps_global_aligner` raw alignment 只小幅变化，但 `/localization_authority/status` 出现大量 `TARGET_JUMP`；根因是输出平滑/限幅落后时，旧 gate 把 raw target 到 lagged output 的距离误判成 RTK 跳变。
- 同日 18:00/18:02 的失败更集中在 MPPI 局部规划：有人阻挡的一次应视为 local costmap 正确停车并尝试绕行，另一次则是短暂 optimizer failure / progress failure 过早终止。
- 17:55 的一段“画龙”与角速度正负频繁切换、`map→odom` 冻结/追赶和 MPPI 追线叠加有关；只降低角速度不能从根上解决。

#### 带来影响

- 稳定但滞后的 `map→odom` 输出不再让 RTK authority 长时间误入 `TARGET_JUMP`，后段画龙和走着走着停住的概率应下降。
- Corridor 保留人群前停车和尝试绕障的 fail-stop 行为，但 local costmap 只处理近场障碍，降低远处稀疏点和大 rolling grid 对 MPPI 的干扰。
- 下一轮 bag 可用 diagnostics 的 raw/output gap 判断是否仍是 authority 滞后，或已经转为纯 MPPI/底盘执行问题。

### [RTK Authority map_base 跳变门控离线验证]

#### 涉及文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/architecture.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

- 新增 `compute_authority_target_delta()`，将 authority 的 target-jump 平移/yaw 判断绑定到 raw RTK `map_base`，而不是 raw `map→odom` target。
- `rtk_map_odom_corrector` 新增 `_last_raw_map_base`，正常发布后记录上一帧可信 raw RTK `map_base`。
- 保留 raw `map→odom` target 的低通、单步限幅和 raw/output gap 诊断，用于继续平滑实际发布的 `map→odom`。
- 增加单元测试，覆盖 raw RTK `map_base` 只移动约 `0.32m`，但因车辆离 odom 原点约 `67m`、yaw 变化导致 raw `map→odom` target 平移超过 `6m` 的场景。

#### 变更原因

- 对 Linux 分析机上 6 个 corridor bag 离线回放发现：旧 `map→odom` target gate 在 `2026-07-09-14-23-47`、`15-45-27`、`17-55-23` 等 bag 中会出现数百次 target reject；但同一时刻 raw RTK `map_base` 最大单步仅约 `0.20-0.36m`，并未表现出真正 RTK 米级跳变。
- 根因是 `map→odom = map_base * inverse(odom_base)`；当车离 odom 原点几十米时，几度 yaw correction 会把 raw `map→odom` target 的平移放大到十几米，而这不代表 RTK 车体位置跳变。

#### 带来影响

- 17:55 类“RTK map_base 平滑但 authority 长时间 `TARGET_JUMP` 冻结”的问题应被消除；离线回放 6 个 bag 时，新 map_base gate 的 reject 次数均为 `0`。
- `map→odom` 仍通过既有低通与 `max_base_yaw_step_m` 慢释放，不会一次性把放大的 target 写入 TF。

### [RTK Corridor 障碍云隔离与 map->odom target 平滑]

#### 涉及文件

- `src/perception/fastlio2/src/map_builder/commons.h`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/perception/fastlio2/config/lio.yaml`
- `src/bringup/config/master_params.yaml`
- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-CN/commands.md`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/architecture.md`
- `docs-EN/commands.md`
- `docs-EN/knowledge/fastlio2.md`

#### 变更内容

- FAST-LIO2 发布层新增 `/fastlio2/body_cloud_nav2_obstacles`，使用 `[-0.20, 1.20]m` 相对高度窗，专供 Nav2 local costmap 标记/清除障碍。
- 保持 `/fastlio2/body_cloud` 的 `[-0.33, 0.30]m` 低窗不变，PGO/LIO 仍消费原 topic。
- Corridor Nav2 STVL 的 mark/clear source 改为订阅 `/fastlio2/body_cloud_nav2_obstacles`，debug bag profile 同步记录该 topic。
- `rtk_map_odom_corrector` 在 raw RTK target 和现有单步限幅之间加入 target deadband + 低通平滑，默认 `alpha=0.20`、平移死区 `0.05m`、yaw 死区 `0.25deg`。
- 增加 corridor/FAST-LIO2/RTK authority 合同测试，防止后续误把 Nav2 costmap 又接回低矮点云或移除 target 平滑。

#### 变更原因

- 近期实车中用户站在车前但车辆没有避障，根因之一是 Nav2 复用了给 PGO/LIO 稳定性调低的 `/fastlio2/body_cloud`，而该云发布前已经裁到 `0.30m` 以下，站立行人的主体点无法进入 costmap。
- 后段“画龙”更像是小幅 RTK/heading target 抖动持续写入 `map→odom`，仅靠单步限幅会让 TF 每帧小幅追噪声；target 进入限幅前需要先做死区和低通。

#### 带来影响

- Corridor local costmap 能看到更完整的站立行人和户外障碍，而 PGO/LIO 的轻量低窗点云不被放宽，避免建图/图优化负载和高处杂点风险一起上升。
- RTK authority 仍是唯一 `map→odom` owner，但小幅全局 target 抖动会被吸收，预期减少后段左右摆动。
- 若下一次仍不避障，应使用 `FYP_CORRIDOR_BAG_PROFILE=debug` 检查 `/fastlio2/body_cloud_nav2_obstacles` 与 local costmap 标记；若仍画龙，再重点看 `/localization_authority/diagnostics` 中 raw target jump 与输出 TF 的关系。

## 2026.07.08

### [Makefile 与 bashrc 改善]

#### 变动文件

- `Makefile`
- `docs-CN/commands.md`
- `docs-CN/handoff/rtk_test.md`
- `docs-EN/commands.md`
- `docs-EN/handoff/rtk_test.md`
- `scripts/.bashrc`

#### 变更内容

- 将 `source /opt/ros/humble/setup.bash && colcon build ...` 提取到文件顶部的单个变量 `COLCON_BUILD` 中
- 用模式规则（pattern rules）替换了重复的启动目标（launch targets）
- 添加了 `build-bringup` 和 `build-fastlio2`，用于根据最新更改构建这些功能包
- 更新了 `.bashrc` 中的别名（如 `ss`），以便快速 source 工作空间
- 在 `.bashrc` 中添加了 `mbuild` 函数，用于运行 `make build-*` 并在构建完成后自动 source
- 简化了命令文档，删除了不必要的命令
- 将 bashrc 文件名更改为 `.bashrc`（之前为 `bashrc.sh`）

#### 修改原因

- Makefile 中遵循相同模式的重复命令太多，显得过于杂乱；新的布局更加整洁，也更容易修改
- 新增的别名（如 `s1`、`ss`、`gs`、`rc`）涵盖了一些常用命令，避免每次都需要输入长串命令
- 以前在机器人上运行任何测试都需要重复执行相同的命令，例如 `cd ~/XJTLU-autonomous-vehicle`、`source /opt/ros/humble/setup.bash` 和 `source install/setup.bash`。新的更改会自动运行它们，以避免繁琐的操作

#### 带来影响

- 添加新的启动模式（launch modes）不再需要修改 Makefile
- 运行 `ss` 等同于 source ROS2 Humble 环境和 `install/setup.bash`
- 启动任何模式都不再需要切换文件夹或 source 工作空间
- 使用 `mbuild` 构建后，不再需要手动执行 source 命令

### [Git 命令]

#### 变动文件

- `docs-EN/handoff/rtk_test.md`
- `docs-CN/handoff/rtk_test.md`

#### 变更内容

添加了普通的 Git 命令来切换 branch，然后 Jetson 上根据新内容 rebuild 的命令。

### [FAST-LIO2 户外点云结构保留调参]

#### 涉及文件

* `src/bringup/config/master_params.yaml`
* `src/perception/fastlio2/config/lio.yaml`
* `src/bringup/test/test_system_gps_corridor_launch.py`
* `docs-CN/knowledge/fastlio2.md`
* `docs-EN/knowledge/fastlio2.md`

#### 变更内容

* FAST-LIO2 户外主配置 `lidar_filter_num` 从 `6` 调到 `4`，`lidar_max_range` 从 `15.0m` 调到 `25.0m`。
* 同步更新 package fallback `fastlio2/config/lio.yaml`，避免单独 launch FAST-LIO2 时回落到旧稀疏档。
* 增加 corridor launch 合同测试，要求 master 与 fallback FAST-LIO2 配置保留足够户外结构点。

#### 变更原因

* 近期 corridor bag 中一帧点云经过 FAST-LIO2 输入过滤后只剩几百点，广场区域远处可匹配结构本来就少，旧的 `6/15m` 档容易进一步减少有效平面/边缘约束。
* 本次先不改 Livox 硬件时间戳路径，避免把 CPU 过载、时间同步和点云密度三个变量混在一起。
* 由于 Jetson 仍可能存在 CPU 压力，本次采用中等增强 `4/25m`，而不是直接使用更重的 `3/25m`。

#### 带来影响

* 户外 corridor 会保留更多远处墙、树、柱等可匹配结构，降低 `NO Effective Points` 和 LIO 退化概率。
* FAST-LIO2 计算量会比旧配置上升，但比 `lidar_filter_num: 3` 更保守；下一次实车应同时观察 `/fastlio2/degeneracy`、FAST-LIO2 日志中的 IMU 样本数和 tegrastats。

### [RTK Corridor runtime 清理与单一 robot_description]

#### 涉及文件

* `Makefile`
* `scripts/launch_with_logs.sh`
* `src/bringup/launch/system_gps_corridor.launch.py`
* `src/bringup/test/test_system_gps_corridor_launch.py`

#### 变更内容

* `make kill` / `make kill-runtime` 的 INT 与 KILL 两段 `pkill` 列表补齐 `[r]tk_map_odom_corrector`、`[s]erial_reader_node`、`[j]oint_state_publisher`、nav-gps 的 `gps_anchor_localizer` / `route_server` / `goal_manager_node`，以及 slam 的 `async_slam_toolbox_node` / `map_saver_server`。
* `scripts/launch_with_logs.sh` 启动前与退出时的 runtime 清理同样补齐这些易残留节点。
* `system_gps_corridor.launch.py` 不再重复 include `robot_description.launch.py`，改为只使用内层 `system_explore.launch.py` 已经启动的 robot description。
* 增加 corridor runtime 清理与单一 robot description 合同测试，防止 RTK authority、底盘反馈、joint state 或跨模式导航节点再次漏出清理列表。

#### 变更原因

* 2026-07-08 18:00、18:02、18:07 的 corridor 日志显示，旧 `rtk_map_odom_corrector` 在 session 结束后仍继续运行并发布 `map -> odom`。
* 最新一次运行时至少有三个 corrector 同时存活，导致 `map -> odom` 抢发布和米级 TF 跳变，最终在第 4 段触发 `ODOM_DIVERGENCE_ABORT|STEP_1.08m_IN_0.00s`。
* 二次检查发现 corridor 通过 explore/robot_description launch 还会启动 `serial_reader_node` 与 `joint_state_publisher`；前者属于底盘反馈链路，残留后会污染下一轮运行的 runtime 状态。
* corridor 外层 launch 又单独 include 了一次 `robot_description.launch.py`，实际会和 explore 内层的 robot/joint state publisher 重复；虽然静态 TF 内容相同，一般不会直接导致右转，但会让 ROS graph、`/tf_static` 和 joint state 发布关系更混乱。
* 切换 nav-gps 或 slam 后再跑 corridor 时，旧的 route/map/slam 相关节点如果没被清理，也可能让 action server、地图服务或 TF 相关诊断变得不干净。

#### 带来影响

* 每次 `make kill`、`make kill-runtime` 或通过 `launch_with_logs.sh` 启动/退出时，RTK authority 的 `map -> odom` 发布者、底盘反馈节点、joint state 发布节点和跨模式导航残留节点都会被清理。
* corridor 启动时只保留一套 robot description 发布者，降低重复节点名、重复 TF_static 和现场调试噪声。
* 下一次 corridor run 不应再因为旧 corrector 残留而出现多个节点抢发 `map -> odom`，也降低了旧底盘反馈或旧 nav-gps/slam 进程混入新 session 的风险。

### [RTK Corridor 全局规划与局部避障解耦]

#### 涉及文件

* `src/bringup/config/nav2_corridor_rtk.yaml`
* `src/bringup/launch/system_gps_corridor.launch.py`
* `src/bringup/test/test_system_gps_corridor_launch.py`
* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
* `docs-CN/knowledge/nav2_tuning.md`
* `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

* 新增 `nav2_corridor_rtk.yaml` 作为 corridor 专用 Nav2 基底；launch 仍在启动时叠加现有 RTK 小步提速、终点停车保持和无物理 recovery profile。
* `nav2_corridor_rtk.yaml` 补齐 `default_nav_through_poses_bt_xml` 空默认槽位，确保 launch 能注入 corridor 无 physical recovery 的 through-poses 行为树。
* Corridor global costmap 改为只承担 RTK 路线级规划：只启用 `inflation_layer`，关闭 unknown tracking，缩小全局 `robot_radius` / `inflation_radius`。
* Local costmap 继续保留 STVL、FRC 和 inflation，使用真实车体半径与实时 Livox 障碍，保持自动避障能力。
* `gps_route_runner` 默认 RTK 子目标间距从 `30m` 改为 `5m`，减少长目标穿过 rolling window 边缘时的全局规划失败。
* 增加 launch / YAML / route-runner 合同测试，防止 corridor 重新回到 Explore 的重全局障碍配置。

#### 变更原因

* 2026-07-08 corridor 失败更像是 Nav2 global planner 对长 RTK 子目标、全局滚动代价图和 unknown/障碍层组合过敏，而不是 RTK 本身失锁。
* 同日晚间复测显示 `bt_navigator` 仍加载 Nav2 上游 `navigate_through_poses_w_replanning_and_recovery.xml`，并因 corridor 只加载 `wait` behavior 而找不到 `spin` action server；根因是新增 corridor YAML 漏了 through-poses rewrite key。
* 在 RTK authoritative 架构下，全局路径的核心任务是把车稳定推进到下一个 RTK 子目标；实时绕障应由 local costmap + MPPI 处理。
* 若 global costmap 同时承担大范围动态障碍、unknown space 和长距离子目标，很容易在户外广场/边界区域生成不了全局路径，从而在还没进入局部避障前失败。

#### 带来影响

* Corridor 更不容易因为全局 costmap 边缘或未知区域直接 NavFn 失败。
* Nav2 lifecycle 能正确加载 corridor through-poses 无恢复树，不再在启动阶段等待缺失的 `spin` action server。
* 自动避障没有被关闭；Livox/FRC 障碍仍在 local costmap 中参与 MPPI 控制。
* RTK route 会以更短子目标推进，便于后续接室内外切换或 prior-map relocalization 时替换全局 authority，而不改 Nav2 主链路。

## 2026.07.07

### [Jetson ~/.bashrc 环境追踪与优化]

#### 变动文件

- `scripts/bashrc.sh`
- `docs-EN/commands.md`
- `docs-CN/commands.md`

#### 变更内容

* 创建了 `scripts/bashrc.sh`，以便在代码仓库中对 Jetson 的 `~/.bashrc` 配置进行版本控制和追踪
* 自定义了配置内容，使其在终端启动时自动显示机器人当前状态的全面概述
* 增加了常用机器人命令的快捷方式与别名（Aliases）
* 编写了标准化的操作指南，指导如何使用 Vim 安全地覆盖和更新 Jetson 本地的 `~/.bashrc`

#### 修改原因

* Jetson 上的本地环境配置经常因多人直接修改而产生偏差或丢失，导致环境难以在其他设备上复制
* 团队成员需要一种快速、自动化的方式，在通过 SSH 或本地终端登录后，能立即查看机器人的诊断信息和连接状态

#### 带来影响

* `~/.bashrc` 文件现已完全纳入版本控制，所有环境级别的自定义命令均可通过 git 历史记录进行审查和追踪
* 现在打开任何新终端或 SSH 会话时，系统都会向用户展示机器人实时状态的直观摘要
* 规范了开发流程，有效防止在 Jetson 硬件上进行未被追踪的、随意的直接修改

### [Corridor Nav2 启动期 bootstrap TF gate 修复]

#### 涉及文件

* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
* `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
* `docs-CN/knowledge/nav2_tuning.md`
* `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

* RTK authority helper 新增 external alignment / bootstrap alignment 选择逻辑；当 external alignment 尚未出现时，corrector 会复用已经建立的 bootstrap alignment。
* `rtk_map_odom_corrector` 在 bootstrap 阶段允许先发布 `map→odom`，即使 `/rtk/status` 尚未进入 RTK Fixed；该输出仍标记为 `RTK_BOOTSTRAP`，只用于让 Nav2 lifecycle 获得 `map` frame。
* 增加回归测试，覆盖“external alignment 缺失时复用 bootstrap”和“bootstrap TF 可在 Fixed 前发布以支持 Nav2 启动”两个场景。

#### 变更原因

* 2026-07-07 10:11 的 corridor 启动卡在 `GPS 已稳定，等待 Nav2 就绪...`；日志显示 `planner_server` 激活 global costmap 时持续报 `Invalid frame ID "map"`，同时 `gps_global_aligner` 在等待 `map→base_link` 后超时。
* 根因是启动期存在闭环：Nav2 costmap 需要 `map→base_link` 才能完成 lifecycle activation；global aligner 也需要 `map→base_link` 才能发布 external alignment；而 corrector 又在 RTK Fixed gate 之前不发布 bootstrap `map→odom`，导致 `map` frame 从未出现。

#### 带来影响

* Corridor 启动时只要已有有效 fix/heading/odom，就能先发布 bootstrap `map→odom`，避免 Nav2 因缺少 `map` frame 卡死。
* 实际开始跑路线仍由 `gps_route_runner` 的 stable-fix 和 alignment gate 控制；非 Fixed bootstrap 只解决启动 TF 可用性，不放宽路线执行条件。

### [RTK Corridor 小步提速与 yaw 重捕获]

#### 涉及文件

* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
* `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
* `src/bringup/config/master_params.yaml`
* `src/bringup/launch/system_gps_corridor.launch.py`
* `src/bringup/test/test_system_gps_corridor_launch.py`
* `docs-CN/knowledge/nav2_tuning.md`
* `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

* RTK authority 输入 gate 支持 `YAW_REACQUIRE`：当 RTK/heading/alignment 新鲜、平移跳变未超限、yaw target jump 位于 `45deg` 重捕获窗口内时，不再直接冻结 `map→odom`，而是继续按单步限幅释放。
* `rtk_map_odom_corrector` 新增 `allow_yaw_reacquire` 和 `max_yaw_reacquire_jump_deg` 参数，并将 corridor 默认 `max_yaw_step_deg` 从 `1.0` 降到 `0.5`。
* Corridor Nav2 launch-time profile 将 `vx_max` 从 `0.45` 小步提高到 `0.55`，同时将 `wz_max` 从 `0.65` 降到 `0.50`、`wz_std` 从 `0.14` 降到 `0.10`、`az_max` 从 `2.0` 降到 `1.0`，并同步收紧 velocity smoother 的角速度/角加速度限制。
* 增加 RTK yaw 重捕获与 corridor profile 的回归/合同测试。

#### 变更原因

* 2026-07-07 成功跑通的 `2026-07-07-08-48-14` bag 显示运动段 `vx` 中位数 `0.428m/s`，p95/max 已达到旧 `0.45m/s` 上限，因此可以小步提速。
* 同一 bag 中 `|wz|` p95 为 `0.537rad/s`，角速度正负切换约 `0.53次/s`，说明左右摆动主要来自横摆修正过积极，而不是单纯线速度过低。
* bag 中 `TARGET_YAW_JUMP` 窗口达到约 `40deg`，对应横向误差峰值；直接冻结 TF 会让 Nav2 在这段时间继续追一个已滞后的 `map→odom`。

#### 带来影响

* 下一轮 corridor 实车会在直线段略快，但角速度和角加速度更保守，预期减少左右抖动。
* 40 度以内的 yaw target jump 会以更小 yaw step 平滑重捕获；超过窗口或伴随平移大跳变时仍会降级，不会把明显异常的定位输入直接拉进 TF。
* Explore 主线参数不变；该调参只通过 `system_gps_corridor.launch.py` 注入 corridor 临时 Nav2 参数文件。

### [RTK corrector shutdown-safe 发布修复]

#### 涉及文件

* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
* `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`

#### 变更内容

* `rtk_map_odom_corrector_node` 在 timer 回调入口检查 rclpy context 是否仍有效。
* mode/status/diagnostics 发布与 `map->odom` TF 广播改为 shutdown-safe helper，context 已失效时直接忽略 publish。
* 新增回归测试，要求 corrector 保留 shutdown-safe publish 路径。

#### 变更原因

* Jetson 上单节点 smoke test 发现 timeout/launch 退出阶段可能先让 rclpy context 失效，而 timer 仍尝试 publish，触发 `publisher's context is invalid`。
* 该异常不会直接说明定位数学错误，但会在 corridor 超时或正常退出时制造误导性的节点崩溃日志，容易掩盖真实导航状态。

#### 带来影响

* corridor 退出、超时或 monitor 关闭 launch 时，corrector 不再因 shutdown race 抛出 publish 异常。
* 正常运行时的 RTK authoritative 计算、gate、限幅和 TF 发布策略不变。

### [Corridor RTK authoritative `map→odom` 定位层]

#### 涉及文件

* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
* `src/navigation/gps_waypoint_dispatcher/setup.py`
* `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
* `src/bringup/config/master_params.yaml`
* `src/bringup/config/pgo_corridor_no_gps.yaml`
* `src/bringup/launch/system_gps_corridor.launch.py`
* `src/bringup/test/test_system_gps_corridor_launch.py`
* `docs/superpowers/specs/2026-07-07-rtk-authoritative-map-odom-design.md`
* `docs/superpowers/plans/2026-07-07-rtk-authoritative-map-odom.md`
* `docs-CN/architecture.md`
* `docs-EN/architecture.md`
* `docs-CN/knowledge/nav2_tuning.md`
* `docs-EN/knowledge/nav2_tuning.md`

#### 变更内容

* 新增 `rtk_map_odom_corrector_node`，在 corridor 中根据 RTK fix、双天线 heading、`/gps_corridor/enu_to_map` 和当前 `odom→base_link` 计算并发布唯一的 `map→odom`
* corrector 在外部 `ENU→map` 尚不可用时会进入 `RTK_BOOTSTRAP`，用当前 RTK/heading/odom 先发布临时 `map→odom`，避免关闭 PGO TF 后 aligner 无法启动
* 新增 ROS-free `rtk_authority.py`，覆盖 `T_map_odom = T_map_base * inverse(T_odom_base)`、RTK map pose 投影、单步限幅和输入 gate
* corridor 的 PGO override 设置 `publish_tf: false`，避免 PGO 与 RTK authority 同时发布 `map→odom`
* corridor rosbag 增加 `/localization_authority/mode`、`/localization_authority/status` 和 `/localization_authority/diagnostics`
* 设计文档预留后续室内外切换接口：室外 RTK authoritative pose 可作为室内先验地图 relocalization seed，后续 prior-map authority 仍接同一 `map→odom` 接口

#### 变更原因

* 现场判断确认 LIO 约 1m 漂移就足以让 Nav2 的 `map→base_link` 偏离 GPS route 并停止；单纯放宽 Nav2 tolerance 或只给 PGO 加 GPS 因子不足以稳定实车导航
* RTK Fixed + 双天线 heading 在室外比当前空旷区域 LIO 更可信，应作为 corridor 的全局位姿 authority
* PGO 关闭 TF 后，系统需要一个不依赖 aligner 输出的初始 `map→odom` 来让 `gps_global_aligner` 完成 bootstrap
* 后续室内外切换计划依赖“室外准确 RTK 位姿 → 室内先验地图匹配”的 handoff，因此本次不写 corridor 专用 hack，而是引入可扩展 authority 状态与诊断 topic

#### 带来影响

* corridor 模式中 Nav2 看到的 `map→base_link` 会被 RTK authoritative `map→odom` 拉回，不再完全跟随 LIO 的米级全局漂移
* 系统仍保留 FAST-LIO2 的 `odom→base_link` 局部连续性和点云障碍输入
* RTK / heading / alignment stale、非 Fixed 或 target 跳变时，authority 会降级并停止更新，避免 GPS 异常瞬间拉飞 TF
* 后续 prior-map relocalizer 可以复用 `/localization_authority/*` 接口，不需要重写 Nav2 或 route runner 主链路

## 2026.07.06

### [Corridor RTK 导航假成功与 recovery 抢 `/cmd_vel` 修复]

#### 涉及文件

* `scripts/monitor_corridor_status.py`
* `src/bringup/behavior_trees/navigate_through_poses_w_replanning_5hz_no_motion_recovery.xml`
* `src/bringup/behavior_trees/navigate_to_pose_w_replanning_5hz_no_motion_recovery.xml`
* `src/bringup/config/nav2_explore.yaml`
* `src/bringup/launch/system_explore.launch.py`
* `src/bringup/launch/system_gps_corridor.launch.py`
* `src/bringup/test/test_monitor_corridor_status.py`
* `src/bringup/test/test_system_gps_corridor_launch.py`
* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_global_aligner_node.py`
* `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
* `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
* `src/perception/pgo_gps_fusion/src/pgo_node.cpp`
* `docs-CN/architecture.md`
* `docs-EN/architecture.md`

#### 变更内容

* 为 corridor 增加无 `Spin` / `BackUp` 的 NavigateToPose / NavigateThroughPoses 行为树，并让 `system_explore.launch.py` 支持通过 `nav_to_pose_bt_xml` 和 `nav_through_poses_bt_xml` 注入不同 BT
* `nav2_explore.yaml` 补齐 `default_nav_through_poses_bt_xml` 空默认槽位，使 launch 的 through-poses BT rewrite 能真正落到 `bt_navigator`
* corridor 运行时将 Nav2 BT `default_server_timeout` 提高到 1000ms、`bt_loop_duration` 调整为 50ms，并让 `behavior_server` 只加载 `wait`
* `gps_route_runner` 已能识别 Nav2 假成功后，前台 monitor 现在直接显示 `Nav2 误报到达但路线无进展`，不会被 aligner shutdown 噪声盖住
* `gps_global_aligner` 将 rcl context shutdown 视为正常退出，不再发布误导性的 `ALIGNER_ABORTED`
* PGO 在没有新 cloud 时按 timer 重发最近一次 `map→odom`，避免 Nav2 查询当前 TF 时落在 LIO 低频间隙

#### 变更原因

* 2026-07-06 室外 RTK corridor 实测中，Nav2 的 20ms action ack timeout 过短，FollowPath 仍在执行时 BT 已进入 recovery，导致前进、后退和原地旋转命令交错写入 `/cmd_vel`
* 2026-07-06 22:33 实测日志显示 `bt_navigator` 仍加载 Nav2 上游 `navigate_through_poses_w_replanning_and_recovery.xml`；根因是 base YAML 缺少 through-poses rewrite key，导致 RewrittenYaml 无法替换不存在的参数
* 同一轮实测中，PGO `map→odom` 发布频率跟随 LIO 有效帧；LIO 掉帧时 Nav2 controller 查询当前 TF 失败，随后误报 `Reached the goal`
* route runner 检测到 `NAV2_FALSE_SUCCESS_ABORT` 后启动关闭流程，aligner 在 shutdown 中抛出的 rcl context 异常盖住了真正失败原因

#### 带来影响

* corridor RTK 验证时不再执行危险的物理 recovery spin/backup；失败会停车并输出路线进度原因
* NavigateThroughPoses action 也会使用 corridor 无恢复 BT，避免 `behavior_server` 只加载 `wait` 时因缺少 `spin` action server 导致 Nav2 生命周期启动失败
* Nav2 FollowPath 不会因为 Jetson 负载下几十毫秒的 action ack 抖动而误入 recovery
* `map→odom` TF 在 LIO 低频或短暂掉帧时仍保持当前时间可查询，降低 Nav2 false success / transform-too-old 风险
* 前台日志能直接指向 `NAV2_FALSE_SUCCESS_ABORT`，现场排查不会再被“对齐器启动失败”误导

### [NTRIP 账户设置脚本更新]

#### 涉及文件

* `scripts/setup_ntrip.sh`
* `scripts/setup_ntrip.py`
* `Makefile`
* `docs-CN/commands.md`
* `docs-EN/commands.md`
* `~/.bashrc`
* `docs-EN/handoff/rtk_test.md`
* `docs-CN/handoff/rtk_test.md`

#### 变更内容

* 删除了未使用的 `scripts/setup_ntrip.sh`
* 更新了脚本，带来了更直观的登录流程：明确提示输入用户名、密码、IP/挂载点，并会自动测试连接以及自动存储凭据
* 增加了使用更新后的 Python 脚本进行登录的 `make` 启动模式
* 更新了命令文档，补充了每个 `make ntrip-*` 启动命令的具体用途说明
* 修改了 `~/.bashrc`，以便在每次登录终端时显示 NTRIP 的状态

#### 变更原因

* 我们经常需要更换 NTRIP 凭据，而之前的设置由于凭据无法持久化保存，导致每次打开新终端时都需要重新进行身份验证
* 登录命令使用繁琐，且无法立即测试连接状态
* 之前的登录脚本不够好用，因为不同供应商提供的凭据信息格式各不相同，导致旧脚本此前一直处于闲置状态
* 新脚本更加简单易用，只需一个命令即可完成登录，无需在每个新终端中重新 source 环境变量或进行身份验证

#### 带来影响

* 现在所有的 NTRIP 凭据均由 `scripts/setup_ntrip.py` 进行统一处理
* 凭据已在机器人中持久化保存，具体存储在 `~/.ntrip_config.json`、`~/.ntrip_credentials.json`、`/tmp/um982_cors.yaml` 和 `/tmp/ntrip_env.sh` 中
* 现在每次登录终端时都会显示当前 NTRIP 的状态
* RTK 检测命令简单了好多

## 2026.07.02

### [在启动文件中添加 URDF]

#### 涉及文件

* `.gitignore`
* `docs-CN/commands.md`
* `docs-EN/commands.md`
* `src/bringup/config/master_params.yaml`
* `src/bringup/launch/robot_description.launch.py`
* `src/bringup/launch/system_explore.launch.py`
* `src/bringup/launch/system_explore_gps.launch.py`
* `src/bringup/launch/system_gps_corridor.launch.py`
* `src/bringup/launch/system_nav_gps.launch.py`
* `src/bringup/launch/system_rtk_basic.launch.py`
* `src/bringup/launch/system_slam.launch.py`
* `src/bringup/launch/system_travel.launch.py`
* `src/bringup/urdf/rosbot/actuator/caster.urdf.xacro`
* `src/bringup/urdf/rosbot/actuator/wheel.urdf.xacro`
* `src/bringup/urdf/rosbot/base.urdf.xacro`
* `src/bringup/urdf/rosbot/common_inertia.xacro`
* `src/bringup/urdf/rosbot/plugins/gazebo_control_plugin.xacro`
* `src/bringup/urdf/rosbot/rosbot.urdf.xacro`
* `src/bringup/urdf/rosbot/sensor/camera.urdf.xacro`
* `src/bringup/urdf/rosbot/sensor/imu.urdf.xacro`
* `src/bringup/urdf/rosbot/sensor/laser.urdf.xacro`

#### 变更内容

* 添加了前几年开发的局部 URDF 文件
* 添加了新的 `robot_description.launch.py` 启动文件以加载 URDF 文件
* 修改了 `src/bringup/launch/` 中的所有启动文件，使其调用新的 `robot_description` 启动文件
* 修改了 `.gitignore` 以忽略从 Windows 传输文件到 Linux 时自动生成的 `*:Zone.Identifier` 文件
* 在 `src/bringup/config/master_params.yaml` 中，修改了 `body_frame` 参数以使用 `base_footprint`（之前为 `base_link`）

#### 变更原因

* 为了在项目中加入 URDF 文件并使其在 Foxglove 中正常渲染，从而清晰地展示机器人的位置和姿态
* 必须更新 `src/bringup/launch/` 中的所有启动文件，引入 `robot_description` 启动文件来加载 URDF 文件
* 通过 `.gitignore` 忽略对项目没有实质贡献的 `Zone.Identifier` 文件
* URDF TF 树将 `base_footprint` 接入为 `base_link` 的父级。为确保只有一个父级，新的机体坐标系（body frame）必须设为 `base_footprint`。

#### 带来影响

* 可以在 Foxglove 中对机器人进行可视化展示
* `src/bringup/launch/` 中的所有启动文件均已包含 `robot_description` 启动文件
* `.gitignore` 能够成功忽略 `Zone.Identifier` 文件
* TF 树已做更改，机器人坐标系（robot frame）现为 `base_footprint`，而非 `base_link`。

#### 主参数（Master Params）变更说明

* **变更理由**：在新添加的 URDF 结构中，`base_footprint` 作为根关节（root link），代表机器人在地面上的投影，而 `base_link`（物理底盘中心）则是它的子节点。如果 FAST-LIO2 的 `body_frame` 参数保持为 `base_link`（发布 `odom -> base_link`），那么 `base_link` 将拥有多个父节点：`odom`（动态发布）和 `base_footprint`（来自 URDF 的静态发布）。在 ROS TF 树中，多父节点是非法的，会导致坐标系解析崩溃。将 `body_frame` 更改为 `base_footprint` 可以生成一个合法且符合标准的链路：`odom -> base_footprint -> base_link`。
* **下游兼容性**：所有继续查询面向/来自 `base_link` 变换的下游节点（例如 `route_server`、`gps_global_aligner`、`gps_route_runner`、`goal_manager` 以及 Nav2 代价地图层）均不受影响，因为 TF 树会透明地解析该变换。
* **验证情况**：FAST-LIO2 中的高度滤波（`publish_cloud_height_filter_enabled`）直接基于 IMU 状态向量高度，该高度与 `body_frame` 字符串的配置无关，因此点云滤波仍能完全保持正确。

以下是关于此参数变更的详细剖析：

#### 1. 该参数变更影响哪些文件

* **参数源**：[master_params.yaml](/src/bringup/config/master_params.yaml) 是中央参数入口点。
* **雷达里程计（LiDAR Odometry）**：FAST-LIO2 ([lio_node.cpp](/src/perception/fastlio2/src/lio_node.cpp)) 读取该参数。它现在将动态坐标变换发布为 `odom -> base_footprint`（此前为 `odom -> base_link`），并将话题输出 `/fastlio2/body_cloud`、`/fastlio2/body_filtered` 和 `/fastlio2/lio_odom` 标记在 `base_footprint` 下。
* **点云转激光扫描（Pointcloud to Laserscan）**：[pointcloud_to_laserscan_node](/src/bringup/launch/system_slam.launch.py%23L64-L74) 订阅了 `/fastlio2/body_cloud`。由于其 `target_frame` 参数为空（`""`），它会继承输入点云的坐标系 ID（frame ID）。因此，现在发布的 `/scan` 话题带有 `header.frame_id = base_footprint`。
* **下游消费者**：订阅 `/scan` 的节点（如 Nav2 代价地图和 SLAM Toolbox）会查询从 `base_footprint` 到其参考坐标系（`odom`/`map`）的 TF 变换。

#### 2. 是否有可能破坏任何现有功能

**不会，它不会破坏任何现有功能。**

* **TF 连通性**：尽管像 `route_server`、`gps_global_aligner_node.py` 和 `gps_route_runner_node.py` 这样的节点会查询相对于 `base_link` 的变换，但 TF 树依然保持连续。因为 `base_footprint` 和 `base_link` 通过静态变换连接，TF 会动态且透明地解析从 `map`/`odom` 到 `base_link` 的查询。
* **2D 导航/对齐**：base.urdf.xacro 中 `base_footprint` 与 `base_link` 之间的平移纯粹是垂直方向的（$Z \approx 22.9\text{ cm}$，且 $X=0, Y=0, \text{yaw}=0$）。由于 2D 路径规划、对齐和导航节点仅使用 $XY$ 坐标和 $\text{yaw}$ 航向角，因此它们接收到的物理值是完全相同的。
* **点云 Z 轴滤波**：FAST-LIO2 的点云高度滤波（`publish_cloud_height_filter_enabled`）使用 IMU 状态向量的估计高度 `m_kf->x().t_wi.z()` 来计算相对高度，这与 `body_frame` 的字符串配置无关。点云滤波依然完全正确。

#### 3. 新的 TF 树结构如何，以及是否被正确遵循

现在，`robot_state_publisher` 已在所有激活的启动模式中成功加载了 URDF。生成的 TF 树布局如下：

* **无全局地图（基础 / SLAM / 探索 模式）**

```mermaid
graph TD
    odom[odom (FAST-LIO2 世界坐标系)] -->|动态变换| base_footprint[base_footprint (地面投影)]
    base_footprint -->|URDF 静态: base_joint| base_link[base_link (底盘中心)]
    base_link -->|URDF 静态| laser_link[laser_link (激光雷达)]
    base_link -->|URDF 静态| imu_link[imu_link (IMU)]
    base_link -->|URDF 静态| camera_link[camera_link (相机)]
    base_link -->|URDF 静态| wheels[车轮 & 万向轮]
```

* **有全局地图（GPS 导航 / PGO 模式）**

```mermaid
graph TD
    map[map (全局地图坐标系)] -->|动态变换 (PGO)| odom[odom]
    odom -->|动态变换| base_footprint[base_footprint]
    base_footprint -->|URDF 静态| base_link[base_link]
    base_link -->|URDF 静态| links[laser_link / imu_link / camera_link / 车轮]
```

该树被 ROS 2 环境完全遵循，因为它构建了一个从根坐标系（`map` 或 `odom`）一直延伸到叶子节点的、清晰的单父节点树。

#### 4. 我们应该针对该变更可能引发问题的边界情况进行测试吗

* **边界情况 1：独立传感器/里程计测试**：
如果开发人员单独运行 FAST-LIO2（例如 `ros2 launch fastlio2 lio_no_rviz.py`）而没有启动 URDF 节点（`robot_description.launch.py`），则 TF 树将止于 `base_footprint`。任何期待 `base_link` 的下游节点都将发生超时。
*建议*：如果下游模块处于激活状态，在手动测试期间务必将 URDF 发布器与 FAST-LIO2 一同运行。
* **边界情况 2：垂直可视化偏移（“悬空机器人”）**：
FAST-LIO2 使用 IMU 的估计位置来发布 `odom -> base_footprint`（该位置在状态向量中的初始高度为 $Z=0$，但在物理上位于地面上方约 $\approx 30\text{ cm}$ 的激光雷达高度处）。
在 URDF 中，`base_link` 位于 `base_footprint` 上方 $22.9\text{ cm}$ 处，而激光雷达位于 `base_link` 上方 $7\text{ cm}$ 处。
结果导致：
* 在物理世界中，激光雷达位于地面上方 $\approx 30\text{ cm}$ 处。
* 在 TF 树中，激光雷达位于 `base_footprint`（处于 IMU 高度）上方 $29.9\text{ cm}$ 处。
这会导致一个视觉上的伪影：在 RViz/Foxglove 中，机器人的物理模型（底盘/车轮）看起来稍微悬空在点云地面点上方。这纯粹是视觉层面的，并不会影响 2D 导航，但这是将通用 LIO 与自定义 URDF 坐标系结合使用时的一个已知伪影。


#### 5. 鉴于该分支中新添加了 URDF，该变更是合理的吗

**是的，这次变更是 100% 合理且必要的。**
如果 `body_frame` 保持为 `base_link`：

1. `robot_state_publisher` 将发布静态变换 `base_footprint -> base_link`。
2. FAST-LIO2 将发布 `odom -> base_link`。
3. 这意味着 `base_link` 将拥有多个父坐标系（`base_footprint` 和 `odom`），这在 ROS TF 库中是非法的。它会导致严重的抖动、错误以及 TF 树完整性的丧失。

将 `base_footprint` 作为动态里程计变换的目标是移动机器人的标准 ROS 惯例，这样能提供一个干净、合规的坐标树。

### [RTK FGO 诊断与 heading anchor 验证]

#### File / 文件

- `scripts/evaluate_rtk_fgo_bag.py`
- `scripts/test_rtk_fgo_metrics.py`
- `src/bringup/config/rtk_fgo.yaml`
- `src/perception/rtk_fgo_localizer/CMakeLists.txt`
- `src/perception/rtk_fgo_localizer/include/rtk_fgo_localizer/diagnostic_snapshot.hpp`
- `src/perception/rtk_fgo_localizer/include/rtk_fgo_localizer/frame_anchor.hpp`
- `src/perception/rtk_fgo_localizer/include/rtk_fgo_localizer/heading_stability.hpp`
- `src/perception/rtk_fgo_localizer/src/diagnostic_snapshot.cpp`
- `src/perception/rtk_fgo_localizer/src/frame_anchor.cpp`
- `src/perception/rtk_fgo_localizer/src/heading_stability.cpp`
- `src/perception/rtk_fgo_localizer/src/rtk_fgo_node.cpp`
- `src/perception/rtk_fgo_localizer/test/test_diagnostic_snapshot.cpp`
- `src/perception/rtk_fgo_localizer/test/test_frame_anchor.cpp`
- `src/perception/rtk_fgo_localizer/test/test_heading_stability.cpp`
- `docs-EN/knowledge/rtk_fgo.md`
- `docs-CN/knowledge/rtk_fgo.md`

#### Change / 改动

- 为 `/rtk_fgo/factor_diagnostics` 增加结构化字段：RTK GGA quality、收星数、HDOP、position/heading innovation、fix 隐含速度和各输入 age。
- 扩展 `scripts/evaluate_rtk_fgo_bag.py`，在 replay JSON metrics 中汇总这些诊断字段。
- 增加 `FrameAnchor::enuYawToMapYaw()`，在 heading innovation 检查和 yaw factor 插入前，把 RTK heading 旋转到 FGO `map` frame。
- 增加支持 wraparound 的 heading stability 检查，用于 frame-anchor bootstrap，并把 `max_heading_spread_deg` 移到 `frame_anchor` 参数组。
- 合并到 `frc` 时，将 `rtk_fgo.yaml` 和 mapping-session frame check 默认值对齐到新的 `base_footprint` 动态里程计 frame。

#### Reason / 原因

- 回放 `reloc_enu_align_20260629_183500` 发现 heading innovation 在比较 FAST-LIO/FGO map-frame yaw 与原始 RTK ENU yaw；而 RTK position 已经经过 frame anchor 旋转，二者 frame 不一致。
- 同一段回放还显示 anchor bootstrap 时双天线 heading 出现瞬时跳变。YAML 已写了 heading spread 阈值，但节点没有声明、读取或执行该限制。
- 原有 replay metrics 不足以区分收星质量、输入 age、frame 约定和 bootstrap 问题。

#### Effect / 影响

- 干净 replay 指标从 `3` 个 strong candidate、`0` 个 locked 样本、`419` 次 heading reject，提升为 `129` 个 strong candidate、`102` 个 locked 样本、`69` 次 heading reject。
- 稳定 heading bootstrap 后 position innovation 在该 bag 中保持可控（`median 1.38 m`、`max 2.96 m`），避免了错误 anchor 情况下的十几米级偏差（`median 16.25 m`、`max 25.84 m`）。
- FGO 默认仍保持 shadow-only；当前 replay 结果支持继续实车验证，但还不能直接让 FGO 接管生产 `map -> odom`。
- `base_footprint` 合并适配能保证未来实验性 FGO TF 发布兼容 URDF 单父 TF 树，也让地图保存检查与 FAST-LIO2 新 child frame 保持一致。

### [Travel 先验点云地图导航链路恢复]

#### File / 文件

- `docs/superpowers/plans/2026-07-02-prior-pointcloud-travel-navigation.md`
- `scripts/launch_with_logs.sh`
- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/test/test_system_travel_launch.py`
- `src/perception/localizer/launch/localizer_launch.py`
- `src/perception/localizer/src/localizer_node.cpp`
- `src/perception/localizer/test/test_localizer_contract.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/known_issues.md`
- `docs-EN/known_issues.md`

#### Change / 改动

- 恢复 `travel` 为实验性先验地图导航链路：`map_yaml` 注入 Nav2 `map_server`，`pcd_map` 传给 `localizer` 预加载 PCD。
- `localizer` 支持启动时加载 PCD，并保留 `/localizer/relocalize` 服务用于现场重载地图和设置初始位姿。
- `nav2_travel.yaml` 改为由 `localizer` 负责 `map -> odom`，AMCL `tf_broadcast=false`；全局 costmap 恢复 `static_layer` 并关闭 rolling window。
- `launch_with_logs.sh` 支持透传额外 launch 参数，`travel` 也支持 `FYP_USE_RVIZ`。
- 合并到 `frc` 时保留 URDF 启动，Travel TF 链路按 `odom -> base_footprint -> base_link` 与新的机器人模型兼容。

#### Reason / 原因

- Nav2 标准先验地图导航需要 2D OccupancyGrid 做全局规划，但当前需求是用先验 PCD 做上电/运行时重定位；两者需要在 `travel` 中接成同一条链。
- `map -> odom` 只能有一个发布者。Travel 模式必须避免 AMCL、PGO 和 `localizer` 同时抢 TF。
- 旧 `travel` 参数中 `map_server.yaml_filename` 为空，且 `localizer` 只是独立工具，没有接入系统 launch。

#### Effect / 影响

- 现场可用 `bash scripts/launch_with_logs.sh travel map_yaml:=... pcd_map:=...` 启动先验地图链路。
- 运行时 TF 约定为 `localizer: map -> odom`、`FAST-LIO2: odom -> base_footprint`、URDF 静态连接 `base_footprint -> base_link`；PGO 默认关闭，或只以 `publish_tf=false` 辅助可视化/保存地图。
- 本地源码合同测试与 Jetson `localizer`/`bringup` ROS 2 Humble 构建已通过；室内实车先验地图导航仍需继续验证。

### [Travel 运行清理补齐 localizer 与 ros2 launch]

#### File / 文件

- `Makefile`
- `scripts/launch_with_logs.sh`
- `src/bringup/test/test_system_travel_launch.py`

#### Change / 改动

- `make kill-runtime` 的清理正则新增 `[r]os2 launch` 与 `[l]ocalizer_node`。
- `scripts/launch_with_logs.sh` 的启动前/退出清理同样新增 `[r]os2 launch` 与 `[l]ocalizer_node`。
- 增加测试覆盖 travel 运行清理必须包含直接 `ros2 launch` 进程和 `localizer_node`。

#### Reason / 原因

- 实车 travel 测试中发现旧 `system_slam.launch.py`、旧 `system_travel.launch.py` 和多个 `/localizer/localizer_node` 可同时残留。
- 残留 localizer 会导致 ROS graph、service 和 TF 行为混乱；Nav2 controller 日志出现 `base_link -> odom` TF 不存在后，navigation lifecycle 被关闭，RViz 点目标后车辆不动。

#### Effect / 影响

- 后续 `make kill-runtime` 和 `scripts/launch_with_logs.sh` 启动前清理会覆盖 travel localizer 与直接启动的 ROS launch 父进程。
- 降低现场反复切换 slam/travel 后出现重复节点、旧 TF 发布者和 Nav2 不执行目标的概率。

### [Travel localizer 抑制未验证与旧时间戳 TF]

#### File / 文件

- `src/perception/localizer/src/localizer_node.cpp`
- `src/perception/localizer/config/localizer.yaml`
- `src/perception/localizer/test/test_localizer_contract.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change / 改动

- `localizer` 启动预加载 PCD 后不再立即广播 `map -> odom`，必须等待 `/localizer/relocalize` 成功。
- 移除 timer 非更新周期对同一 `last_message_time` 的重复 TF 广播。
- 增加 `max_tf_input_age_s`，跳过明显过期输入 stamp，并记录最后一次已发布 TF stamp，避免重复/倒退的 `map -> odom`。
- 更新 Travel 运行说明，明确先验证 `odom -> base_footprint`，再 relocalize，最后检查 `map -> odom`。

#### Reason / 原因

- 现场日志出现 `TF_OLD_DATA ignoring data from the past for frame odom at time 1782993113.484190`，同时 `tf2_echo odom base_link` 显示 FAST-LIO 当前 TF 已恢复到小幅米级变化。
- 根因是 `localizer` 在未完成重定位时也会发布 `map -> odom`，且 10ms timer 会在 1Hz ICP 更新间隔内重复广播同一旧 stamp，污染 Nav2 TF buffer。

#### Effect / 影响

- Travel 启动后不会再用未验证的 identity/旧 stamp `map -> odom` 抢占 TF。
- 操作顺序更严格：启动 travel -> 验证 FAST-LIO `odom -> base_footprint` -> 调 `/localizer/relocalize` -> `relocalize_check` 为 true -> 再发 Nav2 goal。

### [RTK Corridor 低速验收 profile 与诊断 bag 补充]

#### File / 文件

- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/package.xml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- `system_explore.launch.py` 新增 `nav2_params_file` launch 参数，允许上层模式覆盖 Nav2 参数源。
- `system_gps_corridor.launch.py` 启动时从 `nav2_explore.yaml` 生成临时 Nav2 参数文件，注入 RTK corridor 低速验收 profile：`vx_max=0.45`、`wz_max=0.65`、`controller_frequency=20.0`、`batch_size=500`，并同步收紧 velocity smoother。
- Corridor rosbag 记录新增 `/livox/lidar`、`/livox/imu`、`/fastlio2/body_cloud`。
- 增加 launch 合同测试，确保 corridor profile 覆盖项和新增 bag topic 不被后续改动移除。

#### Reason / 原因

- 2026-07-06 室外 RTK corridor rosbag 中 `/rtk/status` 持续为 `RTK Fixed q=4`，说明定位质量不是首要问题。
- 同一 bag 中轨迹向右偏移，且 `/fastlio2/lio_odom` 出现约 `1.18m/0.19s` 跳变；首次验收需要降低控制速度和角速度，避免把 LIO 退化与控制器调参混在一起。
- 2026-07-06 20:06 现场日志显示 `controller_frequency=15Hz` 会让 MPPI 因控制周期大于 `model_dt=0.05s` 而配置失败，Nav2 lifecycle abort 后 `navigate_to_pose` action server 不可用。
- 原 bag 缺少原始 Livox 点云、IMU 和 body cloud，不利于回放定位退化原因。

#### Effect / 影响

- `corridor` 模式保留 Explore MPPI 结构，但默认使用更保守的 RTK 低速验收参数。
- 后续外场测试的 bag 会包含 RTK、FAST-LIO2 odom、Livox 原始输入和 body cloud，方便判断右偏来自 RTK、LIO 还是 Nav2 控制。
- Explore 主线速度参数不变；该 profile 仅由 `system_gps_corridor.launch.py` 注入。

### [RTK Corridor 下发路线目标前等待 Nav2 lifecycle active]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/nav2_lifecycle_ready.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_nav2_lifecycle_ready.py`
- `src/navigation/gps_waypoint_dispatcher/package.xml`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change / 改动

- `gps_route_runner` 现在会同时等待 `navigate_to_pose` action server，以及核心 Nav2 lifecycle 节点（`controller_server`、`planner_server`、`behavior_server`、`bt_navigator`）均进入 `active`。
- 新增 lifecycle readiness helper，并增加回归测试覆盖 `bt_navigator` 尚未 active 时不能认为 Nav2 ready 的情况。
- 显式增加 `lifecycle_msgs` 包依赖。

#### Reason / 原因

- 2026-07-06 20:14 现场日志显示，`gps_route_runner` 在 `1783340067.215` 已发送第一个 corridor subgoal，但 `bt_navigator` 到 `1783340067.248` 才开始 activating。
- 同一 bag 中 `/plan` 数量为 `0`，`/cmd_vel` 数量也为 `0`，说明失败发生在全局规划和控制之前。
- 仅等待 action server 不够严格，因为 action server 可被发现时，Nav2 lifecycle 激活序列可能还没完成。

#### Effect / 影响

- Corridor 路线目标不会再抢在 Nav2 lifecycle 最后激活窗口内下发。
- 由该启动 race 导致的第一个 waypoint 失败，预期会变成 `WAITING_FOR_NAV2` 阶段多等片刻，而不是 `FAILED_WAYPOINT_*_STATUS_6`。

### [RTK Corridor 在 map/GPS 发散时 abort，避免假成功]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/bringup/config/master_params.yaml`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change / 改动

- 新增路线安全检查：将当前 RTK `/fix` 通过当前 ENU-to-map alignment 投到 `map`，并与 `map -> base_link` 对比。
- 新增参数 `map_gps_divergence_warn_m=2.0` 与 `map_gps_divergence_abort_m=5.0`。
- 若偏差超过 abort 阈值，`gps_route_runner` 发布 `MAP_GPS_DIVERGENCE_ABORT|...`，发送零 `/cmd_vel`，并退出；不再把 waypoint 标成 reached。

#### Reason / 原因

- 2026-07-06 20:26 日志显示 runner 在没有发布 `/gps_corridor/goal_map`、`/gps_corridor/path_map`、`/plan`、`/cmd_vel` 的情况下直接报 `WAYPOINT_REACHED`。
- route 本身有效：`start_ref -> waypoint` 约 `13.69m`，启动 RTK 距 `start_ref` 约 `0.91m`。
- 真正问题是 FAST-LIO/TF 发散：最新 `odom -> base_footprint` 约为 `(582.57, -870.68, -2667.62)`，而 RTK 仍在 route 起点附近，导致基于投影的 progress 被夹到 segment 终点。

#### Effect / 影响

- Corridor 模式在定位已明显飞走时不会再报告假成功。
- 下一次现场若 FAST-LIO 再发散，会明确显示 map/GPS divergence 状态，便于把定位问题与 route runner/Nav2 问题分开。

### [RTK Corridor 轻量 bag 与 Livox 逐包日志开关]

#### File / 文件

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/sensor_drivers/livox_ros_driver2/src/lddc.cpp`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- Corridor 自动录包默认改为 lean profile，仅包含 RTK、FAST-LIO2 odom、TF、corridor 状态、目标、costmap、`/cmd_vel` 和 `/plan`。
- 原始 `/livox/lidar`、`/livox/imu` 与 `/fastlio2/body_cloud` 录包改为通过 `FYP_CORRIDOR_BAG_PROFILE=debug` 显式开启。
- Livox 逐包 console 与 CSV 日志现在由 `LIVOX_VERBOSE_PACKET_LOGS` 控制；`log_switch.yaml` 缺失或读取失败时不再默认开启逐包日志。
- 增加 launch/source 合同测试，覆盖 lean bag profile 与 Livox 逐包日志开关。

#### Reason / 原因

- 2026-07-06 20:40 corridor 已经进入 Nav2 并发布 `/cmd_vel`，但随后因 odom watchdog 检测到约 `0.79m/0.017s` 与 `1.10m/0.01s` 的跳变而 abort。
- 同一轮日志显示 Nav2 控制循环超时、BT tick-rate 超时、TF cache drop，tegrastats 中 4 个活动 CPU 核接近 99-100%。
- 即使 `runtime-data/config/log_switch.yaml` 已设置 `livox_ros_driver2.enable_logging: false`，`launch_stdout.log` 仍有逐包 `Publishing IMU data`、点云 dump 和 `Pointcloud log file is not open!` 刷屏。
- 验收跑车时录原始 Livox 数据会额外增加序列化与 I/O 压力，正好挤占 FAST-LIO2 和 Nav2 最需要的实时余量。

#### Effect / 影响

- 正常 corridor 验收保留必要安全与复盘信息，同时降低 Jetson 的 stdout、录包和磁盘压力。
- 需要原始 Livox 回放时仍可显式使用 `FYP_CORRIDOR_BAG_PROFILE=debug`；短时间台架逐包诊断仍可用 `LIVOX_VERBOSE_PACKET_LOGS=1`。
- 后续类似 CPU 饱和回归会先被 corridor launch 合同测试拦住，而不是到外场才发现。

### [RTK Corridor 新鲜 TF 闸门与 FAST-LIO2 低 IMU 丢帧]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/perception/fastlio2/src/map_builder/commons.h`
- `src/perception/pgo_gps_fusion/src/pgo_node.cpp`
- `src/bringup/config/master_params.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/knowledge/fastlio2.md`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change / 改动

- `gps_route_runner` 新增 `tf_pose_max_age_s=0.75`；route start、subgoal 生成和 odom watchdog 检查前都会忽略过旧的 `map -> base_link`。
- 新增纯函数 `summarize_tf_freshness()` 与回归测试，避免旧 TF 样本被当作当前车辆位姿。
- FAST-LIO2 新增 `min_imu_samples_per_lidar=3`；同步包中的 IMU 样本数不足时，在进入 `m_builder->process()` 前直接丢弃该 LiDAR 帧。
- `pgo_gps_fusion` 在 `log_switch.yaml` 缺少 `pgo_node` 时默认安静，硬编码 `[DIAG]` stderr 输出改为 `PGO_VERBOSE_DIAG=1` 后才开启，并对逐帧 synced-cloud INFO 日志做节流。

#### Reason / 原因

- 2026-07-06 21:02 corridor bag 中 `/plan=0`，`/cmd_vel` 只有 4 条；Nav2 明确报 `worldToMap failed` 和 `The goal sent to the planner is off the global costmap`。
- 同一时刻 RTK 投到 map 仍在 `(0,0)` 附近，但 FAST-LIO2/TF 在发路线目标前已经漂到约 `(-50,-20)`。
- 车辆底盘串口反馈速度为 0 时，FAST-LIO2 仍处理了 `0 IMU samples, 925 LIDAR points`、`1 IMU samples, 1086 LIDAR points` 等 IMU 约束不足的同步包。
- route runner 在等待启动 TF 时可能接受旧 TF 样本，随后在发出 Nav2 goal 后才看到几十米级 TF 跳变。

#### Effect / 影响

- Corridor 模式在只有旧 TF 或 live TF 与 RTK 明显发散时，会在下发目标前失败，而不是把滚动 global costmap 外的目标交给 Nav2。
- FAST-LIO2 不再用 IMU 支撑不足的 LiDAR 帧推进滤波器，降低静止状态下由 LiDAR/IMU 同步异常导致的 odom 飞走概率。
- PGO 诊断输出仍可通过 `PGO_VERBOSE_DIAG=1` 做台架调试，但默认不再刷现场运行日志。

### [RTK Corridor TF-Stale 宽限窗口]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/bringup/config/master_params.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change / 改动

- 将 corridor 的 `tf_pose_max_age_s` 从 `0.75` 调整为 `3.0`。
- 新增 `odom_watchdog_tf_stale_abort_s=3.0`；TF stale watchdog 现在需要同时满足连续 stale 次数和连续 stale 持续时间才取消 Nav2。
- 新增 `summarize_tf_watchdog_gap()` 测试，避免 FAST-LIO2 短暂 TF 空窗导致刚接受的 Nav2 goal 被立刻取消。
- 将 FAST-LIO2 的 `Processing sync package` 日志移动到低 IMU 丢包 guard 之后，避免已丢弃的包被误报为已处理。

#### Reason / 原因

- 2026-07-06 21:27 在 `1b895e9` 上的 corridor 运行不再出现旧的 costmap 外起点：Nav2 从 `(0.02, -0.01)` 开始，并产生了 `/plan=1`。
- 但 route 仍失败，因为 runner 在约 `0.29s` 后以 `ODOM_DIVERGENCE_ABORT|...|TF_STALE` 取消了 goal。
- FAST-LIO2 日志显示启动期间存在短暂 LiDAR/IMU 同步空窗和被丢弃的同步包，因此 `0.75s` 的 freshness 窗口加上只按次数判断的 TF stale abort 对现场条件过于激进。

#### Effect / 影响

- Corridor 模式保留旧 TF 安全闸门，但在刚发 goal 后遇到短暂 FAST-LIO2 TF 更新空窗时不会立刻 abort。
- 如果 TF 持续数秒不恢复，watchdog 仍会取消 Nav2 并发布零 `/cmd_vel`。
- 现场日志现在能区分真正进入 FAST-LIO2 滤波的同步包和因 IMU 样本不足而提前丢弃的包。

### [FAST-LIO2 拒绝无 LiDAR 修正的 IMU-only 预测]

#### File / 文件

- `src/perception/fastlio2/src/map_builder/ieskf.h`
- `src/perception/fastlio2/src/map_builder/ieskf.cpp`
- `src/perception/fastlio2/src/map_builder/lidar_processor.h`
- `src/perception/fastlio2/src/map_builder/lidar_processor.cpp`
- `src/perception/fastlio2/src/map_builder/map_builder.h`
- `src/perception/fastlio2/src/map_builder/map_builder.cpp`
- `src/perception/fastlio2/src/lio_node.cpp`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/fastlio2.md`
- `docs-EN/knowledge/fastlio2.md`

#### Change / 改动

- `IESKF::update()` 现在返回该帧是否有有效 LiDAR 量测更新。
- `LidarProcessor::process()` 在更新无效时跳过增量建图。
- `MapBuilder::process()` 在 mapping 阶段 LiDAR 更新无效时恢复到预测前状态和协方差。
- `lio_node` 对被拒绝的帧不发布 TF/odom/cloud，并在 mapping 已开始后输出节流 warning。

#### Reason / 原因

- 2026-07-06 21:40 在 `269c308` 上的 corridor 运行被 route safety 安全拦在下发目标前，但 FAST-LIO2 已经在没有非零 `/cmd_vel` 的情况下从接近 0 漂到约 `(-503, -88, -306)`。
- 日志显示反复出现 `NO Effective Points` 和稀疏的有效 LiDAR 修正。旧流程在 LiDAR 修正无效时仍保留 IMU 预测，并把该预测状态作为正常 TF/odom 发布给下游。
- route safety 已经正确以 `MAP_GPS_DIVERGENCE_ABORT` 停止路线，但底层 LIO 仍需要停止把 IMU-only 漂移当作定位输出。

#### Effect / 影响

- 在低特征或同步不良的启动阶段，如果 LiDAR 修正无效，FAST-LIO2 不再用 IMU-only 预测推进已发布的 TF/odom。
- Corridor route start 应等待真正被 LiDAR 修正过的 `map -> base_link`，而不是收到物理上不可能的 FAST-LIO2 漂移位姿。

### [RTK Corridor Nav2 success 二次验证]

#### File / 文件

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- Corridor 生成 Nav2 参数文件时强制 `general_goal_checker.stateful=false`。
- `gps_route_runner` 现在会在 Nav2 返回 `SUCCEEDED` 后复核物理路线进度；如果仍明显没到 subgoal，则发布 `NAV2_FALSE_SUCCESS_ABORT`，发送零 `/cmd_vel` 并停止路线。
- 增加 Nav2 参数重写和 route-runner success guard 的回归/源码契约测试。

#### Reason / 原因

- 2026-07-06 21:51 在 `2880423` 上的 corridor 运行已经进入路线执行并下发第一个 14m subgoal，但 Nav2 多次记录 `Reached the goal!`，而下一次 `Begin navigating from current location` 仍显示车辆在起点附近。
- 反复假成功导致 runner 多次重发同一个 subgoal，随后出现不稳定控制命令和 odom watchdog abort。

#### Effect / 影响

- Nav2 goal checker 的旧状态/到点锁存不再能让远距离 corridor subgoal 立即完成。
- 如果 Nav2 仍然在没有实际路线进度时返回成功，corridor 会以明确的 `NAV2_FALSE_SUCCESS_ABORT` 安全失败，而不是进入重发循环。

### [RTK Corridor 终点零速度保持与 0.65m/s 小步提速]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/config/master_params.yaml`
- `scripts/monitor_corridor_status.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-EN/commands.md`
- `docs-CN/commands.md`
- `docs-EN/knowledge/gps_planning.md`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/nav2_tuning.md`
- `docs-CN/knowledge/nav2_tuning.md`

#### Change / 改动

- `gps_route_runner` 新增 `terminal_stop_hold_s=1.2` 和 `terminal_stop_publish_hz=20.0`。
- 最终发布 `SUCCEEDED` 前，runner 先发布 `STOPPING_BEFORE_EXIT`，按配置保持零 `/cmd_vel`，再发布 `SUCCEEDED`；部分终止失败路径也会在终止状态前先做零速度保持。
- quiet monitor 新增该刹停保持状态的中文提示，避免现场看起来像卡住。
- corridor 线速度档从 `vx_max=0.55` 小步提升到 `0.65`，并同步 `vx_std=0.18`、`ax_max=0.70`、`ax_min=-1.2`，velocity smoother 限幅改为 `[0.65, 0.0, 0.50]`、`[0.70, 0.0, 0.9]`、`[-1.2, 0.0, -1.0]`；角速度限制继续保持保守。

#### Reason / 原因

- 2026-07-07 10:39 的成功 corridor bag 中，`/rtk/status` 全程 RTK Fixed，路线大部分时间已经顶到旧的 `0.55m/s` 上限，同时 yaw 命令已经比较平滑（`|wz|` p95 约 `0.094rad/s`）。
- 同一个 bag 显示 Nav2 宣布成功时终点前向速度仍在衰减：成功前 33ms 仍有 `0.073m/s`，成功后 17ms 仍有 `0.028m/s`，随后 quiet launch 退出前零速度尾巴很短。
- `monitor_corridor_status.py` 一看到 `SUCCEEDED` 就会退出并杀掉 launch，因此任何放在成功状态之后的刹停保持都会被过早打断；保持必须放在终止成功状态之前。

#### Effect / 影响

- 下一次 corridor 实车运行时，`/cmd_vel` 和 `serial_twistctl` 应该在 launch wrapper 拆节点前留下明显的 1.2s 零速度尾巴。
- 小车获得温和的直线速度提升，同时保留 10:39 成功运行里比较平滑的 yaw 行为。
- 改动只影响 corridor 生成的 Nav2 profile 和 `gps_route_runner` 终止行为；Explore、Travel、nav-gps 的参数文件不在本条目中重调。

### [RTK Corridor PGO TF owner 双保险关闭]

#### File / 文件

- `src/bringup/launch/system_explore.launch.py`
- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/config/pgo_corridor_no_tf.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change / 改动

- Explore launch 新增 `pgo_config_file` 参数，并透传到 PGO launch 的 `pgo_config`。
- Corridor launch 现在给 PGO 同时传入 legacy 扁平配置 `pgo_corridor_no_tf.yaml` 和现有 ROS 参数 override `pgo_corridor_no_gps.yaml`。
- 新增 `pgo_corridor_no_tf.yaml`，显式设置 `publish_tf: false`、`gps.enable: false`，让 corridor 下的 `map -> odom` 只由 `rtk_map_odom_corrector` 发布。
- 扩展 corridor launch 测试，覆盖 PGO legacy config 与 extra params 两条 override 通道。

#### Reason / 原因

- 2026-07-07 17:13、17:19、17:23、17:30 的 corridor bag 显示，RTK 多数或全部为 fixed，FAST-LIO2 `/fastlio2/lio_odom` 没有大步跳变，但 `/tf` 中 `map -> odom` 出现毫秒级互相冲突跳变。
- 17:30 session 中 `/rtk/status` 全部 `q=4`，`/fastlio2/lio_odom` 没有 0.5m 以上跳变，但 `/tf` 里 `map -> odom` 在 30ms 内有 44 次 0.15m 以上冲突，最大约 0.64m。
- 这会让 Nav2 看到的 `map -> base_link` 来回跳，进而触发 planner 失败或 route runner 的 odom watchdog。

#### Effect / 影响

- Corridor 模式下 PGO 不再有机会和 RTK authority 抢 `map -> odom`。
- 如果 Jetson 上仍出现同样的 TF 冲突，下一步优先检查是否未重新 build/source，或是否有其他旧节点残留发布 `map -> odom`。

### [RTK Corridor 降级时保活 map->odom]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change / 改动

- `rtk_map_odom_corrector` 新增降级 hold 行为：当输入质量、RTK fixed 状态、无效 fix/heading 或目标/yaw jump 被 gating 拒绝时，不更新全局位姿，但继续复播最后一次可信 `map -> odom`。
- 增加源码契约测试，覆盖四类降级退出路径必须调用最后可信 TF 复播逻辑。
- 架构文档补充 RTK degraded hold 语义。

#### Reason / 原因

- 2026-07-08 15:42 corridor run 已进入路线执行，第一个目标为 `(x=7.39, y=13.72)`，随后 `gps_route_runner` 报 `ODOM_DIVERGENCE_ABORT|TF_STALE_3.00s` 并落到 `FAILED_WAYPOINT_1_SUBGOAL_1_STATUS_6`。
- rosbag 中 `odom -> base_link` 继续发布，但 `map -> odom` 在 RTK/yaw 被 `TARGET_YAW_JUMP` gating 拒绝后停止刷新，Nav2 controller 看到的是 map/odom 变换 extrapolation failure。

#### Effect / 影响

- RTK 或 heading 瞬时不可信时，corridor 不会把坏样本写进全局 pose，也不会让 `map -> odom` 因停止广播而过期。
- Nav2 在短时 RTK 降级期间仍能基于连续 TF 树继续局部控制；如果车辆实际不动，后续仍会由 progress/odom watchdog 以更准确的原因失败。

### [RTK Corridor Nav2 成功复核短缺容差]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/gps_route_runner_node.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/route_safety.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_route_safety.py`
- `docs-CN/knowledge/gps_planning.md`
- `docs-EN/knowledge/gps_planning.md`

#### Change / 改动

- 新增 `nav2_success_shortfall_tolerance_m` 路线参数，默认 `max(0.75, waypoint_xy_tolerance_m)`。
- Nav2 `SUCCEEDED` 后的路线进度复核改用独立短缺容差；失败状态会额外输出 `tolerance=...`，方便现场判断短缺和阈值的关系。
- 增加纯逻辑测试，覆盖 `shortfall=0.48m` 应通过、`shortfall=13.71m` 仍应失败。

#### Reason / 原因

- 2026-07-08 16:08 corridor run 已到达第 1/6 个点，第 2/6 个点实际进度为 `15.29m / 15.77m`，只差 `0.48m`，但旧 guard 复用 `waypoint_xy_tolerance_m=0.35m`，导致 `NAV2_FALSE_SUCCESS_ABORT`。
- 该 guard 最初用于拦截 `progress=0.00m`、十几米 shortfall 的假成功；把同一个 0.35m 用在沿线投影短缺上，会比 Nav2 的圆形 goal checker 和 RTK/aligner 小幅跳变更严格。

#### Effect / 影响

- 接近目标的正常 Nav2 success 不会因为 0.5m 以内的沿线投影短缺被误杀，路线可以继续进入后续 waypoint。
- 真正无进展或米级短缺的 false success 仍会被 `NAV2_FALSE_SUCCESS_ABORT` 停住。

### [RTK Corridor 直角弯原地转头 profile]

#### File / 文件

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- Corridor 生成 Nav2 参数时，将 progress checker 从默认 `0.15m / 10s` 调整为 `0.10m / 15s`，允许直角弯处短时间原地调车头。
- 将 corridor MPPI profile 从 `vx_max=0.65`、`wz_max=0.50`、`az_max=1.0` 调整为 `vx_max=0.75`、`wz_max=0.90`、`az_max=2.6`。
- 将采样标准差调整为 `vx_std=0.20`、`wz_std=0.24`，让 MPPI 更容易采到有效原地转头轨迹。
- 同步 velocity smoother 限幅为 `[0.75, 0.0, 0.90]`、`[0.75, 0.0, 2.0]`、`[-1.2, 0.0, -2.2]`。

#### Reason / 原因

- 2026-07-09 11:14 和 11:16 的 route `2` 中，第一个子目标到达后第二段需要接近 90 度转向；Nav2 在 10s 内几乎只发原地慢转，`/cmd_vel` 与 `serial_twistctl` 平均 `vcx≈0.001m/s`、`wc≈0.13rad/s`。
- 同一窗口内 `/fastlio2/lio_odom` 只移动约 `0.10-0.14m`，随后 `controller_server` 报 `Failed to make progress`。
- 车体硬件是 90 度全向轮，但当前上位机到 STM32 的协议仍只有 `vcx,wc`，不包含横向 `vcy`；本轮目标不是横移过弯，而是让现有 `vcx,wc` 链路能更果断地原地调正车头。

#### Effect / 影响

- 直角弯处 MPPI 更容易选择较大的 `wc` 原地转头命令，并且 progress checker 不会在合理转向时间内过早失败。
- 直线速度从 `0.65m/s` 小步提升到 `0.75m/s`，但线加速度仍保持温和，避免恢复到 Explore 的 `1.0m/s` 激进档。
- 改动仍只影响 corridor 生成的 Nav2 profile；Explore、Travel、nav-gps 和 STM32 协议不在本条目中改变。

### [RTK Corridor MPPI 角速度饱和回调]

#### File / 文件

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- 保留 `progress_checker=0.10m / 15.0s`，继续允许直角弯处短时间原地调车头。
- 将 corridor MPPI profile 从 `vx_max=0.75`、`wz_max=0.90`、`az_max=2.6` 回调到 `vx_max=0.70`、`wz_max=0.70`、`az_max=1.8`。
- 将采样标准差从 `vx_std=0.20`、`wz_std=0.24` 回调到 `vx_std=0.18`、`wz_std=0.16`。
- 同步 velocity smoother 限幅为 `[0.70, 0.0, 0.70]`、`[0.70, 0.0, 1.4]`、`[-1.2, 0.0, -1.8]`。

#### Reason / 原因

- 2026-07-09 13:27 的 2 点路线可以完成，但第二段 `/cmd_vel` 的 `wz` 已经出现约 `0.90rad/s` 饱和和符号切换；路线完成并不代表 profile 足够平滑。
- 2026-07-09 13:37 和 13:40 的失败窗口中，RTK 始终 fixed，CPU 和 `map -> odom` 跳变都不是主因；MPPI 持续给出较大的角速度/前进速度，但 RTK 与 `/fastlio2/lio_odom` 显示车体实际位移和航向变化很小。
- 这说明上一版把直角弯从“转不动”推到了“角速度太容易打满”的另一端；继续加大角速度会更容易触发打滑、下位机限幅或局部震荡。

#### Effect / 影响

- Corridor 保留比早期 `wz_max=0.50` 更强的原地调头能力，但不再让 MPPI 长时间贴着 `0.90rad/s` 上限采样/输出。
- 下一次外场测试重点观察直角弯是否仍能转过，以及 `/cmd_vel`、`serial_twistctl`、RTK heading 是否出现“命令很大但车体不动”的失配。
- 改动仍只影响 corridor 生成的 Nav2 profile；Explore、Travel、nav-gps 和底盘协议不变。

### [RTK Corrector 远距离 yaw 杠杆限幅]

#### File / 文件

- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_authority.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `src/bringup/config/master_params.yaml`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- 新增 `limit_map_to_odom_step_for_base()`，在限制 `map→odom` 平移和 yaw 角度步长之外，按当前 `odom→base_link` 半径限制 yaw release 对 `map→base_link` 造成的等效平移。
- `rtk_map_odom_corrector` 新增 `max_base_yaw_step_m` 参数，corridor 默认设为 `0.12m`。
- 增加纯逻辑回归测试，复现 `odom→base` 半径约 `67m` 时固定 `0.5deg` yaw step 会造成约 `0.58m` 级 `map→base` 横跳的问题。

#### Reason / 原因

- 2026-07-09 14:00 corridor run 跑到第 `6/7` 个 waypoint 后，route runner 报 `ODOM_DIVERGENCE_ABORT|6|1|STEP_1.18m_IN_0.01s`。
- 离线 bag 分析显示，第 6 段 RTK 保持 `q=4`，RTK 最大单步约 `0.17m/0.20s`，FAST-LIO2 最大单步约 `0.22m`，单独 `map→odom` 平移步长约 `0.20m`，但组合后的 `map→base_link` TF 出现 `0.79m/0.008s` 级跳变。
- 根因是 `map→odom` yaw 绕 odom 原点释放；车辆离 odom 原点约 `67m` 时，固定 `0.5deg` yaw step 会通过杠杆效应产生约 `67 * sin(0.5deg) ≈ 0.58m` 的 base 平移，足以触发 route runner odom watchdog。

#### Effect / 影响

- RTK yaw 重捕获仍可继续释放，不会重新回到长时间冻结 `map→odom` 的状态。
- 车辆离 odom 原点越远，单周期 yaw release 会自动变小，避免 Nav2 看到由 TF 合成造成的米级 `map→base_link` 瞬移。
- 下一次外场若仍触发 odom watchdog，应优先检查是否还有 `map→odom` 平移 target jump、FAST-LIO2 大步跳变或 `/odom_CBoar` 串口反馈解析异常。

### [RTK Corridor MPPI 直线提速与横摆抑制]

#### File / 文件

- `src/bringup/launch/system_gps_corridor.launch.py`
- `src/bringup/config/nav2_corridor_rtk.yaml`
- `src/bringup/test/test_system_gps_corridor_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- 将 corridor 直线速度档从 `vx_max=0.70` 提升到 `vx_max=0.85`，同步 `ax_max=0.85` 与 velocity smoother 的线速度/线加速度上限。
- 保持 `wz_max=0.70`，但将 `wz_std` 从 `0.16` 小幅降到 `0.15`，并将 `az_max` 从 `1.8` 降到 `1.4`。
- 将 MPPI `temperature` 从 `0.3` 提升到 `0.45`，并启用 `regenerate_noises=true`。
- 将 `nav2_corridor_rtk.yaml` 的基准参数与 corridor launch-time override 对齐，避免直接查看 YAML 与实际运行 profile 不一致。

#### Reason / 原因

- 2026-07-09 14:23 的 7 点路线成功完成，但 rosbag 显示 `/cmd_vel` 线速度 p95 已经贴到 `0.70m/s`，约 63% 的移动窗口接近上限，说明“慢”主要来自 corridor profile 限速。
- 同一包中 `abs(wz)` p95 约 `0.64rad/s`，并出现约 `0.53次/s` 的左右换向；如果直接回到 `1.0m/s` 或继续放大角速度，横摆会被速度放大。
- RTK 全程 fixed，Jetson CPU 仍有余量；启用 `regenerate_noises` 可减少固定采样噪声造成的持续偏向，`temperature=0.45` 让 MPPI 选择更不“赢家通吃”。

#### Effect / 影响

- 下一轮外场预期直线速度从 `0.70m/s` 上限提升到 `0.85m/s`，但角速度和角加速度保持保守，避免重新进入角速度饱和。
- 该改动仍只影响 corridor RTK profile；Explore、Travel、nav-gps 和底盘协议不变。
- 若下一包仍有明显“画龙”，应优先分析 `map→odom` / `map→base_link` 平滑，而不是继续盲目降低 `wz_max`。

### [RTK nav-gps 路网选点接入 corridor 稳定链]

#### File / 文件

- `src/bringup/launch/system_nav_gps.launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/rtk_map_odom_corrector_node.py`
- `scripts/build_scene_runtime.py`
- `scripts/launch_with_logs.sh`
- `src/bringup/test/test_system_nav_gps_launch.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_rtk_authority.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- `nav-gps` 不再直接启动旧 Livox/PGO/Nav2/DWB 组合，而是复用 `system_explore.launch.py` 并传入 corridor no-TF/no-GPS PGO 配置、corridor RTK MPPI profile 和无物理 recovery BT。
- `rtk_map_odom_corrector` 新增 `scene_points_file` 和 `use_scene_identity_alignment`，可从 `scene_points.yaml` 的 fixed origin 初始化投影，并在 scene 路网模式下使用固定 ENU→map identity alignment。
- `build_scene_runtime.py` 生成的 `master_params_scene.yaml` 会写入 `rtk_map_odom_corrector` 的 scene origin、`scene_points_file`、`alignment_topic=/gps_scene/enu_to_map` 和 `use_scene_identity_alignment=true`。
- `nav-gps` 默认启动 RTK FGO shadow node，仍固定 `publish_tf=false`、`nav2_use_fgo=false`；默认 lean bag 会记录 RTK、FAST-LIO2、底盘、FGO、TF、goal/costmap 和 `/cmd_vel` 证据。
- `launch_with_logs.sh` 现在会把 `FYP_USE_RVIZ` 透传给 `nav-gps`，方便车上用 headless 启动。

#### Reason / 原因

- 之前 `corridor` 已验证 RTK authoritative `map→odom`、MPPI profile、独立高窗障碍点云和 map->odom 平滑比旧 PGO/LIO 主导链更适合当前室外实车。
- 用户希望从录好的 waypoint 路线升级到 QGIS/GPS 路网“选择目的地”导航；如果继续使用旧 `nav2_gps.yaml` DWB + PGO `map→odom`，会把已定位的问题重新带回 `nav-gps`。
- Scene route graph 的坐标本身来自 fixed ENU origin，所以 RTK authority 不能像 corridor 一样用启动位姿 bootstrap 重定义 map；必须使用 scene fixed origin 的 identity alignment。

#### Effect / 影响

- `nav-gps` 现在成为 RTK 路网选点导航入口：`gps_anchor_localizer` 负责 `NAV_READY` 和 anchor，`route_server` 负责 graph path，`rtk_map_odom_corrector` 负责唯一生产 `map→odom`。
- 导入或修改 scene / QGIS 地图后，必须重新运行 `python3 scripts/build_scene_runtime.py`，否则旧 runtime scene 不会带新的 RTK authority scene 参数。
- 旧 `nav2_gps.yaml` 保留为历史 GPS MVP profile，但当前实车调参应看 corridor RTK MPPI profile 与 `system_nav_gps.launch.py` 的 launch-time override。

### [QGIS GPS 地图编译与任意近路网起步]

#### File / 文件

- `scripts/compile_qgis_scene.py`
- `scripts/test_compile_qgis_scene.py`
- `scripts/build_scene_runtime.py`
- `scripts/nav_gps_menu.py`
- `src/bringup/launch/system_nav_gps.launch.py`
- `src/bringup/test/test_system_nav_gps_launch.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/scene_runtime.py`
- `src/navigation/gps_waypoint_dispatcher/gps_waypoint_dispatcher/goal_manager_node.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_goal_manager_route_graph.py`
- `src/navigation/gps_waypoint_dispatcher/test/test_scene_projection_fallback.py`
- `src/sensor_drivers/gnss/gnss_calibration/gnss_calibration/gps_anchor_localizer_node.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- 新增 QGIS GeoJSON 路网编译脚本，可把 `feature_type=route` 的 LineString 编译为 `scene_gps_bundle.yaml`，并按默认 `5m` 间距加密 route graph 边。
- 编译器只把 fixed origin、端点和交叉点写成稀疏 anchors；可达路线本身由加密后的 route graph 节点/边表达，避免把每个路网点都当 anchor。
- 编译器会从靠近路网的建筑物名称、端点和交叉点生成 `destinations`，用于 `nav-gps` 菜单选择目的地。
- `route_server` 在 `nav-gps` 中启用 nearest-node search；goal manager 现在用当前 `map` 下 pose 发送 `ComputeRoute(use_poses=true)`，再由 route server 把当前位置吸附到最近可通行图节点。
- `nav_gps_menu.py` 等待逻辑增加 RTK authoritative 状态，并只检查 `compute_route` / `follow_path`；实车 `nav-gps` 不再需要先进入旧 anchor `NAV_READY` 或等待 `navigate_to_pose` 才能发送目的地。
- `build_scene_runtime.py`、`scene_runtime.py` 和 `gps_anchor_localizer_node.py` 对 `pyproj` 增加本地 ENU fallback；装有 `pyproj` 时仍走原精确投影路径。

#### Reason / 原因

- 用户希望直接使用已录好的 QGIS GPS 地图，并像导航软件一样选择目的地，而不是只沿着人工录制 waypoint 顺序跑。
- 旧 anchor 机制适合少量 named points，但如果路网很密，要求车必须先靠近某个 anchor 才能规划会不自然；实际需求是“只要车在道路附近，就从最近路段进入路网”。
- QGIS 路线原始点间距可能较大；不加密直接给 A* / route server 会导致 path 太稀疏，Nav2 跟踪时容易把一个长边当成大直线段。
- 本机/分析机可能没有安装 `python3-pyproj`；如果 scene 编译或 node import 因缺依赖直接失败，会阻塞地图导航测试。

#### Effect / 影响

- 当前 `qgis_4_package/3.geojson` 可编译为约 `557` 个 route graph 节点、`558` 条边，最大边长约 `4.99m`，并生成 `12` 个可选目的地。
- `nav-gps` 可以在车辆靠近路网任意位置时规划到目标，不再只依赖少数 anchors 起步；anchors 更多用于诊断和场景语义。
- 车上使用时先编译 QGIS 地图，再运行 `python3 scripts/build_scene_runtime.py`；随后用 `make launch-nav-gps` 和菜单选择目的地。
- `pyproj` 仍是推荐依赖，但临时缺失时 QGIS scene 编译和 nav-gps scene 读取不会直接崩溃；校园尺度 fallback 主要用于保持测试/启动链路可用。

## 2026.07.12

### [修复 Foxglove 位姿目标触发适配器崩溃]

#### File / 文件

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/navigation/indoor_navigation_manager/test/test_foxglove_adapter_contract.py`

#### Change / 改动

- 将位姿目标接收日志改为 `rclpy` logger 支持的单字符串调用。
- 新增 AST 合约测试，确保适配器的 logger 调用不会传入额外位置参数。

#### Reason / 原因

- Foxglove 发布目标后，适配器使用 printf 风格的多个位置参数调用 Python `rclpy` logger，导致回调抛出 `TypeError`、节点退出，目标无法转发给 Nav2。

#### Effect / 影响

- `/move_base_simple/goal` 和 `/foxglove/goal_pose` 到达适配器后不会再因为接收日志而使节点崩溃，并可继续执行定位门控及 `NavigateToPose` 转发。

### [支持 Foxglove 非 map 坐标系目标]

#### File / 文件

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/navigation/indoor_navigation_manager/package.xml`
- `src/navigation/indoor_navigation_manager/test/test_foxglove_adapter_contract.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`

#### Change / 改动

- 位姿目标适配器新增 TF buffer/listener，并将 `base_link`、`odom` 等来源坐标系的 `PoseStamped` 转换到 `map` 后再发送给 Nav2。
- 声明 `tf2_ros` 和 `tf2_geometry_msgs` 运行依赖，并扩展合约测试。

#### Reason / 原因

- 实车 Foxglove 3D 面板发布的目标使用 `header.frame_id=base_link`；旧适配器只接受 `map`，因此目标在进入 `NavigateToPose` 前被拒绝。

#### Effect / 影响

- Foxglove 布局不再必须把目标坐标系固定为 `map`。只要当前 TF 树可解析来源坐标系到 `map`，目标即可进入 Nav2；无法转换时保持 fail-closed 并发布明确拒绝原因。

### [修复 Travel 路径平滑失败和低速无法起步]

#### File / 文件

- `src/bringup/behavior_trees/travel_nav_to_pose_fail_stop.xml`
- `src/bringup/behavior_trees/travel_nav_through_poses_fail_stop.xml`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/config/collision_monitor_travel.yaml`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- Travel fail-stop 行为树移除 `SmoothPath`，直接将 NavFn 路径交给 MPPI，同时继续禁止 Spin、BackUp 和清图恢复。
- Collision Monitor Slow 区从包围车体的矩形改为车头前方 `x=0.45~0.85m, y=+-0.30m` 缓冲区，减速比例从 35% 调整为 60%；Stop 区保持不变。
- Travel MPPI 调整为 `vx_max=0.45m/s`、`vx_std=0.20`、`ax_max=0.70m/s2`、`regenerate_noises=true`，velocity smoother 同步线速度和加速度上限。

#### Reason / 原因

- 实车三次目标中，两条 NavFn 路径在 Savitzky-Golay 平滑后进入障碍并立即失败；第三条短路径进入控制器后，Slow 区持续触发，串口只收到约 `0.02~0.034m/s`，车体反馈速度始终为零，15 秒后触发 `Failed to make progress`。
- 实时点云统计显示 Stop 区为 0 点，但旧 Slow 区有约 400 个车后/侧方点；只前进的 Travel MPPI 不应因后方/侧方点永久压低前进速度。

#### Effect / 影响

- 可规划的 NavFn 路径不会再因额外平滑越界而失败，且无前方近障碍时速度链不再固定缩放到 35%。
- 前方 Slow 区仍会降速，近车体 Stop 区、定位/点云 freshness gate、主机 300ms watchdog 和固件 500ms watchdog 均保持 fail-closed。

### [Travel 底盘模式门控与串口末级平滑]

#### File / 文件

- `src/navigation/indoor_navigation_manager/indoor_navigation_manager/foxglove_adapter_node.py`
- `src/navigation/indoor_navigation_manager/package.xml`
- `src/navigation/indoor_navigation_manager/test/test_foxglove_adapter_contract.py`
- `src/sensor_drivers/serial_twistctl/include/serial_twistctl/twist_command.hpp`
- `src/sensor_drivers/serial_twistctl/src/serial_twistctl_node.cpp`
- `src/sensor_drivers/serial_twistctl/test/test_twist_command.cpp`
- `src/sensor_drivers/serial_twistctl/test/test_watchdog_contract.py`
- `src/bringup/launch/system_travel.launch.py`
- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/package.xml`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/architecture.md`
- `docs-EN/architecture.md`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- Foxglove 适配器订阅 `/chassis/status`，仅在 `CTRL_MODE_HOST` 时接受目标；底盘退出上位机模式时取消活动目标。
- `serial_twistctl` 新增可选末级加速限制器。零速、降速和换向过零立即执行，只限制同方向速度幅值增加；watchdog 触发时同步重置限制器状态。
- Travel 启用末级 `0.30m/s2` 线加速和 `0.80rad/s2` 角加速恢复限制，并把 MPPI 回调为 `vx_max=0.30m/s`、`vx_std=0.16`、`ax_max=0.40m/s2`。

#### Reason / 原因

- 实车 `/chassis/status` 为 `ctrl_mode=2`，即使 Jetson 持续发送 `vcx=0.45m/s`，STM32 也因电机禁用保持零反馈；旧适配器仍接受目标，存在使能电机后活动目标突然执行的风险。
- 串口日志显示 `0.035 -> 0.105 -> 0.175 -> 0.245 -> 0.000 -> 0.385 -> 0.450m/s`。Collision Monitor 在 velocity smoother 下游插入 Stop，上游仍保留高速状态，Stop 解除后直接放行旧速度，造成急冲/卡顿。

#### Effect / 影响

- 底盘未使能或仍在手柄模式时，目标会明确拒绝且不会在后台持续发速度；切换模式也会取消活动目标。
- 安全停车仍为立即零速，降速不会被延迟；解除 Stop 后必须从零按限幅重新起步，避免直接跳回高速。
- 该加速限制器默认关闭，只在 Travel launch 显式启用，不改变 Corridor、Explore 或其它串口使用场景。

### [Travel 原地对准与可靠起步死区]

#### File / 文件

- `src/bringup/config/nav2_travel.yaml`
- `src/bringup/package.xml`
- `src/bringup/test/test_system_travel_launch.py`
- `docs-CN/commands.md`
- `docs-EN/commands.md`
- `docs-CN/knowledge/nav2_tuning.md`
- `docs-EN/knowledge/nav2_tuning.md`

#### Change / 改动

- 用 Rotation Shim 包装 Travel MPPI；路径方向误差超过 `0.35rad` 时，以 `0.30rad/s` 原地转向后再交给 MPPI，并在终点使用同样的原地朝向对准。
- 启用 `VelocityDeadbandCritic`，以 `[0.14, 0.0, 0.18]` 和权重 `35.0` 惩罚低于实车可靠起步区间的线/角速度轨迹。
- 显式声明 Rotation Shim 和 MPPI 控制器运行依赖。
- 扩展 Travel 配置合约测试和双语运行/调参文档。

#### Reason / 原因

- 实车保持 `ctrl_mode=0` 且定位、碰撞门控正常时，串口连续发送 `vcx=0.04~0.13m/s`、`wc=0.09~0.18rad/s`，底盘只移动数厘米，表现为电机卡顿。
- 差速换算后，小线速度与小角速度混合会让一侧轮速接近零、另一侧仍很低；底盘 PID 把近零侧锁在静摩擦区。车辆具备原地转向能力，先对准再前进比同时给微小线/角速度更符合车体特性。

#### Effect / 影响

- 起步方向偏差较大时，车体先用两侧反向轮速可靠原地对准，再进入前进跟踪；MPPI 也会避开长时间停留在实车无法稳定执行的低速区。
- `vx_max=0.30m/s`、Collision Monitor、定位门、底盘模式门、末级加速限制和全部 fail-stop 行为保持不变。
